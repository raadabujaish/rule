<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-ad-krbtgt-password-reset" name="AD — KRBTGT Account Password Reset or Change - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="critical">critical</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1558.001"/>
      <Technique id="T1558"/>
      <Technique id="T1098"/>
      <Tactic id="TA0006"/>
      <Tactic id="TA0005"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Steal or Forge Kerberos Tickets: Golden Ticket (T1558.001)" url="https://attack.mitre.org/techniques/T1558/001/"/>
      <Reference title="MITRE ATT&amp;CK - Account Manipulation (T1098)" url="https://attack.mitre.org/techniques/T1098/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title="Microsoft - AD Forest Recovery: Resetting the krbtgt password" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/ad-forest-recovery-resetting-the-krbtgt-password"/>
      <Reference title="Microsoft - KRBTGT account maintenance considerations" url="https://learn.microsoft.com/en-us/defender-for-identity/"/>
      <Reference title="Microsoft (GitHub) - New-KrbtgtKeys.ps1 (Reset the krbtgt account password/keys)" url="https://github.com/microsoft/New-KrbtgtKeys.ps1"/>
      <Reference title="Microsoft Learn - 4724(S): An attempt was made to reset an account's password" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4724"/>
      <Reference title="Microsoft Learn - 4723(S,F): An attempt was made to change an account's password" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4723"/>
      <Reference title="MITRE ATT&amp;CK - Steal or Forge Kerberos Tickets (T1558)" url="https://attack.mitre.org/techniques/T1558/"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>AD — KRBTGT Account Password Reset or Change</RuleName>
    <RuleId>nbi-ad-krbtgt-reset</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is an Elastic query rule over logs-system.security*. It matches a password change/reset event whose target is the krbtgt account:



Plain English: on a Domain Controller, someone changed the krbtgt secret - either an administrative reset (4724, the normal rotation path) or a self-service password change (4723, which is abnormal for a disabled service account and materially more suspicious). The rule captures the *actor* (winlog.event_data.SubjectUserName), the *DC* (host.name), and *which* event (event.code).

The change is a completed action, not an attempt: by the time the event is logged, the domain's ticket-signing key has already been altered. The investigative question is therefore not *did the key change* (it did) but *who changed it, from where, under what authority, and what else are they doing*.</TriggerLogic>
    <KeyAlertFields>event.code, host.name, source.ip</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$actor"/>
    <Input name="$dc_host"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the AD - KRBTGT Account Password Reset or Change detection on NBI's Elastic Security deployment. The rule fires when a Windows Security 4723 (password change) or 4724 (password reset by an administrator) event is recorded whose winlog.event_data.TargetUserName is krbtgt - the domain's Kerberos ticket-signing account.

krbtgt is the single most sensitive account in an Active Directory forest: its secret is the key with which every Kerberos TGT in the domain is signed and encrypted. It is a disabled account whose password should change only during a deliberate, change-controlled KRBTGT rotation (or a two-stage Golden Ticket remediation). Any other change is, by definition, either an authorised rotation that was not correctly announced, an emergency incident-response action, or an actor with Tier-0 control manipulating the domain's ticket-signing key.

The analyst's job is to determine, quickly and defensibly, whether a krbtgt secret change on this DC represents an authorised rotation, an emergency remediation, or hostile domain manipulation - and to classify the alert as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached.</Overview>
    <BusinessImpact>Because krbtgt signs every Kerberos ticket, whoever controls its secret controls domain-wide authentication:

- A planned KRBTGT rotation is routine hygiene - often performed twice (with a replication interval between resets) to invalidate outstanding tickets after a suspected compromise or on a schedule.
- An unplanned reset by an attacker who already holds Tier-0 can be used to evict defenders mid-incident (invalidating legitimate and IR-issued tickets) or as one step in domain-dominance manipulation.
- A 4723 (self-change) on krbtgt is especially anomalous: krbtgt is disabled and never changes its own password interactively; a 4723 against it points to direct manipulation of the account object.

An alert therefore means: on $dc_host, the krbtgt secret was changed by $actor. In NBI's environment this is a rare, high-signal event - in the validation window no krbtgt-targeted 4723/4724 was observed at all (4723/4724 exist in volume for ordinary user accounts, but none targeting krbtgt). Any firing is exceptional and must be reconciled to a specific authorised cause before it is cleared.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for AD — KRBTGT Account Password Reset or Change.</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4723", "4724")
    AND TO_LOWER(winlog.event_data.TargetUserName) == "krbtgt"
| KEEP @timestamp, host.name, event.code, winlog.event_data.SubjectUserName, winlog.event_data.SubjectDomainName, winlog.event_data.TargetUserName
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm the password-change context on the alert DC">
        <Purpose>Confirm the password-change context on the alert DC</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4723", "4724")
    AND host.name == "$dc_host"
| STATS changes = COUNT(*) BY event.code, winlog.event_data.SubjectUserName, winlog.event_data.TargetUserName
| SORT changes DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4723", "4724", "4738")
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
| STATS events = COUNT(*) BY event.code, host.name, winlog.event_data.TargetUserName
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(user.name) == "$actor"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="5" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(user.name) == "$actor"
| KEEP @timestamp, host.name, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND TO_LOWER(winlog.event_data.TargetUserName) == "$actor"
| STATS logons = COUNT(*) BY source.ip, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="7" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$dc_host"
    AND event.code IN ("4723", "4724", "4738", "4728", "4756", "5136", "4662")
| STATS events = COUNT(*), actors = COUNT_DISTINCT(winlog.event_data.SubjectUserName) BY event.code
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND TO_LOWER(winlog.event_data.TargetUserName) == "$actor"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647", "4672")
    AND TO_LOWER(winlog.event_data.TargetUserName) == "$actor"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND event.code IN ("4723", "4724", "4728", "4756", "4732", "4738", "4662", "5136")
| KEEP @timestamp, host.name, event.code, winlog.event_data.TargetUserName
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND TO_LOWER(winlog.event_data.TargetUserName) == "$actor"
    AND host.name != "$dc_host"
| STATS logons = COUNT(*) BY host.name, winlog.event_data.LogonType, source.ip
| SORT logons DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND event.code IN ("4720", "4722", "4724", "4728", "4732", "4756", "7045", "4698")
| STATS events = COUNT(*) BY event.code
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="13" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$dc_host"
| STATS special_priv_logons = COUNT(*) BY winlog.event_data.SubjectUserName
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="14" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$dc_host"
    AND event.code IN ("1102", "4719", "4739", "4713")
| STATS events = COUNT(*) BY event.code, winlog.event_data.SubjectUserName
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="15" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4662"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND host.name == "$dc_host"
| STATS operations = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY winlog.event_data.SubjectUserName
| SORT operations DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="16" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Scheduled or ad-hoc KRBTGT rotation performed by the AD/Tier-0 team as security hygiene - the single most common benign cause. Must be matched to a specific rotation change record, not assumed.</Item>
        <Item>Two-stage Golden Ticket remediation performed by incident response: two krbtgt resets separated by a replication interval. Legitimate, but often executed *outside* the normal change window - treated here as a process/misconfiguration state to reconcile to the incident record, never dismissed on sight.</Item>
        <Item>KRBTGT-rotation tooling (Microsoft's New-KrbtgtKeys.ps1 or equivalent) run by an administrator. This is authorised only when tied to a named operator and an approved task.</Item>
        <Item>krbtgt-targeted 4723/4724 has a zero baseline in NBI. Over a 4-hour estate-wide window, krbtgt appeared 0 times as a 4723/4724 target. By contrast, ordinary 4723/4724 are common: password-administration accounts such as Lana.alaa perform routine 4724 resets against user and machine accounts on nim-dc-dbap01, and users self-change (4723) constantly. So there is a large legitimate 4723/4724 stream, but none of it touches krbtgt - a krbtgt hit stands entirely alone and there is no noisy legitimate source to tune out.</Item>
        <Item>Only two DCs log this. The change is authoritative on NBI's Domain Controllers - nim-dc-dbap01 (primary, ~595k Security events/4h) and nim-dc2-dbap (~168k/4h). A krbtgt change should appear on a DC; a report of one elsewhere is a data-quality question first.</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. There is no environment-specific allow-list. Do not create a standing exception; if a scheduled rotation is formalised, reconcile each occurrence to its change record rather than muting the rule.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Preserve first, then act. Capture the krbtgt object metadata (pwdLastSet, whenChanged) and both DCs' Security logs before any change; a Tier-0 actor may be racing to clear them.</Action>
      <Action>Treat the domain as compromised on a true_positive. Coordinate with the AD/Tier-0 team and IR to perform the double KRBTGT reset per Microsoft guidance (two resets separated by a replication interval) to invalidate any forged tickets - done deliberately as remediation, not ad hoc.</Action>
      <Action>Disable/segregate the acting account if $actor is implicated, and suspend its sessions; reset its credentials (section 20).</Action>
      <Action>Isolate the actor's origin host identified in section 15.6/section 15.12 if it is a foothold rather than a sanctioned admin workstation.</Action>
      <Action>All containment changes go through the authorised, human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Complete the double KRBTGT reset and rotate all Tier-0 credentials (Domain/Enterprise/Schema Admins, DC machine accounts, and any account with directory-replication rights) - an actor who reached krbtgt likely reached these too.</Action>
      <Action>Remove any persistence discovered in section 17.2 (rogue accounts, privileged-group adds, services, scheduled tasks) and revoke outstanding Kerberos tickets.</Action>
      <Action>Hunt for prior directory-secret replication (DCSync) and forged-ticket use across the estate, using the companion replication and Golden/Silver-ticket analytics.</Action>
      <Action>Remediate the initial-access and privilege-escalation path that gave the actor Tier-0 control in the first place.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Rotate credentials in the correct order: krbtgt (twice), then Tier-0 accounts and DC machine accounts, then any service accounts exposed during the compromise window.</Action>
      <Action>Validate domain health after rotation: replication converges across both DCs, legitimate services re-authenticate, and no anomalous ticket lifetimes or RC4 requests recur.</Action>
      <Action>Return accounts/DCs to normal operation only after section 22 closing criteria are met and monitoring confirms no repeat krbtgt change and no forged-ticket indicators.</Action>
      <Action>Formalise scheduled KRBTGT rotation with change control, restrict who may reset krbtgt, and treat any future 4723 (self-change) against krbtgt as always-anomalous.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- The krbtgt change is not reconciled to an approved KRBTGT-rotation record or an active incident - a krbtgt change alone warrants IR.
- The event is a 4723 (self-change) on krbtgt, or the actor/origin is not a recognised Tier-0 administration path (section 15.6/section 15.12).
- The same actor shows Tier-0 breadth in the window: group adds, other privileged resets, directory-ACL edits (5136), or directory replication (4662) (section 17.2/section 17.3/section 17.5).
- Defence evasion appears - audit-log clearing (1102) or audit-policy change (4719) on the DC (section 17.4).
- Evidence is incomplete because the change may sit outside the window or on the second DC and cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised rotation): an approved KRBTGT-rotation change record names the operator, the DC(s), and the window; the actor/origin match a sanctioned Tier-0 path; no Tier-0 breadth. Record the reference. Do not create a standing exception.</Item>
    <Item>false_positive (blocked-malicious change): a hostile change positively proven contained before any forged-ticket use; documented as blocked-authorised, never "benign".</Item>
    <Item>misconfiguration: an emergency two-stage remediation performed by IR outside the normal change window; reconcile to the incident and record the process gap (ensure the second rotation is scheduled).</Item>
    <Item>true_positive: unauthorised krbtgt manipulation; domain treated as compromised, double reset and Tier-0 credential rotation completed, prior DCSync/forged-ticket hunt closed, and a domain-integrity incident documented.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Zero baseline = maximum fidelity. No krbtgt-targeted 4723/4724 exists in NBI's 4-hour window, against a large stream of ordinary 4723/4724. There is nothing legitimate to tune out - when this rule fires, believe it and reconcile it to a specific authorised cause.</Item>
    <Item>4723 on krbtgt is the sharpest tell. A self-change against a disabled service account is abnormal by construction; it should be escalated ahead of a 4724, which at least matches the normal admin-reset path.</Item>
    <Item>Cover both DCs and the two-stage pattern. A correct rotation shows two resets separated by a replication interval and may land on different DCs (nim-dc-dbap01, nim-dc2-dbap). Always corroborate across both DCs before concluding "absent".</Item>
    <Item>The reset is often the remediation, not the forgery. A Tier-0 actor with DCSync forges Golden Tickets without touching krbtgt; a krbtgt change more often means someone is *rotating* the key. Decide *who* and *why* - pair this rule with the DCSync/replication and Golden-Ticket analytics rather than treating it as a standalone tripwire.</Item>
    <Item>Mixed-case principals. NBI stores account names inconsistently (Lana.alaa, JAMAL.ADMIN, wahab.admin); all principal filters use TO_LOWER(...) so a case difference never hides the actor.</Item>
    <Item>Known != trusted. A recognised admin account or a "known" origin is context to verify against a change record, not an automatic pass - a compromised or misused admin identity is the exact scenario this rule exists to catch. Any scanner/automation identity is investigated identically, never auto-trusted.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) krbtgt-targeted 4723/4724 zero-baseline over 4h on logs-system.security*; (2) DCs = nim-dc-dbap01 (primary) and nim-dc2-dbap; (3) routine 4724 password administration by accounts such as Lana.alaa on nim-dc-dbap01; (4) principal-name case is inconsistent across events. All observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>