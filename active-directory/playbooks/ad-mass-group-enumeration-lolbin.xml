<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-ad-mass-group-enumeration-lolbin" name="AD — Mass Group Enumeration via LOLBin (BloodHound/SharpHound) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Discovery</Category>
    <MitreAttack>
      <Technique id="T1069.002"/>
      <Technique id="T1069"/>
      <Technique id="T1087.002"/>
      <Technique id="T1087"/>
      <Technique id="T1482"/>
      <Tactic id="TA0007"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Permission Groups Discovery: Domain Groups (T1069.002)" url="https://attack.mitre.org/techniques/T1069/002/"/>
      <Reference title="MITRE ATT&amp;CK - Account Discovery: Domain Account (T1087.002)" url="https://attack.mitre.org/techniques/T1087/002/"/>
      <Reference title="MITRE ATT&amp;CK - Domain Trust Discovery (T1482)" url="https://attack.mitre.org/techniques/T1482/"/>
      <Reference title="MITRE ATT&amp;CK - Discovery tactic (TA0007)" url="https://attack.mitre.org/tactics/TA0007/"/>
      <Reference title="SpecterOps - BloodHound / SharpHound documentation" url="https://bloodhound.readthedocs.io/en/latest/data-collection/sharphound.html"/>
      <Reference title="Microsoft Learn - 4799(S): A security-enabled local group membership was enumerated" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4799"/>
      <Reference title="Elastic Security - Detecting BloodHound/SharpHound and group-enumeration activity (prebuilt rules reference)" url="https://www.elastic.co/guide/en/security/current/prebuilt-rules.html"/>
      <Reference title="LOLBAS project - living-off-the-land binaries (net.exe, wmic.exe, rundll32.exe, mshta.exe)" url="https://lolbas-project.github.io/"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>AD — Mass Group Enumeration via LOLBin (BloodHound/SharpHound)</RuleName>
    <RuleId>nbi-ad-bloodhound-enumeration</RuleId>
    <RuleType>threshold</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is an Elastic threshold rule over logs-system.security*. Its detection filter (Kibana KQL) is:



…grouped by host.name + winlog.event_data.SubjectUserName with a count ≥ 15 threshold, and the baseline exclusion of host.name: nim-jump-apv22 and winlog.event_data.SubjectUserName: jamal.admin.

Plain English: one account, on one host, enumerated the memberships of many groups in a short time using a scripting/command interpreter. Event 4799 is emitted per group whose membership is read; a systematic sweep produces a burst of 4799 across most groups. winlog.event_data.TargetUserName on 4799 carries the group whose membership was enumerated, so the count of distinct groups measures the breadth of the sweep - the key discriminator between a targeted operational lookup and a full BloodHound collection.</TriggerLogic>
    <KeyAlertFields>event.action, event.code, host.name, process.command_line, process.args, source.ip</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$actor"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the AD - Mass Group Enumeration via LOLBin (BloodHound/SharpHound) detection on NBI's Elastic Security deployment. The rule is a threshold analytic: it fires when 15 or more Windows Security 4799 events (a security-enabled local group membership was enumerated) occur, grouped by host.name + winlog.event_data.SubjectUserName within the interval, where the enumerating winlog.event_data.CallerProcessName is a living-off-the-land binary (powershell, net.exe, net1.exe, wmic, cmd, rundll32, cscript, wscript, mshta). The deployed rule additionally excludes one baselined administrative pair (nim-jump-apv22 + jamal.admin) - treated here as context to verify, not a trusted allowlist.

Rapid, broad group-membership enumeration driven by a scripting/command LOLBin is the collection footprint of SharpHound/BloodHound, the reconnaissance tooling attackers use to map Active Directory attack paths to Domain Admin. The analyst's job is to decide whether a given burst is sanctioned discovery, an attacker mapping the domain before lateral movement, or a newly deployed management/security tool not yet baselined - classifying as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached.</Overview>
    <BusinessImpact>An alert means: on $host, $actor drove ≥15 group-membership enumerations through a LOLBin in the interval. SharpHound's default collection reads the membership of essentially every group in the domain, so a genuine collection lights up 4799 broadly and quickly, attributed to the interpreter it ran under (most often powershell.exe, sometimes net.exe/net1.exe via net localgroup/net group).

The event is a completed enumeration, not an attempt - the group data has already been read. But enumeration alone is not proof of malice: the same 4799 pattern is produced by legitimate administration, inventory/audit tooling, and endpoint-security agents. The decisive question is what the same actor did next: reconnaissance immediately followed by credential access or lateral movement is the malicious pattern; reconnaissance by a documented management/security function that maps to the host's role is authorised discovery.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for AD — Mass Group Enumeration via LOLBin (BloodHound/SharpHound).</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the threshold logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the threshold logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND (winlog.event_data.CallerProcessName LIKE "*powershell*" OR winlog.event_data.CallerProcessName LIKE "*net.exe" OR winlog.event_data.CallerProcessName LIKE "*net1.exe" OR winlog.event_data.CallerProcessName LIKE "*wmic.exe" OR winlog.event_data.CallerProcessName LIKE "*cmd.exe" OR winlog.event_data.CallerProcessName LIKE "*rundll32*" OR winlog.event_data.CallerProcessName LIKE "*cscript*" OR winlog.event_data.CallerProcessName LIKE "*wscript*" OR winlog.event_data.CallerProcessName LIKE "*mshta*")
| STATS enum_events = COUNT(*), distinct_groups = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY host.name, winlog.event_data.SubjectUserName
| WHERE enum_events >= 15
| SORT enum_events DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm the burst on the alert host + actor (all callers)">
        <Purpose>Confirm the burst on the alert host + actor (all callers)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND host.name == "$host"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
| STATS enum_events = COUNT(*), distinct_groups = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY winlog.event_data.CallerProcessName
| SORT enum_events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND host.name == "$host"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
| KEEP @timestamp, winlog.event_data.CallerProcessName, winlog.event_data.TargetUserName, winlog.event_data.SubjectUserName
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND host.name == "$host"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND (winlog.event_data.CallerProcessName LIKE "*powershell*" OR winlog.event_data.CallerProcessName LIKE "*net.exe" OR winlog.event_data.CallerProcessName LIKE "*net1.exe" OR winlog.event_data.CallerProcessName LIKE "*wmic.exe" OR winlog.event_data.CallerProcessName LIKE "*cmd.exe" OR winlog.event_data.CallerProcessName LIKE "*rundll32*" OR winlog.event_data.CallerProcessName LIKE "*cscript*" OR winlog.event_data.CallerProcessName LIKE "*wscript*" OR winlog.event_data.CallerProcessName LIKE "*mshta*")
| STATS enum_events = COUNT(*), distinct_groups = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY winlog.event_data.CallerProcessName
| SORT enum_events DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(user.name) == "$actor"
| STATS executions = COUNT(*) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(user.name) == "$actor"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="7" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
| STATS enum_events = COUNT(*), distinct_groups = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY host.name
| SORT enum_events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND host.name == "$host"
| STATS enum_events = COUNT(*), distinct_groups = COUNT_DISTINCT(winlog.event_data.TargetUserName), actors = COUNT_DISTINCT(winlog.event_data.SubjectUserName) BY winlog.event_data.CallerProcessName
| SORT enum_events DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="9" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND TO_LOWER(winlog.event_data.TargetUserName) == "$actor"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="10" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647", "4672")
    AND TO_LOWER(winlog.event_data.TargetUserName) == "$actor"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="11" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND event.code == "4799"
| KEEP @timestamp, winlog.event_data.CallerProcessName, winlog.event_data.TargetUserName
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="12" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND event.code IN ("4648", "5140", "5145")
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
    AND event.code IN ("4720", "4722", "4728", "4732", "4756", "7045", "4698")
| STATS events = COUNT(*) BY event.code
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="14" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY winlog.event_data.SubjectUserName
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="15" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND event.code IN ("1102", "4719")
| STATS events = COUNT(*) BY event.code, winlog.event_data.SubjectUserName
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="16" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4799"
    AND TO_LOWER(winlog.event_data.SubjectUserName) == "$actor"
| STATS total_enum = COUNT(*), distinct_groups = COUNT_DISTINCT(winlog.event_data.TargetUserName), hosts = COUNT_DISTINCT(host.name) BY winlog.event_data.CallerProcessName
| SORT total_enum DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="17" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Administrative scripts that legitimately enumerate group memberships (onboarding/offboarding, access reviews, net group in a logon script) run by an operator from an administrative host.</Item>
        <Item>Inventory/audit and asset-management tooling that walks group memberships by design. On NBI this is significant: the ManageEngine UEMS inventory agent (dcinventory.exe) enumerates many distinct groups on managed hosts - a benign bulk-enumeration lookalike (see section 6).</Item>
        <Item>Endpoint-security agents (e.g. Kaspersky avp.exe) that read local group membership as part of posture checks - high 4799 volume but low distinct-group breadth, and *not* via a LOLBin.</Item>
        <Item>The rule's own baseline pair (nim-jump-apv22 + jamal.admin) - an already-excluded administrative pattern; a *new* admin/host pair behaving the same way is a stale-baseline misconfiguration, not automatically benign.</Item>
        <Item>In the validation window, NBI's 4799 is driven by machine accounts and agents - not by the rule's LOLBins. The dominant 4799 producers are: machine accounts via C:\Windows\System32\svchost.exe and C:\Windows\Cluster\clussvc.exe (2 distinct groups each - routine service checks); the ManageEngine dcinventory.exe inventory agent (running as the host machine account, e.g. NIM-JUMP-APV03$, with high distinct-group counts of 27-33 - the closest benign match to a SharpHound sweep); and Kaspersky avp.exe (many interactive users, ~1 distinct group each). None of these is powershell/net/wmic/etc., so the LOLBin-restricted rule is effectively silent right now - which is the healthy state.</Item>
        <Item>nim-jump-apv03 is a real interactive jump host. It shows both machine-account service/inventory 4799 (dcinventory.exe, svchost.exe) and interactive-user 4799 via avp.exe (e.g. Wahab.Admin, plus many vendor/contractor accounts). If an interactive user there ran a LOLBin sweep, this is exactly where it would surface - so characterise the caller carefully before judging.</Item>
        <Item>Distinct-group breadth is the NBI discriminator. The benign high-volume producers cluster at either ~2 groups (service checks) or a fixed inventory set; a LOLBin producing a *broad* distinct-group count that approaches the domain's whole group structure is the SharpHound signature. Do not tune out the ManageEngine inventory agent by muting nim-jump-apv03 - scope any exception to the exact machine-account + dcinventory.exe caller.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain/quarantine) if a LOLBin-driven true_positive is confirmed, to stop the derived lateral movement. On a shared jump/VDI host, coordinate with IT to avoid dropping unrelated sessions, but prioritise containment.</Action>
      <Action>Suspend or force-logoff $actor's session and disable the account pending investigation if the user context is implicated; reset its credentials (section 20).</Action>
      <Action>Capture the collector and its output on $host (the SharpHound binary and .zip/.json) as evidence - NBI does not collect these, so host-side acquisition is the only way to prove the collection and scope what was mapped.</Action>
      <Action>Terminate the collector process if the host cannot yet be isolated.</Action>
      <Action>All containment changes go through the authorised, human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove any persistence discovered in section 17.2 (rogue accounts, privileged-group adds, services, scheduled tasks) and delete the collector binary and output identified host-side.</Action>
      <Action>Run a full anti-malware/EDR scan on $host and hunt for the same collector hash and behaviour across peers, especially other jump/VDI hosts and any host $actor touched (section 15.4).</Action>
      <Action>Assume the attack-path map is in adversary hands on a confirmed collection: prioritise remediation of the shortest paths to Tier-0 the map would reveal (over-privileged accounts, dangerous ACLs, DA sessions on non-Tier-0 hosts).</Action>
      <Action>Remediate the initial-access vector that gave $actor the foothold.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Reset $actor's password and any credentials accessible from $host during the session; if privileged accounts were exposed, rotate those too.</Action>
      <Action>Restore $host from a known-good image if a collector/persistence was planted; otherwise validate eradication holds after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms no renewed enumeration.</Action>
      <Action>Harden: constrain who may run discovery LOLBins on servers, enable command-line auditing on the jump/workstation class (currently bimodal), keep the inventory/security-agent 4799 baseline current, and monitor the sanctioned tooling so genuine SharpHound activity stands out.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and the AD team) when any of the following hold:

- The sweep is LOLBin-driven (powershell/net/wmic/…) with broad distinct-group breadth by an interactive user (section 14.2/section 15.2a/section 17.5).
- Any credential-access or lateral-movement follow-on by $actor is present (section 17.1), especially explicit-credential logons or admin-share access after the sweep.
- Persistence was established in the window (section 17.2), or defence evasion (log clearing / audit-policy change) appears (section 17.4).
- The host role, actor identity, or follow-on cannot be established - escalate as needs_escalation so endpoint IR can pull the process tree and any collector artifacts.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised discovery/administration): the caller and actor map to a documented inventory/audit/security function or a named operator (role evidence attached), breadth is consistent with that function, and there is no hands-on follow-on.</Item>
    <Item>false_positive (blocked-malicious): a hostile enumeration positively proven contained before any follow-on; documented as blocked-authorised, never "benign".</Item>
    <Item>misconfiguration: a legitimate new/changed management or security tool (or a new admin/host pair like the rule's excluded one) enumerating by design but not yet baselined; add the tool/host-account pair to the baseline.</Item>
    <Item>true_positive: a LOLBin-driven broad sweep and/or derived follow-on; host isolated, collector artifacts captured, actor credentials reset, mapped lateral paths investigated, incident documented.</Item>
    <Item>needs_escalation: handed to endpoint IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Breadth beats volume. The distinct-group count (COUNT_DISTINCT(winlog.event_data.TargetUserName)) is the true SharpHound tell - a broad sweep toward the whole group structure - more than raw event volume. NBI's benign high-volume producers cluster at ~2 groups (service checks) or a fixed inventory set.</Item>
    <Item>The caller decides everything. In the validation window NBI's 4799 is driven by svchost.exe/clussvc.exe (machine accounts, ~2 groups), dcinventory.exe (ManageEngine inventory, 27-33 groups, machine account - the benign bulk-enum lookalike), and avp.exe (Kaspersky, interactive users, ~1 group) - none a LOLBin. Always resolve CallerProcessName and the actor type (interactive user vs machine account) before judging.</Item>
    <Item>Machine account != trusted. A HOST$ machine account driving dcinventory.exe is context to verify against the ManageEngine deployment, not an automatic pass. Any scanner/automation identity is investigated identically, never auto-trusted; the rule's own nim-jump-apv22/jamal.admin exclusion is a baseline to reconcile, not a blanket trust.</Item>
    <Item>Command-line capture is bimodal and often the wrong tier. process.command_line/process.args are absent on the jump/VDI hosts where an interactive sweep is most plausible; lean on the 4799 breadth, caller image, and follow-on behaviour instead. Enabling the command-line GPO on that host class is the single highest-value hardening ask from this rule.</Item>
    <Item>The output file is the proof, and it's host-side. NBI never captures the SharpHound .zip/.json; recovering it (and the collector binary) from $host is what converts a strong behavioural case into definitive evidence.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) NBI 4799 baseline drivers = svchost/clussvc (machine, ~2 groups), dcinventory.exe (ManageEngine, 27-33 groups, machine account), avp.exe (Kaspersky, interactive, ~1 group); (2) the rule's LOLBin set is silent in the validation window; (3) nim-jump-apv03 is an interactive jump host with mixed machine-account and interactive-user 4799; (4) distinct-group breadth is the NBI discriminator. All observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>