<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-ad-shadow-credentials-keycredentiallink" name="AD — Shadow Credentials (msDS-KeyCredentialLink modified) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="critical">critical</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1098"/>
      <Technique id="T1556"/>
      <Tactic id="TA0006"/>
      <Tactic id="TA0003"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Account Manipulation (T1098)" url="https://attack.mitre.org/techniques/T1098/"/>
      <Reference title="MITRE ATT&amp;CK - Modify Authentication Process (T1556)" url="https://attack.mitre.org/techniques/T1556/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="MITRE ATT&amp;CK - Persistence tactic (TA0003)" url="https://attack.mitre.org/tactics/TA0003/"/>
      <Reference title='SpecterOps - "Shadow Credentials: Abusing Key Trust Account Mapping for Account Takeover" (Elad Shamir)' url="https://posts.specterops.io/shadow-credentials-abusing-key-trust-account-mapping-for-takeover-8ee1a53566ab"/>
      <Reference title="The Hacker Recipes - Shadow Credentials" url="https://www.thehacker.recipes/a-d/movement/kerberos/shadow-credentials"/>
      <Reference title="Whisker (key-credential attack tooling)" url="https://github.com/eladshamir/Whisker"/>
      <Reference title="Certipy - Active Directory Certificate Services abuse (incl. shadow credentials)" url="https://github.com/ly4k/Certipy"/>
      <Reference title="Microsoft Learn - Windows Hello for Business key trust and msDS-KeyCredentialLink" url="https://learn.microsoft.com/en-us/windows/security/identity-protection/hello-for-business/hello-hybrid-key-trust"/>
      <Reference title="Microsoft Learn - Event 5136 (a directory service object was modified)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5136"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>AD — Shadow Credentials (msDS-KeyCredentialLink modified)</RuleName>
    <RuleId>nbi-ad-shadow-credentials</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is a query rule on the Directory Service Changes audit. Its one-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline) is:



Plain English: any modification (5136) or creation (5137) of a directory object where the changed attribute is msDS-KeyCredentialLink. The event names the acting principal (winlog.event_data.SubjectUserName → $actor), the target object (winlog.event_data.ObjectDN → $victim_dn), and the Domain Controller that logged it (host.name → $dc_host).

Why this is high-signal: msDS-KeyCredentialLink is written rarely and by a short list of legitimate provisioners. Over NBI's directory-change stream (~2,790 5136 + ~16 5137 events in a 4-hour window), the attribute-level writes are dominated by servicePrincipalName, lockoutTime, member, and userCertificate - and msDS-KeyCredentialLink did not appear at all. A single write to it, outside a known Windows Hello enrollment, is one of the cleanest "attacker planted persistence" signatures Active Directory produces.

Note it is an effect detection: the audit records that the attribute changed, but does not decode the key-credential blob. Confirm the current key credentials on the victim</TriggerLogic>
    <KeyAlertFields>host.name, source.ip</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$actor"/>
    <Input name="$victim_dn"/>
    <Input name="$victim_sam"/>
    <Input name="$dc_host"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the AD - Shadow Credentials (msDS-KeyCredentialLink modified) detection on NBI's Elastic Security deployment. The rule fires when a Windows Security Directory Service Changes event (5136 object modified, or 5137 object created) records a write to the msDS-KeyCredentialLink attribute of an account object.

That attribute holds public-key credentials used for PKINIT - certificate/key-based Kerberos pre-authentication. Writing a key credential onto an account lets the writer subsequently authenticate as that account using an attacker-held private key, with no knowledge of the password and no password change. It is the "Shadow Credentials" attack: durable, password-independent impersonation that survives password resets. Legitimate writers of this attribute are narrow - chiefly Windows Hello for Business device provisioning and Azure AD Connect / device-registration flows.

The analyst's job is to decide, with evidence, whether the write is authorised key-credential provisioning (false_positive), an attacker planting a Shadow Credential to impersonate the victim (true_positive), a benign identity-management process operating outside the expected provisioning flow (misconfiguration), or unprovable with the telemetry at hand (needs_escalation) - with the concern highest when the victim is a privileged user or a Domain Controller computer object and the writer is not a recognised provisioning identity.</Overview>
    <BusinessImpact>An alert means: on Domain Controller $dc_host, principal $actor wrote the msDS-KeyCredentialLink attribute of account $victim_dn (event 5136 modify or 5137 create).

What is already established:

- A key credential was added or changed on the victim object. From this moment, whoever holds the corresponding private key can request a PKINIT TGT as the victim - password-independent impersonation.
- The write succeeded (5136/5137 are recorded on successful directory changes with the appropriate SACL and "Audit Directory Service Changes: Success" enabled).

What is not yet established - the investigation:

- Is $actor a sanctioned provisioning identity (Windows Hello / Azure AD Connect / device registration / a delegated enrollment account) or an ordinary/compromised principal that should never write this attribute?
- How sensitive is $victim_dn - an ordinary workstation object, a privileged user, or a Domain Controller / Tier-0 computer object (worst case: a direct path to domain compromise)?
- Has the planted key already been used to obtain a certificate-based (PKINIT) TGT (section 17.5)?

Because the attribute grants durable access, an alert is treated as critical until authorised pr</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for AD — Shadow Credentials (msDS-KeyCredentialLink modified).</Goal>
    <Workflow>
      <Step order="1" name="Confirm the key-credential write and victim (reused verbatim from the validated v2 playbook, INV-01)">
        <Purpose>Confirm the key-credential write and victim (reused verbatim from the validated v2 playbook, INV-01)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code IN ("5136","5137") AND winlog.event_data.AttributeLDAPDisplayName == "msDS-KeyCredentialLink"
    AND host.name == "$dc_host" AND winlog.event_data.ObjectDN == "$victim_dn"
    AND @timestamp >= NOW() - 4 hours
| STATS writes = COUNT(*) BY event.code, winlog.event_data.SubjectUserName, winlog.event_data.ObjectDN, host.name
| SORT writes DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Estate-wide key-credential sweep (is this happening anywhere?)">
        <Purpose>Estate-wide key-credential sweep (is this happening anywhere?)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("5136","5137")
    AND winlog.event_data.AttributeLDAPDisplayName == "msDS-KeyCredentialLink"
| STATS writes = COUNT(*), objects = COUNT_DISTINCT(winlog.event_data.ObjectDN) BY winlog.event_data.SubjectUserName, host.name
| SORT writes DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("5136","5137")
    AND host.name == "$dc_host"
    AND winlog.event_data.ObjectDN == "$victim_dn"
| KEEP @timestamp, event.code, winlog.event_data.AttributeLDAPDisplayName, winlog.event_data.SubjectUserName, winlog.event_data.ObjectClass
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$dc_host"
    AND user.name == "$actor"
| STATS executions = COUNT(*) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="5" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$dc_host"
    AND user.name == "$actor"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="6" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$actor"
| STATS logons = COUNT(*), distinct_sources = COUNT_DISTINCT(source.ip) BY host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="7" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("5136","5137")
    AND host.name == "$dc_host"
| STATS writes = COUNT(*), attrs = COUNT_DISTINCT(winlog.event_data.AttributeLDAPDisplayName), objects = COUNT_DISTINCT(winlog.event_data.ObjectDN) BY winlog.event_data.SubjectUserName
| SORT writes DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="8" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$actor"
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4768", "4769", "4776")
    AND winlog.event_data.TargetUserName == "$actor"
| STATS events = COUNT(*), distinct_hosts = COUNT_DISTINCT(host.name), distinct_sources = COUNT_DISTINCT(source.ip) BY event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("5136", "5137")
    AND host.name == "$dc_host"
    AND winlog.event_data.SubjectUserName == "$actor"
| KEEP @timestamp, event.code, winlog.event_data.AttributeLDAPDisplayName, winlog.event_data.ObjectDN, winlog.event_data.ObjectClass
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769")
    AND winlog.event_data.TargetUserName == "$actor"
    AND host.name != "$dc_host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.SubjectUserName == "$actor" AND event.code IN ("5136","5137")
    AND winlog.event_data.AttributeLDAPDisplayName IN ("msDS-KeyCredentialLink","servicePrincipalName","nTSecurityDescriptor")
    AND @timestamp >= NOW() - 4 hours
| STATS changes = COUNT(*), objects = COUNT_DISTINCT(winlog.event_data.ObjectDN) BY winlog.event_data.AttributeLDAPDisplayName
| SORT changes DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="13" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$dc_host"
| STATS special_priv_logons = COUNT(*) BY winlog.event_data.SubjectUserName
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="14" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$dc_host"
    AND event.code IN ("1102", "4719")
| STATS events = COUNT(*) BY event.code, winlog.event_data.SubjectUserName
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="15" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4768", "4769")
    AND winlog.event_data.TargetUserName == "$victim_sam"
| STATS requests = COUNT(*), distinct_sources = COUNT_DISTINCT(source.ip) BY event.code, source.ip, winlog.event_data.TicketEncryptionType
| SORT requests DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Windows Hello for Business (WHfB) enrollment. The canonical legitimate writer: when a user enrolls a device in WHfB, the provisioning flow writes a key credential to the user object. This is the primary benign cause and is identifiable by a recognised provisioning identity/flow and a matching enrollment record.</Item>
        <Item>Azure AD Connect / device registration / hybrid join. Device-registration and hybrid-join processes write key credentials as part of cloud-trust provisioning, typically from a known sync/registration service account and infrastructure.</Item>
        <Item>ADCS / certificate-enrollment automation. Certificate-provisioning identities that manage key/cert material on device objects (in NBI, the NAM-CA-APP$ enrollment app writes userCertificate to branch computer objects - a *cert*-attribute analog; a key-credential equivalent would be similarly automated). A recognised enrollment account writing to expected device objects is authorised.</Item>
        <Item>Delegated identity-management tooling performing key writes as part of a sanctioned onboarding flow - authorised, but only if it maps to a real provisioning owner (else it is a misconfiguration, not a clean FP).</Item>
        <Item>msDS-KeyCredentialLink has a zero baseline. In a 4-hour window across both DCs, no key-credential writes occurred; over 24h the discovery pass also found none. There is no noisy legitimate WHfB writer to tune out on NBI - so any firing is a strong anomaly with no standing allow-list.</Item>
        <Item>The live "key-material provisioning" analog is NAM-CA-APP$ writing userCertificate. The AD CS enrollment app account (NAM-CA-APP$, authenticating from the 10.11.18.6 management tier) writes userCertificate - not msDS-KeyCredentialLink - onto branch computer objects (e.g. CN=BZM-CUS-DK-03,OU=Computers,OU=Basra Zubair Mall,OU=NBI-Branches,DC=nbirq,DC=com). This is the closest sanctioned key-material write in the environment and the pattern a real key-credential provisioner would resemble: a recognised service account, from management infrastructure, targeting expected device objects. Use it as the reference for "what authorised looks like" - but note the rule targets msDS-KeyCredentialLink specifically, which no NBI process currently writes.</Item>
        <Item>The DCs are nim-dc-dbap01 and nim-dc2-dbap. Directory-change auditing is active there (SPN, member, UAC, userCertificate writes all present), so the SACL/audit prerequisite for this rule is met - an empty anchor reflects absence of the behaviour, not absence of auditing.</Item>
        <Item>No historical NBI benign-true-positive allow-list ships with this rule. Do not exempt a writer or victim off one alert. If an exception is ever warranted, scope it to the exact (provisioning identity, object class, OU) and only after the provisioning owner confirms.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Remove the added key credential from $victim_dn (clear the malicious msDS-KeyCredentialLink value via LDAP / DSInternals Remove-ADKeyCredential) - this revokes the attacker's password-independent access. Preserve the value first (evidence).</Action>
      <Action>Reset the victim account ($victim_sam): for a user, reset the password and, critically, revoke existing Kerberos tickets (the attacker may already hold a TGT); for a computer/DC object, treat as a domain-integrity incident - a shadow credential on a DC object can enable DCSync, so rotate/reset per DC-compromise procedure and involve IR.</Action>
      <Action>Investigate and contain $actor as potentially compromised: disable/hold the writing principal if it is not a proven provisioning identity, and isolate its origin host (section 15.6).</Action>
      <Action>Preserve volatile evidence first where feasible (the current key-credential blob on the victim, the writer's session/tickets, the DC's directory-change log) - NBI does not decode the blob, so the LDAP read is the authoritative artefact.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove every planted key credential found in the campaign (section 17.2 / section 14.2): audit msDS-KeyCredentialLink on all objects the writer touched, and on Tier-0 accounts and DC objects regardless.</Action>
      <Action>Revoke any PKINIT-obtained access: if section 17.5 shows the key was used, revoke the victim's tickets and rotate its secrets; for a computer/DC victim, follow DC-compromise eradication (krbtgt considerations if domain-wide compromise is suspected).</Action>
      <Action>Fix the right that enabled the write: find and remove the ACL/delegation that gave $actor write access to the victim's msDS-KeyCredentialLink (BloodHound "AddKeyCredentialLink" edge); this is the root cause and must be closed or the attack recurs.</Action>
      <Action>Hunt the writer's broader activity (section 15.4/section 17.1) and remediate the initial-access vector that compromised the writing principal.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Confirm the victim's key credentials are clean (only legitimate WHfB/enrollment entries remain) and that no anomalous PKINIT for the victim recurs on monitoring.</Action>
      <Action>Restore trust in affected Tier-0 assets: for a DC/DC-object involvement, validate domain integrity (replication health, no rogue key credentials or ACLs remain) before standing down.</Action>
      <Action>Return the account/object to service only after section 22 closing criteria are met.</Action>
      <Action>Harden: narrowly delegate who may write msDS-KeyCredentialLink (remove broad GenericWrite/GenericAll on user/computer objects), enable and monitor Directory Service Changes auditing on Tier-0, deploy dedicated key-credential / PKINIT anomaly detection (so a transient write is still caught), and review ADCS/PKINIT trust configuration.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- $victim_dn is a Domain Controller computer object or a Tier-0 / privileged user - treat as a domain-integrity incident immediately.
- $actor is not a recognised provisioning identity, or its origin is a workstation/foothold rather than sanctioned management infrastructure (section 15.6).
- A campaign is present: key credentials across multiple accounts, or combined msDS-KeyCredentialLink + servicePrincipalName + nTSecurityDescriptor writes by one principal (section 17.2).
- A PKINIT TGT for the victim appears after the write (section 17.5), or the key credential was added then removed within minutes (section 17.4 / section 15.1) - evidence of use-and-cleanup.
- Evidence is incomplete because of NBI's telemetry gaps (blob not decoded, ACL step not audited, provisioning owner unknown) and th</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a WHfB / Azure AD Connect / enrollment record positively matches the exact writer + victim + time, the writer is a recognised provisioning identity from a sanctioned origin, the victim is the expected object class, and there is no campaign or anomalous PKINIT. Record the enrollment reference. Scope any exception to the exact (provisioning identity, object class, OU) - never a blanket writer/victim allow.</Item>
    <Item>false_positive (blocked/authorised): the hostile key credential was positively proven removed before any PKINIT use, documented as a blocked-malicious attempt (evidence preserved, writer hunted) - never "benign"; or a sanctioned red/purple-team exercise under ROE.</Item>
    <Item>misconfiguration: a benign identity-management process wrote the key outside the expected provisioning flow; it maps to a real owner with no attacker origin/campaign/use. Reconcile and baseline.</Item>
    <Item>true_positive: Shadow Credentials confirmed; the planted key(s) removed, victim reset/rotated (DC-integrity handling if applicable), the enabling ACL/delegation fixed, writer investigated, PKINIT-use hunt completed, and no recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to AD/identity + IR with INV-01..03 and the direct LDAP key-credential read documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Zero baseline = high fidelity. msDS-KeyCredentialLink writes do not occur in NBI's directory-change stream (0 in 4h and in the 24h discovery pass). There is no legitimate WHfB writer to tune out; when this rule fires, believe it and reconcile against the provisioning inventory.</Item>
    <Item>The event is an effect, not the key. The audit proves the attribute changed but does not decode the blob - always read the victim's current msDS-KeyCredentialLink directly (LDAP/DSInternals). Empty telemetry corroboration != safe.</Item>
    <Item>Victim class drives severity. A DC computer object or Tier-0 user victim is a domain-integrity incident (DCSync/takeover path); an ordinary workstation object is lower blast radius. Read winlog.event_data.ObjectClass and the OU in $victim_dn first.</Item>
    <Item>Look for the ACL step and the delete. The classic sequence is nTSecurityDescriptor change (right acquisition) → msDS-KeyCredentialLink write → delete of the same attribute after a TGT is obtained. section 15.1 (all writes to the victim) and section 17.4 surface both ends; a write-then-delete pair is a strong true-positive tell - but NBI did not record any nTSecurityDescriptor writes in-window, so the ACL step may be invisible.</Item>
    <Item>PKINIT use is inferred, not proven. 4768 for the victim shows a TGT was requested and its crypto/pre-auth type, but not the certificate identity - treat an anomalous PKINIT TGT for the victim from an unexpected source as use.</Item>
    <Item>The real provisioning analog is NAM-CA-APP$ → userCertificate. NBI's live "authorised key-material write" pattern is the AD CS enrollment app writing certs to branch computer objects from the 10.11.18.6 tier - the shape a legitimate key-credential provisioner would take; use it as the "authorised" reference, not as an allow-list.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) msDS-KeyCredentialLink zero-baseline over 4h/24h on both DCs; (2) DCs = nim-dc-dbap01, nim-dc2-dbap, directory-change auditing active; (3) 4768 uses source.ip (not winlog.event_data.IpAddress, which is 0%) plus TicketEncryptionType/PreAuthType; (4) NAM-CA-APP$ (from 10.11.18.6) = the live cert-enrollment provisioning writer analog. All observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>