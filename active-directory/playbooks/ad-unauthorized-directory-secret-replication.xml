<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-ad-unauthorized-directory-secret-replication" name="AD — Unauthorized Directory Secret Replication - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1003"/>
      <Technique id="T1003.006"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - OS Credential Dumping: DCSync (T1003.006)" url="https://attack.mitre.org/techniques/T1003/006/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title='ADSecurity (Sean Metcalf) - "Mimikatz DCSync / Sync - Extracting Domain Credentials"' url="https://adsecurity.org/?p=1729"/>
      <Reference title="The Hacker Recipes - DCSync" url="https://www.thehacker.recipes/a-d/movement/credentials/dumping/dcsync"/>
      <Reference title='Elastic Security - "Potential Credential Access via DCSync" prebuilt rule reference' url="https://www.elastic.co/guide/en/security/current/potential-credential-access-via-dcsync.html"/>
      <Reference title="Microsoft Learn - Event 4662 (an operation was performed on an object)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4662"/>
      <Reference title="Microsoft Learn - DS-Replication-Get-Changes-All extended right" url="https://learn.microsoft.com/en-us/windows/win32/adschema/r-ds-replication-get-changes-all"/>
      <Reference title="Microsoft Learn - Reset the Kerberos krbtgt account password (Golden Ticket remediation)" url="https://learn.microsoft.com/en-us/defender-for-identity/remediation-actions"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>AD — Unauthorized Directory Secret Replication</RuleName>
    <RuleId>nbi-ad-t1003_006-secret-replication-nonsync</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is a query rule on Directory Service Access. Its one-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline) is:



Plain English: a 4662 that exercised the Get-Changes-All (secret-replication) right, performed by any principal other than the sanctioned AAD Connect SID or Local System. The event names the actor (winlog.event_data.SubjectUserName / SubjectUserSid → $subject_user / $subject_sid), the DC (host.name → $dc_host), the right (winlog.event_data.Properties), and the actor's domain (winlog.event_data.SubjectDomainName).

The behavioural idea: in a correctly-run domain, only Domain Controllers (as SYSTEM) and a small, known set of sync services replicate secrets. Proven on NBI, the only principal exercising Get-Changes-All is the sanctioned sync account. So the rule's true condition has a zero baseline - any non-sanctioned principal reading directory secrets is, by construction, one of the highest-signal events Active Directory can produce.

Critical limitation (see section 8): 4662 carries no source IP. The operator host is not in the event - it must be recovered from the actor's authentication events (4624/4768) around the same time (se</TriggerLogic>
    <KeyAlertFields>host.name, source.ip</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$subject_user"/>
    <Input name="$subject_sid"/>
    <Input name="$dc_host"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the AD - Unauthorized Directory Secret Replication detection on NBI's Elastic Security deployment. The rule fires on Windows Security 4662 (Directory Service Access) where the exercised control-access right includes DS-Replication-Get-Changes-All (GUID 1131f6ad-9c07-11d1-f79f-00c04fc2dcd2) and the acting principal (SubjectUserSid) is neither the sanctioned Azure AD Connect account nor Local System (S-1-5-18).

That right is the one that reads password hashes and secrets from the directory - it is the exact mechanism of DCSync. Because 4662 records a successfully exercised access, an alert means the secrets were replicated: this is not an attempt, it is a domain-credential-compromise event. Whoever performed it can obtain the hashes of any/all domain accounts - including krbtgt (enabling Golden Tickets) and Domain Admins - i.e. the basis for total domain takeover and durable persistence.

The analyst's job is to decide, with positively-proven evidence, whether the actor is an authorised, documented sync/migration service (false_positive), an unauthorised identity extracting domain secrets (true_positive - DCSync compromise), a recognised product reconfigured to run under an unapproved account (misconfiguration), or unprovable with the telemetry at hand (needs_escalation). Because secrets were read, IR is engaged in parallel with triage, and authorisation is never assumed from the account name.</Overview>
    <BusinessImpact>An alert means: on Domain Controller $dc_host, non-sanctioned principal $subject_user (SID $subject_sid) exercised DS-Replication-Get-Changes-All - it replicated directory secrets (DCSync).

What is already established:

- Secrets were read. 4662 logs successfully-exercised access; the Get-Changes-All right returns password material. This is post-access, not an attempt - the credential store was queried.
- The actor is not the sanctioned replicator. The rule already excluded the AAD Connect SID and SYSTEM, so $subject_user is *by definition* an identity that should not be doing this.

What is not yet established - the investigation:

- Is there a positively-documented authorisation for this identity to replicate directory data (a sanctioned migration/sync/backup service within an approved window)? If not, this is unauthorised secret extraction.
- Where did the actor operate from (section 15.6)? 4662 has no source IP; recovering the actor's source.ip from 4624/4768 identifies the operator host - a workstation/jump host is operator-driven DCSync (compromise); a sanctioned sync server points to authorised/misconfiguration.
- Was it a targeted pull or a broad privileged session (sectio</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for AD — Unauthorized Directory Secret Replication.</Goal>
    <Workflow>
      <Step order="1" name="Confirm the replication and the secret right (reused verbatim from the validated v2 playbook, INV-01)">
        <Purpose>Confirm the replication and the secret right (reused verbatim from the validated v2 playbook, INV-01)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.SubjectUserName == "$subject_user" AND event.code == "4662"
    AND @timestamp >= NOW() - 2 hours
| STATS events = COUNT(*),
        secret_right = COUNT(CASE(winlog.event_data.Properties LIKE "*1131f6ad*", 1, null))
    BY host.name
| SORT events DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="2" name="Estate-wide secret-replication baseline (who exercises Get-Changes-All?)">
        <Purpose>Estate-wide secret-replication baseline (who exercises Get-Changes-All?)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4662"
    AND winlog.event_data.Properties LIKE "*1131f6ad*"
| STATS secret_pulls = COUNT(*), dcs = COUNT_DISTINCT(host.name) BY winlog.event_data.SubjectUserName, winlog.event_data.SubjectUserSid
| SORT secret_pulls DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4662"
    AND winlog.event_data.SubjectUserSid == "$subject_sid"
    AND host.name == "$dc_host"
| STATS events = COUNT(*),
        secret_right = COUNT(CASE(winlog.event_data.Properties LIKE "*1131f6ad*", 1, null)),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY winlog.event_data.SubjectUserName, winlog.event_data.SubjectDomainName
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$dc_host"
    AND user.name == "$subject_user"
| STATS executions = COUNT(*) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="5" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$dc_host"
    AND user.name == "$subject_user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="6" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4768")
    AND winlog.event_data.TargetUserName == "$subject_user"
| STATS logons = COUNT(*), distinct_sources = COUNT_DISTINCT(source.ip) BY host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="7" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4662"
    AND host.name == "$dc_host"
| STATS events = COUNT(*),
        secret_right = COUNT(CASE(winlog.event_data.Properties LIKE "*1131f6ad*", 1, null))
    BY winlog.event_data.SubjectUserName, winlog.event_data.SubjectUserSid
| SORT secret_right DESC, events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="8" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.TargetUserName == "$subject_user"
    AND event.code IN ("4624","4768")
    AND @timestamp >= NOW() - 2 hours
| STATS logons = COUNT(*)
    BY source.ip, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4768", "4769", "4776")
    AND winlog.event_data.TargetUserName == "$subject_user"
| STATS events = COUNT(*), distinct_hosts = COUNT_DISTINCT(host.name), distinct_sources = COUNT_DISTINCT(source.ip) BY event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$subject_user"
    AND event.code IN ("4662", "4672", "5140", "5145", "4688")
| KEEP @timestamp, event.code, host.name, winlog.event_data.SubjectDomainName
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769")
    AND winlog.event_data.TargetUserName == "$subject_user"
    AND host.name != "$dc_host"
| STATS events = COUNT(*), distinct_sources = COUNT_DISTINCT(source.ip) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$subject_user"
    AND event.code IN ("5136", "5137", "4720", "4728", "4732", "7045")
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND winlog.event_data.SubjectUserName == "$subject_user"
| STATS special_priv_logons = COUNT(*) BY host.name
| SORT special_priv_logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="14" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("1102", "4719")
    AND winlog.event_data.SubjectUserName == "$subject_user"
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="15" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.SubjectUserName == "$subject_user"
    AND @timestamp >= NOW() - 2 hours
| STATS events = COUNT(*) BY event.code
| SORT events DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="16" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Authorised directory-sync / migration services. A documented sync or migration product legitimately granted replication rights during an approved change window (e.g. a directory migration, a second identity-sync tool). Authorised only with a change record naming the identity and window.</Item>
        <Item>Backup / DR products that use directory replication APIs and were sanctioned to hold the right. Legitimate when documented and running from their own server.</Item>
        <Item>A sanctioned product reconfigured to run under an unexpected account - a benign configuration error (the product is approved, the account is not on the approved-replicator list). This is a misconfiguration, not a clean FP, until the account is corrected.</Item>
        <Item>Exactly one principal replicates secrets - and the rule excludes it. Over a 4-hour window, the only identity exercising Get-Changes-All (1131f6ad) was MSOL_cb8d5eb8df87 (SID S-1-5-21-3845771475-482288069-3644183667-18109), 246 times on nim-dc-dbap01. That sanctioned SID and SYSTEM (S-1-5-18) are excluded, so the rule's true condition has a zero baseline. There is no other legitimate replicator to tune out - any firing names an identity that should never do this.</Item>
        <Item>nim-dc-dbap01 is the busy DC for replication; nim-dc2-dbap also serves it. The sanctioned account's Get-Changes-All is concentrated on nim-dc-dbap01 (with a small amount on nim-dc2-dbap). A non-sanctioned actor's 4662 could land on either DC - scope the confirm (section 14.1) to the alert's DC but sweep both (section 14.2).</Item>
        <Item>winlog.event_data.Properties reliably carries the control-access GUID on 4662 at NBI (validated: values like %%7688 {1131f6ad-…} {19195a5b-…}), so the secret-right test (Properties LIKE "*1131f6ad*") is dependable. The 1131f6aa… GUID (Get-Changes, metadata only) is the non-secret sibling - the rule and this playbook key specifically on 1131f6ad (Get-Changes-All, secrets).</Item>
        <Item>No approved-replicator allow-list beyond the sanctioned sync SID ships with this rule. Maintain that list explicitly; add a SID only after a documented authorisation, and prefer converting any named account holding replication rights to a managed/monitored identity.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Treat as domain-credential compromise from the moment secret_right &gt; 0 is confirmed for a non-sanctioned actor. The secrets are already read - containment is about limiting the attacker's *use* of them, not preventing the read.</Action>
      <Action>Rotate krbtgt twice (with the required replication interval between rotations) to invalidate any Golden Tickets the attacker can forge from the pulled krbtgt hash - the single most important containment step for DCSync.</Action>
      <Action>Reset exposed privileged and service credentials (Domain Admins, Tier-0 service accounts, and any account whose secrets were replicated) as scoped by IR; force domain-wide credential hygiene where a bulk pull is confirmed.</Action>
      <Action>Isolate and hunt the operator host recovered in section 15.6 (the source.ip) - network-contain it, preserve it for forensics, and recover the DCSync tool and any dumped output.</Action>
      <Action>Remove the unauthorised replication right: identify and strip the DS-Replication-Get-Changes-All grant on the domain object from the actor's identity (and review the domain-head ACL for other rogue grants).</Action>
      <Action>Preserve volatile evidence first where feasible (the operator host's memory/process list and tool output, the DCs' security logs, the domain-object ACL). Investigation itself is read-only; deploy/confirm changes only via the authorised human-approved DEPLOY path.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Complete the domain-secret rotation per the DCSync playbook: krbtgt (twice), privileged/service credentials, and any specifically-targeted accounts; review for Golden/Silver Tickets and Skeleton-Key-style access.</Action>
      <Action>Remove the rights-grant and any persistence (section 17.2): the rogue replication ACL, new privileged accounts, group additions, directory backdoors (key credentials, ACLs), and services created around the pull.</Action>
      <Action>Rebuild or fully remediate the operator host and any host the actor's credentials reached (section 17.1); assume credentials handled there are exposed.</Action>
      <Action>Close the path that granted the right: how did the actor obtain DS-Replication-Get-Changes-All (compromised DA/DC, or an ACL edit)? Fix the root cause - remove standing replication rights from non-managed identities - or the pull recurs.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Validate domain integrity before standing down: krbtgt rotated twice, no rogue replication rights remain on the domain object, no forged-ticket or persistence artefacts, and only the sanctioned SID exercises Get-Changes-All on monitoring (section 14.2 back to baseline).</Action>
      <Action>Restore trust in Tier-0 assets (DCs, the AAD Connect server, admin workstations) and confirm privileged credentials are reset and healthy.</Action>
      <Action>Return to service only after section 22 closing criteria are met and monitoring confirms no non-sanctioned replication recurs.</Action>
      <Action>Harden: audit which principals hold DS-Replication-Get-Changes-All and remove all but the sanctioned managed identity, isolate Tier-0, monitor replication rights on the domain object, and keep the sync-account-novel-source rule active (it covers abuse of the excluded sanctioned SID this rule cannot see).</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to IR / the AD-Tier-0 team as a domain-credential-compromise (DCSync) incident when any of the following hold (page on confirmation, not after full analysis):

- section 14.1 confirms secret_right &gt; 0 for a non-sanctioned principal - secrets were read; this alone warrants IR.
- section 15.6 shows the actor authenticated from a workstation / jump host / unexpected source (not a sanctioned sync server), or section 17.5 shows a broad privileged session.
- The replication is bulk (high events/secret_right across DCs), implying krbtgt/Domain-Admin extraction and full domain compromise.
- Persistence or a preceding rights-grant on the domain object is found (section 17.2), or the actor moved laterally (section 17.1) or cleared logs (section 17.4).
- Authorisation for this identity to replicate cannot be positively proven, or the operator origin is unclear - escalate as needs_escalatio</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a change record positively authorises this exact SID to replicate directory data within the window, section 15.6 shows it acting from its sanctioned server, and section 17.5 shows only replication + its own auth. Attach the authorisation and add the SID to the approved-replicator list; recommend converting named accounts holding replication rights to a managed identity. The account name alone never suffices.</Item>
    <Item>false_positive (blocked/authorised): a positively-proven authorised technique execution (sanctioned red/purple-team under ROE), documented as authorised - never "benign" - with the secrets-exposed scope still verified.</Item>
    <Item>misconfiguration: a recognised sanctioned product reconfigured to run under an unapproved account; origin on its normal server, no operator breadth. Engage the owner to use the approved managed account and update the approved-replicator list.</Item>
    <Item>true_positive: unauthorised DCSync confirmed; krbtgt rotated twice, exposed credentials reset, operator host contained, the replication ACL corrected/rogue right removed, domain integrity reviewed, and no recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to AD/Tier-0 + IR with section 14.1 (secret-right scope), section 15.6 (operator origin), and section 17.5 (breadth) documented; the identity treated as unauthorised until positively proven otherwise.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The core fact is settled: secrets were read. 4662 logs successfully-exercised access, so secret_right &gt; 0 is post-access - investigate and contain as compromise, do not debate whether the pull "happened."</Item>
    <Item>The SID is authoritative; the name is not. Key the anchor on SubjectUserSid (section 15.1) - names can be reused/spoofed, and the rule's exclusion is SID-based. Record $subject_sid first.</Item>
    <Item>4662 has no source IP - recovering the operator host is the whole game. section 15.6 (the actor's 4624/4768 around the pull) is the decisive pivot: a sanctioned sync server vs a workstation/jump host separates authorised from DCSync faster than anything else. An actor authenticated outside the window may lack a recoverable origin - widen carefully.</Item>
    <Item>1131f6ad = secrets; 1131f6aa = metadata. The rule and every query here key on Get-Changes-All (1131f6ad). If secret_right is 0 (only Get-Changes), re-check the alert - the critical condition is the -All right. Properties reliably carries the GUID on NBI (validated).</Item>
    <Item>The sanctioned account is the blind spot. This rule excludes the AAD Connect SID (S-1-5-21-3845771475-482288069-3644183667-18109) and S-1-5-18, so a compromise of the sync account replicates secrets without firing here - the sync-account-sign-in-from-new-source rule is the required complement, and replication-ACL/krbtgt auditing catches the rights-grant that precedes a pull.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) sole sanctioned replicator = MSOL_cb8d5eb8df87, SID S-1-5-21-3845771475-482288069-3644183667-18109, on nim-dc-dbap01; (2) Get-Changes-All has a zero baseline for all other principals (4h); (3) 4662 Properties reliably carries 1131f6ad (secrets) vs 1131f6aa (metadata); (4) 4662 has no source IP - operator host recovered from 4624/4768. All observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>