<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-credential-access-many-distinct-service-tickets" name="Potential Kerberoasting — Account Requesting Service Tickets for Many Distinct Service Accounts (4769) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1558"/>
      <Technique id="T1558.003"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Steal or Forge Kerberos Tickets (T1558)" url="https://attack.mitre.org/techniques/T1558/"/>
      <Reference title="MITRE ATT&amp;CK - Kerberoasting (T1558.003)" url="https://attack.mitre.org/techniques/T1558/003/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="Microsoft - 4769: A Kerberos service ticket was requested (event reference)" url="https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4769"/>
      <Reference title="Microsoft - Kerberos ticket encryption types (0x12 AES / 0x17 RC4)" url="https://learn.microsoft.com/en-us/windows-server/security/kerberos/kerberos-authentication-overview"/>
      <Reference title="Elastic - Potential Kerberoasting Attack (prebuilt rule reference)" url="https://www.elastic.co/docs/reference/security/prebuilt-rules/rules/windows/credential_access_kerberoasting_unusual_number_of_service_tickets_requested"/>
      <Reference title="SpecterOps / Will Schroeder - Kerberoasting revisited" url="https://posts.specterops.io/kerberoasting-revisited-d434351bd4d1"/>
      <Reference title="GhostPack - Rubeus (kerberoast)" url="https://github.com/GhostPack/Rubeus"/>
      <Reference title="Microsoft - Group Managed Service Accounts (gMSA) overview" url="https://learn.microsoft.com/en-us/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Potential Kerberoasting — Account Requesting Service Tickets for Many Distinct Service Accounts (4769)</RuleName>
    <RuleId>nbi-kerberoasting-excessive-service-tickets-4769</RuleId>
    <RuleType>threshold</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is a KQL threshold rule. Conceptually (from the rule definition):



Threshold: group by winlog.event_data.TargetUserName, fire at cardinality ≥ 10 distinct winlog.event_data.ServiceName. Plain English: one ordinary account requested service tickets for at least ten distinct user-type service accounts in a short span. The exclusions matter: *$ removes machine accounts (as requesters) and machine SPNs (as targets); */* removes host-based SPNs (e.g. HOST/…, MSSQL/… written with a slash), narrowing the target set to user-type service accounts - the roastable, crackable ones; krbtgt is excluded as it is not a roast target.

One-line Kibana KQL filter for pivoting in Discover / Timeline (the requester's qualifying requests):



The rule counts distinct services, not raw request volume - so an account that requests one service thousands of times does not trip it, while an account that touches ten distinct user-service accounts once each does. That is the roast-harvest shape.</TriggerLogic>
    <KeyAlertFields>source.ip, host.name</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$requester"/>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Potential Kerberoasting - Account Requesting Service Tickets for Many Distinct Service Accounts detection on NBI's Elastic Security deployment. The rule is a threshold analytic on Windows Security Event 4769 (Kerberos service-ticket request): it fires when one requesting account (winlog.event_data.TargetUserName) requests service tickets for 10 or more DISTINCT service accounts (winlog.event_data.ServiceName) within the interval - after excluding machine requesters, machine/host-based service targets, and krbtgt.

That fan-out is the signature of Kerberoasting: an account sweeping the domain for user-type service accounts and requesting their service tickets so the encrypted ticket material can be cracked offline to recover the service-account passwords. The analyst's job is to decide whether this is a legitimate broad-access application/account that naturally touches many services (false_positive - authorised), an actor harvesting tickets for offline cracking (true_positive), a scanning/inventory tool enumerating services (misconfiguration if benign-but-unbaselined - but never auto-trusted), or unprovable from telemetry (needs_escalation).</Overview>
    <BusinessImpact>An alert means: $requester requested Kerberos service tickets for ≥10 distinct user-type service accounts within the interval, from $source_ip. Each 4769 is the domain controller issuing a service ticket whose encrypted portion is derived from the target service account's password hash - so a broad fan-out means the requester now holds crackable material for many service accounts.

The discriminators the investigation turns on: how many distinct services and how fast (a tight burst is roasting; a high-but-stable-over-time count is more like a broad-access app); whether an RC4 (0x17) downgrade accompanies the fan-out (deliberately requesting crackable material - see the companion RC4 playbook); and whether the requester/source is a documented broad-access function. It does *not* by itself prove the tickets were cracked or the passwords fell - that is offline and invisible; the alert is the harvest, and response assumes exposure.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Potential Kerberoasting — Account Requesting Service Tickets for Many Distinct Service Accounts (4769).</Goal>
    <Workflow>
      <Step order="1" name="Confirm the distinct-service fan-out for the requester (reproduce the rule)">
        <Purpose>Confirm the distinct-service fan-out for the requester (reproduce the rule)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
    AND NOT winlog.event_data.TargetUserName LIKE "*$*"
    AND NOT winlog.event_data.ServiceName LIKE "*$*"
    AND NOT winlog.event_data.ServiceName LIKE "*/*"
    AND NOT winlog.event_data.ServiceName LIKE "krbtgt*"
| STATS distinct_services = COUNT_DISTINCT(winlog.event_data.ServiceName), requests = COUNT(*), sources = COUNT_DISTINCT(source.ip)
| LIMIT 5
]]>
        </Query>
      </Step>
      <Step order="2" name="Enumerate the services and their encryption (the roast target set)">
        <Purpose>Enumerate the services and their encryption (the roast target set)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
    AND NOT winlog.event_data.ServiceName LIKE "*$*"
    AND NOT winlog.event_data.ServiceName LIKE "*/*"
    AND NOT winlog.event_data.ServiceName LIKE "krbtgt*"
| STATS requests = COUNT(*) BY winlog.event_data.ServiceName, winlog.event_data.TicketEncryptionType
| SORT requests DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
    AND NOT winlog.event_data.ServiceName LIKE "*$*"
    AND NOT winlog.event_data.ServiceName LIKE "*/*"
    AND NOT winlog.event_data.ServiceName LIKE "krbtgt*"
| KEEP @timestamp, winlog.event_data.ServiceName, winlog.event_data.TicketEncryptionType, source.ip, host.name
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.TargetUserName == "$requester"
    AND event.code IN ("4768", "4769", "4624", "4625")
| STATS events = COUNT(*), services = COUNT_DISTINCT(winlog.event_data.ServiceName), sources = COUNT_DISTINCT(source.ip) BY event.code
| SORT events DESC
| LIMIT 12
]]>
        </Query>
      </Step>
      <Step order="5" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
| STATS requests = COUNT(*), services = COUNT_DISTINCT(winlog.event_data.ServiceName), last_seen = MAX(@timestamp) BY host.name
| SORT requests DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="6" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND source.ip == "$source_ip"
| STATS requests = COUNT(*), distinct_services = COUNT_DISTINCT(winlog.event_data.ServiceName), requesters = COUNT_DISTINCT(winlog.event_data.TargetUserName)
| LIMIT 5
]]>
        </Query>
      </Step>
      <Step order="7" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.TargetUserName == "$requester"
    AND event.code IN ("4768", "4769")
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.TicketEncryptionType, source.ip
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4768", "4769")
    AND winlog.event_data.TargetUserName == "$requester"
| KEEP @timestamp, event.code, winlog.event_data.ServiceName, winlog.event_data.TicketEncryptionType, source.ip, host.name
| SORT @timestamp ASC
| LIMIT 300
]]>
        </Query>
      </Step>
      <Step order="9" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4648", "5140", "5145")
| STATS events = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4720", "7045", "4698")
    AND winlog.event_data.SubjectUserName LIKE "Docsafe*"
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND winlog.event_data.TargetUserName == "$requester"
| STATS special_priv_logons = COUNT(*) BY host.name
| SORT special_priv_logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="12" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("1102", "4719")
    AND winlog.event_data.SubjectUserName LIKE "Docsafe*"
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="13" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
    AND NOT winlog.event_data.ServiceName LIKE "*$*"
    AND NOT winlog.event_data.ServiceName LIKE "*/*"
    AND NOT winlog.event_data.ServiceName LIKE "krbtgt*"
| STATS requests = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY winlog.event_data.ServiceName, winlog.event_data.TicketEncryptionType
| SORT requests DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="14" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Broad-access applications / service accounts that legitimately request tickets for many services as part of normal operation (federation brokers, gateways, monitoring, backup). These are false_positive (authorised) only when the account/source maps to a documented broad-access function and the pattern is stable (not a bursty harvest) - confirmed, not assumed.</Item>
        <Item>Vulnerability scanners / inventory tools enumerating services by design. Never auto-trusted - investigated identically to any source; if benign but un-baselined, that is a misconfiguration (stale detection baseline), not an automatic pass.</Item>
        <Item>Shared multi-user hosts (Citrix/RDS/terminal servers) where many users' ticket requests aggregate under one source, inflating the source's apparent fan-out (though the rule keys on the *requesting account*, not the source).</Item>
        <Item>Legitimate admin tooling that touches many services during maintenance.</Item>
        <Item>The rule is quiet in NBI - the top qualifying requester reached only 2 distinct user-type services. Docsafe@NBIRQ.COM (a document-management service account in the 10.11.15.0/24 app range) requested tickets for its own docsafe service plus machine SPNs (which the rule excludes) - 2 distinct user-type services over 57 requests from 10.11.15.25. That is a stable broad-access pattern well under the threshold. Because the baseline is this low, a genuine breach of 10+ distinct services is a strong, meaningful exception.</Item>
        <Item>source.ip is populated on 100% of NBI 4769 - so source pivoting is reliable, despite the rule note's winlog.event_data.IpAddress caveat (that field is empty on NBI; ECS source.ip is the one to use).</Item>
        <Item>The domain is AES-first. 4769 encryption in the window was overwhelmingly 0x12 (AES256) plus 0xffffffff (unspecified); no 0x17 (RC4) was observed. An RC4 among a fan-out is therefore a sharp escalator (pair with the RC4 companion rule).</Item>
        <Item>10.11.15.0/24 and the VDI ranges (10.11.101/102.0/24) are app/Citrix/aggregator space - high 4769 volume from these sources is expected, but the *requesting account's* distinct-service cardinality is what the rule (and this playbook) judge.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>If roasting is confirmed: treat every swept service account's password as exposed. Prioritise rotation of high-value service accounts (SQL/app/backup) immediately (section 20), and disable/reset the requesting account ($requester) pending investigation of its compromise.</Action>
      <Action>Isolate the source $source_ip if it is a workstation/unknown host; if it is shared infrastructure (Citrix/RDS/app), coordinate rather than blanket-block.</Action>
      <Action>Preserve evidence - the 4769 fan-out, the service set, and the source-host state - before remediation.</Action>
      <Action>Investigation is read-only; containment changes go via the authorised human-approved DEPLOY path.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate the exposed service-account passwords - long/complex or, preferably, migrate to gMSA so the passwords are machine-managed and not crackable; enforce AES-only on those accounts to remove RC4 exposure.</Action>
      <Action>Investigate and contain the requesting account and source host - how $requester was used to roast (compromise of that account, or an attacker-controlled host), and remove any roasting tooling on the source host.</Action>
      <Action>Hunt for offline-cracking follow-on - service-account logons from new sources (section 17.1) indicating a cracked credential in use.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Complete service-account rotation (high-value first) and confirm the applications relying on them are updated; prefer gMSA where possible.</Action>
      <Action>Reset the requesting account and any credentials it exposed; review its entitlements.</Action>
      <Action>Enforce AES and remove RC4 on service accounts and services (removes the downgrade avenue).</Action>
      <Action>Return accounts/hosts to service only after section 22 closing criteria are met and monitoring confirms no renewed fan-out.</Action>
      <Action>Keep the fan-out analytic paired with the RC4-4769 tripwire so both bulk and stealthy roasting are covered (section 23).</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / IR + the AD team when any of the following hold:

- A large, bursty distinct-service fan-out from a non-sanctioned account (section 14.1), especially with RC4 (0x17) among the requests (section 14.2) or high-value service accounts targeted.
- The requesting account or source then logs on as a swept service account or reaches new hosts/shares (section 17.1), or the account gains special privileges (section 17.3).
- Evidence of log clearing / audit tampering on the DCs (section 17.4).
- The account role, source nature, or harvest outcome cannot be established - escalate as needs_escalation with the interval reconciled and the gap named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>true_positive: roasting confirmed (large/bursty fan-out, RC4 and/or high-value targets, non-sanctioned account); exposed service-account passwords rotated (high-value first / gMSA), requesting account and source contained, offline-cracking and follow-on-logon hunt completed, incident documented.</Item>
    <Item>false_positive (authorised): account/source confirmed as a documented broad-access function whose stable pattern matches, no RC4/burst - documented; account-source pair baselined in known_infrastructure.</Item>
    <Item>false_positive (blocked): a hostile harvest positively proven blocked (requests denied / no usable tickets) - documented as a blocked malicious attempt, never "benign".</Item>
    <Item>misconfiguration: a benign inventory/monitoring tool enumerating services, un-baselined - baselined.</Item>
    <Item>needs_escalation: handed to the AD team + Tier 3 with the interval reconciled and the role/outcome gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Count distinct services, and check the burst. The rule fires on distinct-service cardinality, not raw volume; the harvest signature is *many distinct user-services in a tight span*. Reconcile the &lt;=4h window against the alert interval before concluding "low". KB-worthy (NBI): top qualifying requester Docsafe@NBIRQ.COM reached only 2 distinct user-services - the rule is quiet, so a 10+ breach is high-signal.</Item>
    <Item>RC4 is the escalator. NBI is AES-first (no 0x17 in the window); any RC4 among a fan-out is a deliberate downgrade for faster cracking - pair this investigation with the RC4-4769 companion rule. KB-worthy (NBI): 4769 encryption is 0x12/0xffffffff only; no RC4 baseline.</Item>
    <Item>source.ip is the reliable pivot. It is 100%-populated on NBI 4769; ignore the empty winlog.event_data.IpAddress. KB-worthy (NBI): source.ip 100% on 4769; 10.11.15.25/10.11.15.0/24 is app/Citrix space; DCs are nim-dc-dbap01/nim-dc2-dbap.</Item>
    <Item>Scanners/inventory tools are investigated, not whitelisted. A tool enumerating services is a hypothesis to confirm; if benign but un-baselined it is a misconfiguration, not an automatic pass.</Item>
    <Item>Assume exposure on a real harvest. Cracking is offline and invisible; the alert is the exposure - rotate the swept service accounts (gMSA/AES) rather than waiting for evidence of a crack. All observations live on 2026-08-17.</Item>
  </Limitations>
</Playbook>