<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-credential-access-rc4-ticket-aes-domain" name="Credential Access — RC4 (0x17) Kerberos Ticket in an AES-Only Domain (Downgrade / Roast / Forged Ticket) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1558"/>
      <Technique id="T1558.003"/>
      <Technique id="T1558.001"/>
      <Technique id="T1550.003"/>
      <Technique id="T1550"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Steal or Forge Kerberos Tickets (T1558)" url="https://attack.mitre.org/techniques/T1558/"/>
      <Reference title="MITRE ATT&amp;CK - Kerberoasting (T1558.003)" url="https://attack.mitre.org/techniques/T1558/003/"/>
      <Reference title="MITRE ATT&amp;CK - Golden Ticket (T1558.001)" url="https://attack.mitre.org/techniques/T1558/001/"/>
      <Reference title="MITRE ATT&amp;CK - Use Alternate Authentication Material: Pass the Ticket (T1550.003)" url="https://attack.mitre.org/techniques/T1550/003/"/>
      <Reference title="Microsoft - 4769: A Kerberos service ticket was requested (encryption types)" url="https://learn.microsoft.com/en-us/previous-versions/windows/it-pro/windows-10/security/threat-protection/auditing/event-4769"/>
      <Reference title="Microsoft - Decrypting the Selection of Supported Kerberos Encryption Types" url="https://techcommunity.microsoft.com/t5/core-infrastructure-and-security/decrypting-the-selection-of-supported-kerberos-encryption-types/ba-p/1628797"/>
      <Reference title="Microsoft - AES vs RC4 and msDS-SupportedEncryptionTypes" url="https://learn.microsoft.com/en-us/windows-server/security/kerberos/kerberos-authentication-overview"/>
      <Reference title="SpecterOps - Kerberoasting revisited (RC4 downgrade)" url="https://posts.specterops.io/kerberoasting-revisited-d434351bd4d1"/>
      <Reference title="The Hacker Recipes - Silver &amp; Golden tickets" url="https://www.thehacker.recipes/ad/movement/kerberos/forged-tickets"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Credential Access — RC4 (0x17) Kerberos Ticket in an AES-Only Domain (Downgrade / Roast / Forged Ticket)</RuleName>
    <RuleId>nbi-kerberos-rc4-downgrade</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is a KQL query rule (from the rule definition):



Plain English: any service-ticket request encrypted with RC4-HMAC (0x17) for a service other than krbtgt. In an AES-only domain, every such event is anomalous by design - the rule is a high-fidelity tripwire that should be near-silent (and is: no RC4 was observed in the validation window).

One-line Kibana KQL filter for pivoting in Discover / Timeline (the source's RC4 requests):



Why RC4 matters: RC4-HMAC ticket material is derived from the service account's NTLM hash and cracks far faster than AES256 - it is the currency of Kerberoasting and of Silver/Golden ticket forgery (Mimikatz/impacket default to RC4). In a domain that otherwise issues AES, an RC4 request is one of the highest-fidelity indicators that an attacker is harvesting or forging Kerberos credentials.</TriggerLogic>
    <KeyAlertFields>source.ip, host.name</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$requester"/>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Credential Access - RC4 (0x17) Kerberos Ticket in an AES-Only Domain detection on NBI's Elastic Security deployment. The rule is a query analytic on Windows Security Event 4769 (Kerberos service-ticket request) that fires when a ticket is issued with winlog.event_data.TicketEncryptionType = 0x17 (RC4-HMAC) for a non-krbtgt service.

NBI is an AES-first domain: legitimate 4769 requests are 0x12 (AES256). An RC4 ticket is therefore anomalous and is the footprint of a deliberate encryption downgrade for offline cracking (Kerberoasting), a forged Silver/Golden ticket, or a legacy system reintroducing RC4. The analyst's job is to decide whether this is a legacy system legitimately negotiating RC4 (misconfiguration - still remediate), an attacker forcing RC4 to roast or presenting a forged ticket (true_positive), or an RC4 request that was rejected/failed so no crackable material was issued (false_positive - blocked malicious, never "benign").</Overview>
    <BusinessImpact>An alert means: on a domain controller, a service ticket was requested/issued with RC4 encryption for a non-krbtgt service, from $source_ip by $requester. The meaning branches on the *targeting shape* (section 14.1):

- One high-value service repeated → a targeted roast or a Silver-ticket pattern.
- Many distinct services → bulk Kerberoasting with a deliberate downgrade.
- A krbtgt-like or unusual service / anomalous context → possible forged-ticket use (Golden/Silver).
- A single host/app consistently emitting RC4 → a genuine legacy client (misconfiguration).

The decisive test is whether the requesting identity is normally AES-only (making this RC4 a genuine *downgrade*) versus a system that always speaks RC4 (legacy).</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Credential Access — RC4 (0x17) Kerberos Ticket in an AES-Only Domain (Downgrade / Roast / Forged Ticket).</Goal>
    <Workflow>
      <Step order="1" name="Confirm the RC4 request from this source (reproduce the rule)">
        <Purpose>Confirm the RC4 request from this source (reproduce the rule)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TicketEncryptionType == "0x17"
    AND winlog.event_data.ServiceName != "krbtgt"
    AND source.ip == "$source_ip"
| STATS rc4_requests = COUNT(*), services = COUNT_DISTINCT(winlog.event_data.ServiceName) BY winlog.event_data.TargetUserName, winlog.event_data.ServiceName, host.name
| SORT rc4_requests DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Downgrade test - is the requester normally AES-only">
        <Purpose>Downgrade test - is the requester normally AES-only</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
| STATS requests = COUNT(*), services = COUNT_DISTINCT(winlog.event_data.ServiceName) BY winlog.event_data.TicketEncryptionType
| SORT requests DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
| KEEP @timestamp, winlog.event_data.ServiceName, winlog.event_data.TicketEncryptionType, source.ip, host.name
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.TargetUserName == "$requester"
    AND event.code IN ("4768", "4769")
| STATS events = COUNT(*), services = COUNT_DISTINCT(winlog.event_data.ServiceName), sources = COUNT_DISTINCT(source.ip) BY event.code, winlog.event_data.TicketEncryptionType
| SORT events DESC
| LIMIT 12
]]>
        </Query>
      </Step>
      <Step order="5" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TargetUserName == "$requester"
| STATS requests = COUNT(*), rc4 = COUNT(*) WHERE winlog.event_data.TicketEncryptionType == "0x17", last_seen = MAX(@timestamp) BY host.name
| SORT requests DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="6" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND source.ip == "$source_ip"
| STATS requests = COUNT(*), distinct_services = COUNT_DISTINCT(winlog.event_data.ServiceName), requesters = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY winlog.event_data.TicketEncryptionType
| SORT requests DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="7" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.TargetUserName == "$requester"
    AND event.code IN ("4768", "4769")
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.TicketEncryptionType, source.ip
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4768", "4769")
    AND winlog.event_data.TargetUserName == "$requester"
| KEEP @timestamp, event.code, winlog.event_data.ServiceName, winlog.event_data.TicketEncryptionType, source.ip, host.name
| SORT @timestamp ASC
| LIMIT 300
]]>
        </Query>
      </Step>
      <Step order="9" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4648", "5140", "5145")
| STATS events = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4720", "7045", "4698")
    AND winlog.event_data.SubjectUserName LIKE "Docsafe*"
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND winlog.event_data.TargetUserName == "$requester"
| STATS special_priv_logons = COUNT(*) BY host.name
| SORT special_priv_logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="12" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("1102", "4719")
    AND winlog.event_data.SubjectUserName LIKE "Docsafe*"
| STATS events = COUNT(*) BY event.code, host.name
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="13" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4769"
    AND winlog.event_data.TicketEncryptionType == "0x17"
    AND winlog.event_data.ServiceName != "krbtgt"
| STATS rc4_requests = COUNT(*), requesters = COUNT_DISTINCT(winlog.event_data.TargetUserName), sources = COUNT_DISTINCT(source.ip) BY winlog.event_data.ServiceName
| SORT rc4_requests DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Genuine legacy systems/applications that negotiate RC4 because they cannot do AES - a real hardening gap (misconfiguration), evidenced by a *consistent* RC4 profile for a specific host/app, not a one-off downgrade. Still remediate to AES.</Item>
        <Item>Accounts explicitly configured to allow RC4 (msDS-SupportedEncryptionTypes) - a configuration weakness to fix, not necessarily an attack.</Item>
        <Item>A blocked/failed RC4 request - if the request was rejected and no usable ticket was issued, that is a blocked malicious attempt (false_positive of the "blocked" form), documented, never "benign".</Item>
        <Item>The AES-only baseline holds - no RC4 was observed. In the window, 4769 encryption was 0x12 (AES256, ~434k) and 0xffffffff (unspecified, ~795); 0x17 (RC4) count was 0. So any 0x17 is a strong, high-fidelity exception - there is no benign RC4 to tune out.</Item>
        <Item>0xffffffff (unspecified) is not RC4. It appears in the baseline and is not what this rule targets; do not confuse it with 0x17.</Item>
        <Item>source.ip is 100%-populated on NBI 4769 - reliable source pivoting (use source.ip, not the empty winlog.event_data.IpAddress).</Item>
        <Item>The requester-baseline test is decisive here. A requester whose profile is otherwise entirely 0x12 that suddenly emits 0x17 is a genuine downgrade; a host/app consistently emitting RC4 is legacy. The validated Docsafe@NBIRQ.COM baseline is 0x12/0xffffffff (no RC4), the pattern an attacker's downgrade would break.</Item>
        <Item>No blanket allow-list. If a legacy RC4 client is confirmed, scope any interim allowance to that single host and plan its AES remediation - never broaden.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>If a downgrade roast or forged ticket is confirmed: treat the targeted service account(s) as exposed and rotate immediately (section 20); for forged-ticket cases, treat as a domain-integrity event and engage Tier-0.</Action>
      <Action>Isolate the source $source_ip if it is a workstation/unknown host; coordinate for shared infrastructure.</Action>
      <Action>Disable/reset the requesting account ($requester) pending investigation of its compromise.</Action>
      <Action>Preserve evidence - the RC4 4769(s), the service targets, the requester baseline, and the source-host state - before remediation. Investigation is read-only; changes go via the authorised human-approved DEPLOY path.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate exposed service-account passwords (long/complex or gMSA) and enforce AES-only on those accounts/services (remove RC4 from supported etypes) so the downgrade avenue is closed.</Action>
      <Action>For forged-ticket cases: rotate KRBTGT twice (Golden), review/rotate the specific service key (Silver), and conduct a Tier-0 review for prior directory-secret replication (DCSync) that may have supplied the keys.</Action>
      <Action>Remove any forgery/roasting tooling on the source host and remediate its foothold.</Action>
      <Action>Fix any legacy RC4 client identified (misconfiguration branch) by migrating it to AES.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Complete credential rotation (exposed service accounts; KRBTGT for Golden) and confirm dependent applications are updated.</Action>
      <Action>Eliminate residual RC4 - enforce AES on accounts and services, remove RC4 from supported etypes where possible, and verify no legacy client reintroduces it.</Action>
      <Action>Return accounts/hosts to service only after section 22 closing criteria are met and monitoring confirms no renewed RC4.</Action>
      <Action>Keep RC4-4769 as an always-on high-fidelity tripwire, paired with the distinct-service fan-out analytic so both stealthy single-ticket and bulk-roast cases are covered (section 23).</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / IR + the AD/Tier-0 team when any of the following hold:

- A confirmed downgrade from an AES-only requester (section 14.2) with a roast (many distinct services) or forged-ticket (krbtgt-like/anomalous) shape (section 14.1/section 15.12).
- The requesting account gains special privileges (section 17.3) or the credential is used (logons/share access, section 17.1).
- Evidence pointing to Golden/Silver forgery (RC4 service-ticket with no preceding TGT; anomalous PAC/lifetime) - a domain-integrity event.
- Log clearing / audit tampering on the DCs (section 17.4).
- The requester baseline, source nature, or ticket outcome cannot be established - escalate as needs_escalation with the alert timestamp reconciled and the gap named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>true_positive: RC4 downgrade for roasting, or a forged Silver/Golden ticket, confirmed; exposed service-account passwords rotated (gMSA/AES), KRBTGT rotated for Golden cases, source contained, prior-compromise/offline-cracking hunt completed, incident documented.</Item>
    <Item>false_positive (blocked): the RC4 request proven rejected/failed with no usable ticket issued - documented as a blocked malicious attempt, never "benign"; source hunted.</Item>
    <Item>misconfiguration: a specific host/app confirmed as a legacy RC4 client - AES remediation planned; any interim allowance scoped to that single host, never broadened.</Item>
    <Item>needs_escalation: handed to the AD/Tier-0 team with the alert timestamp reconciled and the identity/outcome gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>RC4 in an AES domain is high-fidelity - believe it. NBI's baseline is 0x12/0xffffffff with no 0x17, so any RC4 is a strong exception with nothing benign to tune out. KB-worthy (NBI): 4769 encryption baseline is AES-only (0x12) plus 0xffffffff; RC4 (0x17) count 0.</Item>
    <Item>The downgrade test is the discriminator. An AES-only requester emitting RC4 is a genuine downgrade (attack); a host/app consistently emitting RC4 is legacy (misconfiguration). Run section 14.2 every time. KB-worthy (NBI): Docsafe@NBIRQ.COM baseline is AES-only - the pattern a downgrade would break.</Item>
    <Item>0xffffffff is not RC4. It is "unspecified" and part of the normal baseline - do not treat it as the alert condition.</Item>
    <Item>Forgery needs corroboration beyond 4769. No ticket lifetime is logged; use the absence of a preceding 4768 TGT, anomalous PAC/lifetime, and privilege use (section 15.12/section 17.3) to corroborate Silver/Golden. KB-worthy (NBI): 4769 lacks ticket lifetime; forged-ticket confirmation requires TGT/PAC corroboration.</Item>
    <Item>source.ip is the reliable pivot (100% on NBI 4769; ignore empty winlog.event_data.IpAddress); 10.11.15.0/24 is app/Citrix space; DCs are nim-dc-dbap01/nim-dc2-dbap. Assume exposure on a real hit and rotate/KRBTGT rather than waiting for proof of a crack. All observations live on 2026-08-17.</Item>
  </Limitations>
</Playbook>