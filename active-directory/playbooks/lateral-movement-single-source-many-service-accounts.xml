<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-lateral-movement-single-source-many-service-accounts" name="Lateral Movement — Single Source Authenticating as Many Distinct Service/Privileged Accounts - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Lateral Movement</Category>
    <MitreAttack>
      <Technique id="T1078"/>
      <Technique id="T1078.002"/>
      <Technique id="T1021.001"/>
      <Technique id="T1021"/>
      <Technique id="T1550"/>
      <Tactic id="TA0008"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts (T1078)" url="https://attack.mitre.org/techniques/T1078/"/>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts: Domain Accounts (T1078.002)" url="https://attack.mitre.org/techniques/T1078/002/"/>
      <Reference title="MITRE ATT&amp;CK - Remote Services: Remote Desktop Protocol (T1021.001)" url="https://attack.mitre.org/techniques/T1021/001/"/>
      <Reference title="MITRE ATT&amp;CK - Use Alternate Authentication Material (T1550)" url="https://attack.mitre.org/techniques/T1550/"/>
      <Reference title="MITRE ATT&amp;CK - Lateral Movement tactic (TA0008)" url="https://attack.mitre.org/tactics/TA0008/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="Microsoft Learn - 4624: An account was successfully logged on (LogonType reference)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4624"/>
      <Reference title="Microsoft Learn - 4625: An account failed to log on (SubStatus codes)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4625"/>
      <Reference title="Microsoft Learn - 4769: A Kerberos service ticket was requested" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4769"/>
      <Reference title="Microsoft Learn - 5145: A network share object was checked for access (RelativeTargetName)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5145"/>
      <Reference title="Elastic Security - ES|QL reference (RLIKE, STATS, COUNT_DISTINCT)" url="https://www.elastic.co/guide/en/elasticsearch/reference/current/esql.html"/>
      <Reference title="MITRE ATT&amp;CK - Service Accounts / credential reuse context (T1078.003 Local Accounts)" url="https://attack.mitre.org/techniques/T1078/003/"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Lateral Movement — Single Source Authenticating as Many Distinct Service/Privileged Accounts</RuleName>
    <RuleId>nbi-corr-many-service-accounts-one-source</RuleId>
    <RuleType>esql</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is an ES|QL correlation over Windows Security. In plain English it: keeps successful logons (event.code == "4624") of LogonType 3 or 10 that carry a real source.ip (not ::1); lower-cases winlog.event_data.TargetUserName and keeps only identities matching the service/privileged naming list; groups by source.ip; and fires when a single source.ip presents &gt;= 10 DISTINCT service/privileged identities (svc_identities), reporting the reached dest_hosts and the svc_accounts list.

Faithful reproduction of the trigger logic (ES|QL, live-tested, &lt;= 4h):



One-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline - the &gt;= 10 distinct aggregation itself is ES|QL, not KQL):



Why LogonType 3/10 and a real source.ip: the rule needs a network origin to attribute the fan-out to. LogonType 2 (local interactive) carries no source.ip and is out of scope; ::1 (loopback) is a local artifact, not a remote origin. The naming list is what turns "many logons" into "many *privileged* logons" - the signal the rule is built to surface.</TriggerLogic>
    <KeyAlertFields>source.ip, process.entity_id</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the single-source / many-service-account correlation on NBI's Elastic Security deployment. The rule fires when one source.ip successfully authenticates (Windows Security Event 4624, LogonType 3 network or 10 RemoteInteractive/RDP) as ten or more DISTINCT service/privileged identities in the window - identities matched against NBI's service/privileged naming list (*.admin, *sys_user, *.servacc, *.prod, icbs*, forti.*, citrix*, sccm*, veeam*, backup*, oracle*, sql*, svc*, *service*, …). One origin presenting many privileged identities is the footprint of a credential-spray / credential-reuse operator working through harvested service accounts - but it is also exactly what NBI's sanctioned session brokers do by design: a PAM proxy or a Citrix/RDS broker fans many privileged sessions from one address.

The analyst's job is to decide, from the shape of the authentication (not the raw count), whether the many-account activity is an authorised broker (false_positive - role validated, not assumed), credential-reuse lateral movement (true_positive), a newly-deployed benign broker not yet baselined (misconfiguration), or unproven (needs_escalation) - and to attach the evidence.</Overview>
    <BusinessImpact>An alert means: on NBI, one network origin ($source_ip) successfully logged on as ≥ 10 different service/privileged accounts inside the window. Because these are *successful* 4624 events, every one of those authentications worked - the credentials were valid and accepted. The question is therefore never "were the logons successful" (they were) but "is one address legitimately entitled to present all these privileged identities, or has an operator concentrated many stolen/reused service credentials at one foothold".

Two populations produce this exact shape:

- A sanctioned session broker - PAM proxy, Citrix/RDS StoreFront, or a monitoring/automation node - is *designed* to authenticate as many accounts from one address. On NBI, CITRIX.NBI and Solarwinds.Srv from 10.11.18.21 are live examples: one address, many identities, many hosts, by design.
- A credential-reuse / lateral-movement operator who has harvested multiple service or admin credentials and is replaying them from a single foothold to move across the estate as trusted service identities.

The two are separated by *how* the authentication behaves and *what the source then does on the reached hosts* - the subject of section</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Lateral Movement — Single Source Authenticating as Many Distinct Service/Privileged Accounts.</Goal>
    <Workflow>
      <Step order="1" name="Detection Summary">
        <Purpose>Detection Summary</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
    AND source.ip IS NOT NULL AND source.ip != "::1"
| EVAL u = TO_LOWER(winlog.event_data.TargetUserName)
| WHERE u RLIKE ".*\\.admin|.*sys_user|.*\\.servacc|.*\\.prod|icbs.*|forti\\..*|citrix.*|sccm.*|veeam.*|backup.*|oracle.*|sql.*|svc.*|.*service.*"
| STATS svc_identities = COUNT_DISTINCT(u), dest_hosts = COUNT_DISTINCT(host.name), svc_accounts = VALUES(u) BY source.ip
| WHERE svc_identities >= 10
| SORT svc_identities DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Reproduce the correlation (confirm the alert)">
        <Purpose>Reproduce the correlation (confirm the alert)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND source.ip == "$source_ip"
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
| EVAL u = TO_LOWER(winlog.event_data.TargetUserName)
| WHERE u RLIKE ".*\\.admin|.*sys_user|.*\\.servacc|.*\\.prod|icbs.*|forti\\..*|citrix.*|sccm.*|veeam.*|backup.*|oracle.*|sql.*|svc.*|.*service.*"
| STATS svc_identities = COUNT_DISTINCT(u), dest_hosts = COUNT_DISTINCT(host.name), svc_accounts = VALUES(u)
| LIMIT 5
]]>
        </Query>
      </Step>
      <Step order="3" name="Account inventory + how broadly each reaches (rule Step 1, verbatim SVCFAN-01)">
        <Purpose>Account inventory + how broadly each reaches (rule Step 1, verbatim SVCFAN-01)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
    AND @timestamp >= NOW() - 2 hours
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name),
        logon_types = VALUES(winlog.event_data.LogonType)
    BY winlog.event_data.TargetUserName
| SORT logons DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="4" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
| STATS logons = COUNT(*), distinct_accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName),
        distinct_hosts = COUNT_DISTINCT(host.name),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
| LIMIT 5
]]>
        </Query>
      </Step>
      <Step order="5" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name),
        logon_types = VALUES(winlog.event_data.LogonType),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY winlog.event_data.TargetUserName
| SORT hosts DESC, logons DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="6" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
| STATS logons = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName),
        logon_types = VALUES(winlog.event_data.LogonType)
    BY host.name
| SORT accounts DESC, logons DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="7" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code == "4769"
| STATS tickets = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName)
    BY winlog.event_data.ServiceName
| SORT tickets DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="8" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND event.code IN ("5140","5145")
    AND @timestamp >= NOW() - 2 hours
| STATS accesses = COUNT(*), hosts = COUNT_DISTINCT(host.name),
        accounts = COUNT_DISTINCT(winlog.event_data.SubjectUserName)
    BY winlog.event_data.ShareName
| SORT accesses DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND event.code IN ("4624","4625","4648","4778","4779")
    AND @timestamp >= NOW() - 2 hours
| STATS events = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName),
        hosts = COUNT_DISTINCT(host.name)
    BY event.code
| SORT events DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624","4625","4648","5140","5145")
| EVAL actor = COALESCE(winlog.event_data.TargetUserName, winlog.event_data.SubjectUserName)
| KEEP @timestamp, event.code, host.name, actor, winlog.event_data.LogonType, winlog.event_data.ShareName, winlog.event_data.RelativeTargetName
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND event.code IN ("4624","4648","4769","5140","5145")
    AND @timestamp >= NOW() - 2 hours
| STATS events = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName)
    BY host.name, event.code
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND event.code == "5145"
    AND @timestamp >= NOW() - 2 hours
    AND TO_LOWER(winlog.event_data.RelativeTargetName) IN ("svcctl","atsvc","winreg","samr","lsarpc","srvsvc","ntsvcs")
| STATS accesses = COUNT(*), hosts = COUNT_DISTINCT(host.name),
        accounts = COUNT_DISTINCT(winlog.event_data.SubjectUserName)
    BY winlog.event_data.RelativeTargetName
| SORT accesses DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="13" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND event.code == "4624" AND winlog.event_data.LogonType IN ("3","10")
    AND @timestamp >= NOW() - 2 hours
| EVAL u = TO_LOWER(winlog.event_data.TargetUserName)
| WHERE u RLIKE ".*admin.*|.*\\.da|administrator|krbtgt|.*domain.*|.*enterprise.*|.*\\.sa"
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY u
| SORT hosts DESC, logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="14" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND @timestamp >= NOW() - 2 hours
    AND event.code IN ("4624","5140","5145")
| EVAL admin_share = CASE(TO_LOWER(winlog.event_data.ShareName) LIKE "*admin$*" OR TO_LOWER(winlog.event_data.ShareName) LIKE "*c$*", 1, 0)
| STATS logons = COUNT(*), distinct_accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName),
        distinct_hosts = COUNT_DISTINCT(host.name), admin_share_hits = SUM(admin_share)
| LIMIT 5
]]>
        </Query>
      </Step>
      <Step order="15" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Sanctioned session brokers (PAM / Citrix / RDS). Their entire function is to present many privileged accounts from one address. This is the dominant benign source of this alert and must be validated by role and behaviour, never auto-cleared on the address alone.</Item>
        <Item>Monitoring / management / backup platforms. SolarWinds, SCCM, Veeam, vulnerability scanners, and agent-management servers authenticate to many hosts as a service identity (or a small set) by design - a legitimate one-source-to-many-hosts pattern. On NBI, Solarwinds.Srv from 10.11.18.21 fans to 20+ hosts routinely.</Item>
        <Item>Automation / orchestration nodes running scheduled jobs under several service accounts.</Item>
        <Item>A single misconfigured/typo'd service credential hammering many hosts and generating heavy *failures* - noisy, but not a multi-account reuse (see section 6 for the live NBI example).</Item>
        <Item>10.11.18.21 is a live Citrix/monitoring broker source. In a 2-hour window it presented CITRIX.NBI (~31,700 network logons across 23 hosts), Solarwinds.Srv (~6,300 logons across 22 hosts), the NIM-SO-APV1$ machine account (across 32 hosts), and several *.admin operator accounts (Wahab.Admin, karrar.admin, Mohammadd.admin, ~10 logons each). This is the authorised-broker archetype - a cohesive service/VDI account family to a stable host set. Validate the role, but do not treat the address as an automatic allow.</Item>
        <Item>The 10.11.101.x range is the Citrix VDI tier. Single VDI/StoreFront addresses there present *hundreds* of distinct human user identities (e.g. 10.11.101.25 = ~600 distinct users across 2 hosts) plus the StoreFront machine account. Most of those are human users that do not match the service/privileged naming list, so they rarely trip this specific rule - but they will dominate a naïve "many accounts from one IP" pivot. Keep the naming-list lens on.</Item>
        <Item>10.11.102.x is the PAM range referenced by the rule as a sanctioned privileged broker. Investigate it identically; a PAM address presenting privileged accounts is expected but still validated on behaviour.</Item>
        <Item>A stale/typo'd admin credential produces heavy failures that are NOT reuse. On 10.11.18.21, ~12,700 4625 failures in 2 hours were all one non-existent account (Ahmed.Adminnnnnn, SubStatus 0xc0000064 = "user name does not exist") across 11 hosts - a misconfigured automation, not a spray of the *presented* service accounts. Read *which* account is failing before calling failures "guessing".</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate / block $source_ip at the network layer if a true_positive is confirmed, to stop further reuse. On a shared broker/VDI address, coordinate with IT so unrelated legitimate sessions are not dropped blindly - but prioritise containment where hands-on lateral movement is proven.</Action>
      <Action>Disable and reset the reused service/privileged accounts identified in section 15.1/section 15.4 - especially any Tier-0 identity (section 17.3). Service-account resets need change coordination (they break dependent jobs), so scope to the accounts actually presented from $source_ip and sequence with the owning teams.</Action>
      <Action>Force-logoff active sessions the source established on the reached hosts (section 15.5) where they can be terminated without destroying volatile evidence.</Action>
      <Action>Preserve evidence first where feasible: capture the source host's process/credential state and the reached hosts' recent activity before resets, since NBI cannot reconstruct host-side execution from logs-system.security-* alone.</Action>
      <Action>Deploy/confirm any change only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate every reused credential (section 15.1) and audit where each service account is permitted to log on; remove standing rights that let one foothold present so many identities.</Action>
      <Action>Remove persistence discovered via section 17.2 on the reached hosts - remote-created services (7045), scheduled tasks (4698), rogue accounts (4720), and any Run-key/registry persistence hinted by winreg pipe access - validated host-side.</Action>
      <Action>Identify and remediate the source foothold: how the operator obtained the service credentials (cached logons, stored RunAs/task creds, config files, LSASS) on the source host, and close that vector.</Action>
      <Action>Hunt the estate for the same reused accounts appearing from *other* sources (the distributed-reuse evasion, section 23) and for the same tooling on peer hosts.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Return accounts to service only after rotation and logon-restriction hardening, confirming dependent jobs work under least privilege.</Action>
      <Action>Restore any reached host from known-good state if persistence or tampering was extensive; otherwise validate eradication holds after reboot.</Action>
      <Action>Route privileged access exclusively through the validated broker/PAM, so genuine reuse stands out against a clean baseline.</Action>
      <Action>Return $source_ip to service only after section 22 closing criteria are met and monitoring confirms the many-account condition does not recur outside sanctioned brokering.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to SOC L2 / IR (and notify the customer) when any of the following hold:

- An unrelated privileged grab-bag (section 15.1/section 15.4) from a non-broker source, especially with failures interleaved with successes on the presented accounts (section 15.12) or ADMIN$/C$ access across reached hosts (section 15.10/section 17.5).
- A Tier-0 / Domain Admin identity is in the presented set (section 17.3) - this alone warrants IR.
- Onward lateral movement to domain controllers or other Tier-0 systems (section 17.1), or remote persistence primitives (section 17.2).
- The source's role cannot be confirmed as an inventoried broker, or hands-on access is present but its authorisation is unclear.
- Evidence is incomplete because of NBI's gaps (NTLM-only reuse with no source.ip; 5140/5145 not audited on reached hosts) and the alert cannot be safely cleared - escalate as needs_escalation wit</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised broker): $source_ip's broker role is positively validated from inventory and behaviour matches - cohesive account family (section 15.1/section 15.4), stable host set (section 15.5), broker-consistent shape (section 15.12), no hands-on admin-share use beyond role (section 15.10/section 17.1). Record the correlated logons and the validation source; do not create a blanket exception.</Item>
    <Item>false_positive (blocked-malicious): a hostile many-account attempt positively proven unsuccessful - failures with no successful privileged session and no hands-on access. Documented as a blocked reuse attempt, never "benign"; preserve evidence and monitor the source.</Item>
    <Item>misconfiguration: a legitimate new/changed broker or automation node presenting many accounts benignly, simply not yet baselined - validate and add it to the known-broker inventory.</Item>
    <Item>true_positive: credential-reuse lateral movement confirmed - unrelated privileged set and/or hands-on impact from a non-broker source; containment/eradication/recovery completed, scope of accounts/hosts established, source foothold investigated, no residual persistence or recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to L2/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Shape beats count. A sanctioned broker and a reuse operator both present many accounts from one address; the raw svc_identities count never decides the verdict. Read cohesion (one family vs grab-bag), auth shape (failures on the *presented* accounts?), and hands-on (ADMIN$/C$) instead.</Item>
    <Item>The classic broker "session signature" is not available on NBI. Events 4778/4779 are effectively not collected (~11 / ~3 per 4h estate-wide) and 4648 is sparse - do not expect "heavy explicit-credential + reconnect/disconnect" to confirm a broker. Confirm the broker from inventory + account cohesion + no hands-on, not from session events that do not exist here.</Item>
    <Item>Kerberos is the reliable source-attributable view. source.ip is 100% on 4768/4769 and 0% on 4776 (NTLM). Prefer 4769 (section 15.6) to profile the source's reach; an NTLM-only operator is a real blind spot - treat that gap as needs_escalation.</Item>
    <Item>Read which account fails before calling it a spray. In the validated NBI sample, ~12,700 4625 on 10.11.18.21 were all one non-existent account (Ahmed.Adminnnnnn, SubStatus 0xc0000064) - a misconfigured automation, not guessing of the presented service accounts.</Item>
    <Item>4672 and host-local persistence/evasion events carry no source.ip. Privilege confirmation (4672), service installs (7045), tasks (4698), account creation (4720), log clearing (1102), and audit-policy change (4719) must be pivoted on the reached host, not on the source. 5145 RelativeTargetName (winreg/svcctl/atsvc/samr) is the one source-attributable remote-admin signal.</Item>
    <Item>Empty 5140/5145 != broker. The hands-on discriminator depends on share auditing being enabled on the *reached* hosts; where it is not, absence is a telemetry gap.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) source.ip population by event - 4768/4769 = 100%, 4624 ≈ 97% (Type 3/10 only), 4776 = 0%; (2) 4778/4779 effectively uncollected, 4648 sparse; (3) 10.11.18.21 = Citrix/SolarWinds broker source (CITRIX.NBI, Solarwinds.Srv, NIM-SO-APV1$) fanning to 20-30+ hosts; (4) recurring 4625 noise = Ahmed.Adminnnnnn (0xc0000064) misconfigured automation; (5) NBI AD domain nbirq.com. All observed live on 2026-08-19.</Item>
  </Limitations>
</Playbook>