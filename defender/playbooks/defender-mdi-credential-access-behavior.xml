<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-defender-mdi-credential-access-behavior" name="Microsoft Defender / MDI — Credential Access Behavior (Kerberoasting / Ticket Abuse / Pass-the-Hash / Spray) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1558.003"/>
      <Technique id="T1558"/>
      <Technique id="T1550.002"/>
      <Technique id="T1550"/>
      <Technique id="T1550.003"/>
      <Technique id="T1110.003"/>
      <Technique id="T1110"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="MITRE ATT&amp;CK - Kerberoasting (T1558.003)" url="https://attack.mitre.org/techniques/T1558/003/"/>
      <Reference title="MITRE ATT&amp;CK - Use Alternate Authentication Material: Pass the Hash (T1550.002)" url="https://attack.mitre.org/techniques/T1550/002/"/>
      <Reference title="MITRE ATT&amp;CK - Use Alternate Authentication Material: Pass the Ticket (T1550.003)" url="https://attack.mitre.org/techniques/T1550/003/"/>
      <Reference title="MITRE ATT&amp;CK - Brute Force: Password Spraying (T1110.003)" url="https://attack.mitre.org/techniques/T1110/003/"/>
      <Reference title="Microsoft Learn - Defender for Identity credential-access alerts" url="https://learn.microsoft.com/en-us/defender-for-identity/credential-access-alerts"/>
      <Reference title="Microsoft Learn - Defender for Identity security alerts (overview)" url="https://learn.microsoft.com/en-us/defender-for-identity/alerts-overview"/>
      <Reference title="Elastic - ES|QL reference" url="https://www.elastic.co/guide/en/elasticsearch/reference/current/esql.html"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Microsoft Defender / MDI — Credential Access Behavior (Kerberoasting / Ticket Abuse / Pass-the-Hash / Spray)</RuleName>
    <RuleId>nbi-defender-credential-access-behavior</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-m365_defender.*</Index>
    <TriggerLogic>The deployed rule is an Elastic query (KQL) rule over logs-m365_defender.*. It fires on every Defender alert whose tactic is Credential Access:



Plain English: any Microsoft Defender / MDI alert (event.kind == "alert") carrying the Credential Access tactic (threat.tactic.name == "CredentialAccess") re-surfaces as an Elastic alert. The *specific* behaviour is carried in m365_defender.alert.detector_id / m365_defender.alert.title, for example:

- xdr_PossibleOverPassTheHash - "Possible overpass-the-hash attack" (ticket/hash reuse). This is by far the dominant detector in NBI (~138 of ~195 credential-access alerts over 30 days).
- KerberoastingSecurityAlert / xdr_PossibleKerberoastingAttack - service-ticket exposure / roasting (offline-cracking risk).
- xdr_SusKerberosAuth_AsReq / xdr_SusKerberosAuth_TgsReq - anomalous Kerberos AS-REQ / TGS-REQ ticketing.
- PasswordSpray / xdr_RiskyLoginByTitanPasswordSprayIp - password spraying.
- DfsCoerceSecurityAlert - authentication coercion.

This is an alert-driven meta-detection: it has no field-level threshold of its own; it inherits MDI's machine-learning / signature verdict and its severity (observed at 73 = high for every credential-acce</TriggerLogic>
    <KeyAlertFields>m365_defender.alert.detector_id, event.severity, user.name, m365_defender.alert.evidence.device_dns_name, m365_defender.alert.evidence.user_account.account_name, m365_defender.alert.evidence.user_account.user_principal_name, source.ip, m365_defender.alert.evidence.ip_address, m365_defender.alert.evidence.process.command_line, file_details.sha256, evidence.url, m365_defender.alert.service_source</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$actor"/>
    <Input name="$device"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Credential Access Behavior meta-detection on NBI's Elastic Security deployment. The rule re-surfaces any Microsoft Defender for Identity (MDI) / Defender XDR alert whose tactic is Credential Access against directory services - service-ticket exposure / Kerberoasting, alternate-authentication-material reuse (overpass-the-hash, pass-the-hash, pass-the-ticket), anomalous Kerberos ticketing, and password spraying. These are the techniques an operator uses to harvest and reuse domain credentials after gaining a foothold, and they run against domain controllers (Tier-0 identity infrastructure).

The rule inherits Defender's verdict; the SOC job is not to re-derive the detection but to corroborate it with the actor's surrounding behaviour and disposition and decide the outcome. The analyst must determine, quickly and defensibly, whether this is a real active credential attack (true_positive), an authorised assessment or a positively-explained legitimate identity artefact or a proven-blocked attempt (false_positive), a noisy detector / exposed-SPN service-account condition (misconfiguration), or something that cannot yet be decided (needs_escalation) - with evidence attached.</Overview>
    <BusinessImpact>An alert means Microsoft Defender for Identity attributed a credential-access technique to $actor against one or more directory hosts ($device, usually a DC). MDI derives these from domain-controller network/ETW sensors: it watches Kerberos AS-REQ/TGS-REQ patterns, NTLM authentication, LDAP, and DNS on the DC and raises an alert when the pattern matches a known credential-theft technique.

For the dominant NBI case - xdr_PossibleOverPassTheHash - the alert means MDI observed authentication that looks like a stolen NTLM hash or Kerberos key being used to request Kerberos tickets (the "overpass-the-hash" / "pass-the-key" pattern), i.e. an actor authenticating with material they harvested rather than an interactively-typed password. For Kerberoasting detectors it means a principal requested service tickets (TGS) for SPNs in a way consistent with harvesting them for offline cracking. For spray detectors it means many accounts were tried from one source.

Crucially, **the alert asserts that MDI *saw the behaviour*, not that the attack necessarily *succeeded*** (a hash may be replayed but rejected; a roasted ticket may never be cracked). The investigation must therefore establish (a) whe</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Microsoft Defender / MDI — Credential Access Behavior (Kerberoasting / Ticket Abuse / Pass-the-Hash / Spray).</Goal>
    <Workflow>
      <Step order="1" name="Confirm the actor's credential techniques and targets (reuse of the deployed investigation query MDICRED-INV-01)">
        <Purpose>Confirm the actor's credential techniques and targets (reuse of the deployed investigation query MDICRED-INV-01)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-m365_defender.*
| MV_EXPAND threat.tactic.name
| WHERE user.name == "$actor" AND threat.tactic.name == "CredentialAccess"
    AND @timestamp >= NOW() - 4 hours
| STATS alerts = COUNT(*), max_severity = MAX(event.severity),
        devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        last_seen = MAX(@timestamp)
    BY m365_defender.alert.detector_id, m365_defender.alert.title
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm Defender disposition and source (reuse of the deployed investigation query MDICRED-INV-03)">
        <Purpose>Confirm Defender disposition and source (reuse of the deployed investigation query MDICRED-INV-03)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-m365_defender.*
| MV_EXPAND threat.tactic.name
| WHERE user.name == "$actor" AND threat.tactic.name == "CredentialAccess"
    AND @timestamp >= NOW() - 4 hours
| STATS alerts = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY m365_defender.alert.status, event.severity, m365_defender.alert.service_source
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name == "CredentialAccess"
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY m365_defender.alert.detector_id, m365_defender.alert.title, m365_defender.alert.status
| SORT alerts DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="4" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        max_severity = MAX(event.severity)
    BY threat.tactic.name, m365_defender.alert.detector_id
| SORT alerts DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="5" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours AND event.kind == "alert"
| MV_EXPAND m365_defender.alert.evidence.device_dns_name
| WHERE m365_defender.alert.evidence.device_dns_name == "$device"
| MV_EXPAND threat.tactic.name
| STATS alerts = COUNT(*), actors = COUNT_DISTINCT(user.name), max_severity = MAX(event.severity)
    BY threat.tactic.name, m365_defender.alert.detector_id
| SORT alerts DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="6" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name == "CredentialAccess"
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY m365_defender.alert.detector_id, m365_defender.alert.title, m365_defender.alert.status
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="7" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| KEEP @timestamp, threat.tactic.name, m365_defender.alert.detector_id, m365_defender.alert.title,
       event.severity, m365_defender.alert.status, m365_defender.alert.evidence.device_dns_name
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="8" name="Attack-Chain Validation">
        <Purpose>Attack-Chain Validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-m365_defender.*
| MV_EXPAND threat.tactic.name
| WHERE user.name == "$actor"
    AND threat.tactic.name IN ("Discovery","LateralMovement","Exfiltration","PrivilegeEscalation")
    AND @timestamp >= NOW() - 4 hours
| STATS alerts = COUNT(*), max_severity = MAX(event.severity),
        detectors = VALUES(m365_defender.alert.detector_id)
    BY threat.tactic.name
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name == "LateralMovement"
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        max_severity = MAX(event.severity)
    BY m365_defender.alert.detector_id, m365_defender.alert.title
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name == "Persistence"
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        max_severity = MAX(event.severity)
    BY m365_defender.alert.detector_id, m365_defender.alert.title
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name == "PrivilegeEscalation"
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        max_severity = MAX(event.severity)
    BY m365_defender.alert.detector_id, m365_defender.alert.title
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="12" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name == "DefenseEvasion"
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        max_severity = MAX(event.severity)
    BY m365_defender.alert.detector_id, m365_defender.alert.title
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="13" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-m365_defender.*
| WHERE @timestamp >= NOW() - 4 hours AND event.kind == "alert" AND user.name == "$actor"
| MV_EXPAND threat.tactic.name
| WHERE threat.tactic.name IN ("Exfiltration","Impact","Collection")
| STATS alerts = COUNT(*), devices = COUNT_DISTINCT(m365_defender.alert.evidence.device_dns_name),
        max_severity = MAX(event.severity)
    BY threat.tactic.name, m365_defender.alert.detector_id
| SORT alerts DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="14" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Authorised penetration tests / red-team / purple-team assessments. These deliberately execute Kerberoasting, pass-the-hash, and spraying and will trip every one of these detectors. NBI's own scanwave.* assessment identities (e.g. scanwave.ahmadjamal, observed with 63 credential-access alerts across 4 detectors) are the clearest example. A scanner or sanctioned-test identity is investigated identically and is never auto-trusted or whitelisted - authorisation is confirmed against the authorised-testing register, by scope and window, not assumed from the name.</Item>
        <Item>Legitimate service accounts with exposed SPNs. A real service account whose SPN is registered and heavily used can produce ticketing patterns MDI reads as roasting/exposure, especially if it uses RC4 or has a weak/old password. This is a hardening condition, not an intrusion (see section 6, misconfiguration).</Item>
        <Item>Administrative and vulnerability-management tooling that authenticates broadly across the estate can resemble reuse or spraying.</Item>
        <Item>Blocked / failed attempts. A spray that locked out, or a hash/ticket that was replayed but denied, is a *malicious attempt positively proven blocked* - recorded as blocked-malicious, never "benign".</Item>
        <Item>Wahab.Admin and sibling admin identities dominate the credential-access alerts. Over 30 days the top credential-access actors are scanwave.ahmadjamal (63, a sanctioned scanner), then Wahab.Admin (53), wahab.admin (27, note the case-variant - Defender emits both casings for the same human), jamal.admin (20), karrar.admin (10), ahmed.adminnnnnn (8). Every one is at severity 73, and every Wahab.Admin alert is the single detector xdr_PossibleOverPassTheHash. A recurring admin identity flagged only for overpass-the-hash, with no surrounding recon/lateral/exfil in Defender's own feed, is the archetypal NBI ambiguity - most likely a privileged administrative workflow or tooling that reuses tickets across DCs, but impossible to clear on volume alone because that is exactly what a compromised admin credential also looks like.</Item>
        <Item>Case-variant identities fragment the picture. Wahab.Admin vs wahab.admin, DC vs dc appear as distinct user.name values. When scoping an actor, check both casings (section 15.4) so you do not under-count the footprint.</Item>
        <Item>Domain controllers are the targets. The devices carrying these alerts are DCs and DB/app servers in nbirq.com (NIM-DC-DBAPV01, NIM-DC2-DBAP, NIM-DC-DBAP01, plus ksa-soo-dbstg01, nik-jp-apv01, nik-vv-apv02). This is consistent with directory-targeted credential access, not endpoint noise.</Item>
        <Item>No environment-specific allow-list exists. There is no recorded benign-true-positive tuning for this rule. Do not create a blanket exception for an admin identity off a pattern of alerts; if an exception is warranted after positive authorisation, scope it to the exact identity + detector + device + window, and re-confirm periodically.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Reset $actor and any exposed/roastable service accounts the actor could have harvested; where a privileged identity is implicated, treat all credentials reachable from the affected DCs as exposed.</Action>
      <Action>Invalidate affected Kerberos tickets. For confirmed ticket/hash reuse against DCs, consider double krbtgt rotation per IR runbook (with the DC/AD team, staged to avoid outage) and revoke sessions for the actor.</Action>
      <Action>Isolate the source host once identified (from raw DC logon source.ip in logs-system.security*, since this index carries no IP) to stop further credential replay.</Action>
      <Action>Preserve evidence first where feasible: the MDI alert set (section 14/section 15), DC security logs (4768/4769/4776), and memory of the source host - NBI does not retain the raw ticketing in this index, so DC-side capture is the way to recover it.</Action>
      <Action>Deploy changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate and gMSA-convert exposed service accounts, enforce Kerberos AES (disable RC4 where possible), and remove any attacker-added credentials or SPNs discovered on the affected accounts.</Action>
      <Action>Remediate any persistence surfaced in section 17.2 (rogue accounts, scheduled tasks, services) on the source host and DCs.</Action>
      <Action>Hunt for reuse across the estate: the same actor and the same DCs ($device set) in logs-system.security* (4768/4769/4776), and other actors pressuring the same DCs (section 15.5), especially other *.admin identities and case-variants.</Action>
      <Action>Remediate the initial-access vector that gave the attacker the foothold and the harvested credentials in the first place.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Restore trust in the identity plane: confirm krbtgt rotation completed, service-account passwords rotated, and Protected Users / tiering applied to the affected privileged accounts.</Action>
      <Action>Return $actor and affected accounts to service only after section 22 closing criteria are met and monitoring shows the credential-access alerts do not recur for the actor/DCs.</Action>
      <Action>Harden: enforce AES-only Kerberos on service accounts, review SPN exposure, and - because this index lacks origin IP and raw ticketing - ensure DC Security auditing (4768/4769 with RC4 flags, 4776) is retained in logs-system.security* for future investigations.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / IR (and notify the customer and AD / identity team) when any hold:

- $actor shows multiple distinct credential techniques or one technique against many DCs at severity 73 (section 14.1 / section 15.1), with a new/unresolved disposition (section 14.2) and no authorised assessment confirmed.
- Surrounding recon + lateral movement / privilege escalation / exfiltration by the same actor (section 17) - this is a full kill chain.
- The targeted DC ($device) is under credential pressure from multiple actors (section 15.5), indicating a campaign rather than a single alert.
- The acting identity is a service or highly-privileged account, or the AD team confirms the credential use succeeded.
- Evidence is incomplete because of NBI's alert-index gaps (no origin IP, no raw ticketing here) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised assessment): the authorised-testing register positively matches $actor + window (including ScanWave / sanctioned pentest identities). Record the engagement reference; do not auto-trust the name; scope any exception tightly.</Item>
    <Item>false_positive (explained legitimate artefact): the AD / identity team positively explains the identity (a known exposed-SPN service account, or a confirmed admin workflow/tooling) with no unexpected follow-on. Attach the evidence.</Item>
    <Item>false_positive (blocked malicious attempt): the attempt is proven blocked/failed (spray lockout; hash/ticket use denied). Documented as blocked-malicious, never "benign"; keep watching the actor.</Item>
    <Item>misconfiguration: a legitimate service/app trips the detector via an exposed SPN / odd ticketing; the SPN is remediated (gMSA / strong password) and the account baselined.</Item>
    <Item>true_positive: active/unauthorised credential access confirmed; actor and exposed accounts reset, tickets invalidated, source host isolated, reuse and lateral movement hunted, incident documented.</Item>
    <Item>needs_escalation: handed to Tier 3 / IR + AD team with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>This is an alert index, not raw identity telemetry. The reliable, fully-populated fields are user.name, detector_id, title, status, event.severity, and evidence.device_dns_name. Everything endpoint-shaped (process/file/URL/registry) and the origin IP are 0% on credential-access alerts - do not build an investigation step on a field this subset does not carry.</Item>
    <Item>service_source is null across NBI (all 2,812 docs). The rule and its investigation_fields overstate it (and host.name, category, description). Use the detector_id prefix (xdr_*, *SecurityAlert) and the DC targets as your MDI signal.</Item>
    <Item>Overpass-the-hash dominates. ~138 of ~195 credential-access alerts are xdr_PossibleOverPassTheHash; Wahab.Admin alone contributes 53 (7 DCs). A recurring admin identity flagged only for OPtH is NBI's signature ambiguity - never clearable on volume alone.</Item>
    <Item>Check both name casings. Wahab.Admin / wahab.admin (and DC / dc) are distinct user.name values; scope the actor under each so you do not under-count.</Item>
    <Item>Empty bracket != innocent. The most consequential evasions (offline Kerberoast cracking, throttled spray, admin-ticket reuse) generate no surrounding alerts. Lean on disposition + authorised-testing register + raw DC Kerberos (logs-system.security* 4768/4769 RC4, 4776) rather than expecting section 17 to light up.</Item>
    <Item>Complementary detections: the directory-reconnaissance rule (enumeration that precedes roasting), the multi-stage / high-impact correlation rule (one entity spanning tactics), and the new-entity high-severity rule (a fresh identity crossing into severe status) catch what a single MDI credential alert misses.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) service_source/category/description null across logs-m365_defender.*; (2) source.ip/evidence.ip_address 0% on Credential-Access alerts; (3) evidence.device_dns_name is multivalued and the authoritative host field (host.name unmapped); (4) top credential-access actors are *.admin case-variants + scanwave.*, all severity 73, OPtH-dominant; (5) NIM-DC-DBAPV01.nbirq.com sees 5-actor OPtH pressure. All observed live 2026-08-17.</Item>
  </Limitations>
</Playbook>