<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-linux-anti-forensics-file-immutability-secure-deletion" name="Linux — Anti-Forensics: File Immutability or Secure Deletion (chattr/shred) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="Medium">Medium</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Defense Evasion</Category>
    <MitreAttack>
      <Technique id="T1070.004"/>
      <Technique id="T1070"/>
      <Technique id="T1222.002"/>
      <Technique id="T1222"/>
      <Technique id="T1485"/>
      <Tactic id="TA0005"/>
      <Tactic id="TA0040"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Indicator Removal: File Deletion (T1070.004)" url="https://attack.mitre.org/techniques/T1070/004/"/>
      <Reference title="MITRE ATT&amp;CK - File and Directory Permissions Modification: Linux and Mac (T1222.002)" url="https://attack.mitre.org/techniques/T1222/002/"/>
      <Reference title="MITRE ATT&amp;CK - Data Destruction (T1485)" url="https://attack.mitre.org/techniques/T1485/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title="man7.org - chattr(1) (immutable/append-only attributes)" url="https://man7.org/linux/man-pages/man1/chattr.1.html"/>
      <Reference title="man7.org - shred(1) (secure overwrite)" url="https://man7.org/linux/man-pages/man1/shred.1.html"/>
      <Reference title="man7.org - auditctl(8) (audit rule control)" url="https://man7.org/linux/man-pages/man8/auditctl.8.html"/>
      <Reference title="Elastic - Auditd Manager integration (fields and setup)" url="https://docs.elastic.co/integrations/auditd_manager"/>
      <Reference title="Elastic ES|QL - language reference (functions incl. MV_CONCAT)" url="https://www.elastic.co/guide/en/elasticsearch/reference/current/esql.html"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Linux — Anti-Forensics: File Immutability or Secure Deletion (chattr/shred)</RuleName>
    <RuleId>nbi-lnx-anti-forensics</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-auditd.log-*</Index>
    <TriggerLogic>The deployed rule is a KQL match query over Linux auditd process telemetry. It matches on the tool name only; the intent lives entirely in the arguments (the flags and the target path).

One-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline):



Plain English: any process execution on a Linux host where the image name is chattr, shred, wipe, or srm. The rule deliberately casts by name so that both intents (immutability-setting and secure-erase) are caught; the investigation below is what separates hardening/disposal from anti-forensics. Because these four binaries have an effectively zero baseline across NBI's Linux fleet (no execution observed in the trailing 30 days, and 0 in the live 4-hour window on the busiest node), any firing is genuinely notable rather than routine noise.

Faithful ES|QL reproduction of the detection logic (estate-wide, live-tested - see section 14.1) is used in place of KQL for every runnable investigation query, because NBI is Elastic Security and all executable analytics here are ES|QL over the _query API.</TriggerLogic>
    <KeyAlertFields>source.ip, user.name, event.action, process.name, process.executable, process.args, process.pid, process.parent.pid, user.id, user.effective.id, process.working_directory, auditd.summary.object.primary</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$source_ip"/>
    <Input name="$suspicious_pid"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Linux - Anti-Forensics: File Immutability or Secure Deletion (chattr/shred) detection on NBI's Elastic Security deployment. The rule fires when a Linux execve is recorded whose process.name is one of chattr, shred, wipe, or srm. chattr sets or clears extended file attributes - most importantly +i (immutable), which blocks modification and deletion of a file *even by root*; shred, wipe, and srm overwrite a file's contents so the data cannot be recovered.

The binary name alone cannot decide intent. chattr +i is used both by hardening baselines (to protect a config file) and by intruders (to lock a webshell or persistence file so responders cannot remove it). shred/wipe/srm are used both by sanctioned data-disposal jobs and by intruders destroying logs, shell history, and their own tooling to defeat forensics. The analyst's job is to recover the exact command and target, characterise the actor and privilege, look for a surrounding evidence-tampering cluster, and classify the alert as true_positive, false_positive (authorised OR proven-blocked-malicious), misconfiguration, or needs_escalation - with evidence attached.</Overview>
    <BusinessImpact>An alert means: on $host, one of chattr/shred/wipe/srm was executed. What that signifies depends entirely on the argument that follows:

- chattr +i &lt;file&gt; (or +a append-only) makes a file immutable/append-only. On a compromised host this is *persistence-locking*: an attacker sets +i on a webshell, a rogue cron file, a modified authorized_keys, or a dropped binary so that a responder's rm fails with "Operation not permitted" even as root. It can also be a legitimate hardening control on an approved config.
- chattr -i &lt;file&gt; *unlocks* a previously-immutable file - frequently the precursor to tampering with a protected system file (for example clearing the immutable bit on a log or a config before editing it).
- shred/wipe/srm &lt;file&gt; overwrite the file's bytes to defeat recovery. Against /var/log/*, /var/log/audit/audit.log, ~/.bash_history, /var/spool/mail, or the attacker's own tooling, this is *evidence destruction*.

The alert therefore captures the *effect* (a tool ran); the investigation recovers the *object* (what it operated on) and the *context* (who, at what privilege, alongside what else). The decisive fact is the target path: a routine application config is one story; /v</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Linux — Anti-Forensics: File Immutability or Secure Deletion (chattr/shred).</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND process.name IN ("chattr","shred","wipe","srm")
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.name, user.id, process.name, process.executable, argline
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm on the alert host - recover command, flags and target file">
        <Purpose>Confirm on the alert host - recover command, flags and target file</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE event.category=="process" AND host.name=="$host"
    AND process.name IN ("chattr","shred","wipe","srm")
    AND @timestamp >= NOW() - 4 hours
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.name, user.id, process.name, process.executable, argline, auditd.summary.object.primary, process.working_directory
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.name IN ("chattr","shred","wipe","srm")
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.id, user.effective.id, process.name, process.executable, argline, auditd.summary.object.primary, process.pid, process.parent.pid, process.working_directory
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND process.name IN ("chattr","shred","wipe","srm")
| STATS execs = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.id) BY process.name, process.executable
| SORT execs DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.name IN ("chattr","shred","wipe","srm")
    AND process.args IS NOT NULL
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.id, process.executable, arguments, auditd.summary.object.primary
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND user.name == "$user"
| STATS execs = COUNT(*), distinct_children = COUNT_DISTINCT(process.name) BY process.parent.pid
| SORT execs DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.parent.pid == $suspicious_pid
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, user.id, process.parent.pid, process.name, process.pid, process.executable, argline, process.working_directory
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE event.category=="process" AND host.name=="$host" AND user.name=="$user"
    AND @timestamp >= NOW() - 4 hours
| STATS execs=COUNT(*) BY process.name, user.id, user.effective.id
| SORT execs DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="9" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND user.name == "$user"
| STATS execs = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
| STATS execs = COUNT(*), users = COUNT_DISTINCT(user.id) BY process.name
| SORT execs ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, event.action
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.name IN ("chattr","shred","wipe","srm")
| STATS execs = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable, auditd.summary.object.primary, process.working_directory
| SORT execs DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.action, source.ip, process.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND user.name == "$user"
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, process.parent.pid, process.pid, process.name, process.executable, argline, user.id
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND (user.name == "$user" OR source.ip == "$source_ip")
    AND host.name != "$host"
    AND source.ip IS NOT NULL
| STATS events = COUNT(*) BY host.name, source.ip, user.name, event.action
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.name IN ("crontab","at","batch","systemctl","systemd-run","cron","crond","update-rc.d")
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, process.name, argline, user.id, user.effective.id
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.name IN ("sudo","su","pkexec","doas","setuid")
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, user.name, user.id, user.effective.id, process.name, argline
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE event.category=="process" AND host.name=="$host"
    AND process.name IN ("chattr","shred","wipe","srm","rm","truncate","auditctl","journalctl","logrotate","unlink")
    AND @timestamp >= NOW() - 4 hours
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, process.name, argline, user.name, user.id
| SORT @timestamp DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process"
    AND host.name == "$host"
    AND process.name IN ("shred","wipe","srm","truncate","rm","chattr")
    AND auditd.summary.object.primary IS NOT NULL
| STATS ops = COUNT(*) BY process.name, auditd.summary.object.primary, user.id
| SORT ops DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Hardening baselines that set chattr +i on approved configuration files (for example locking /etc/resolv.conf, /etc/passwd, or a compliance-mandated file). Legitimate, but must be matched to the documented baseline, not assumed.</Item>
        <Item>Sanctioned data-disposal / decommissioning jobs that shred/wipe/srm scratch files, retired backups, or media before disposal - a real, authorised use of secure deletion.</Item>
        <Item>Log-rotation and cleanup automation that shreds its own working files. Legitimate if it targets its own non-forensic paths and recurs on schedule.</Item>
        <Item>Administrator / red-team / purple-team exercises deliberately exercising anti-forensic technique. These are *not* benign - they are authorised malicious-technique execution and must be confirmed against a change ticket or exercise ROE before classifying as false_positive (blocked/authorised), never dismissed on sight.</Item>
        <Item>True near-zero baseline. chattr/shred/wipe/srm executed 0 times across the fleet in the trailing 30 days and 0 times in the live 4-hour window on the busiest node (nim-pam-dbv07), against a healthy ~141k process events per 4h fleet-wide. There is no noisy legitimate source to tune out; any firing stands alone.</Item>
        <Item>The actor is almost always root, and user.name is frequently null. On the busiest Linux hosts (the PAM/PostgreSQL container nodes nim-pam-dbv06/-dbv07 and the devops Kubernetes masters/workers), process activity runs overwhelmingly as root (user.id == 0), and most auditd process documents carry a null user.name. Corroborate the actor with user.id/user.effective.id; do not treat a null user.name as "no actor". This also means the "non-privileged user escalates" model does not apply here - the risk is a compromised root or service context, and sudo/su are effectively absent in-window (root operates directly).</Item>
        <Item>The target file (auditd.summary.object.primary) is populated only on the auditd_manager stream. It is present and rich on logs-auditd_manager.auditd-* (for example on sh/ps/awk events) but is not carried on logs-auditd.log-*. For chattr/shred events the operated-on path may therefore be visible only through the arguments (argline) on some documents; recover it from the host if the field is null.</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. There is no environment-specific allow-list to apply. Do not create a blanket exception for a host or account off a single alert; scope any exception to an exact tool + target path + user + change reference, and only after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Preserve volatile evidence first. Because on-host destruction is exactly what is in play, copy the remaining logs, the audit trail, and shell history off-box immediately before any further change - the record you need may be actively shrinking.</Action>
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to stop further destruction and any parallel lateral movement. On a shared container node, coordinate with the platform team to protect co-located workloads while prioritising containment.</Action>
      <Action>Snapshot/image before further change so immutable files and partially-shredded artifacts are captured for forensics.</Action>
      <Action>Suspend the actor's session and, if a named account is implicated, disable it pending investigation; if root/service context, treat the host's stored credentials and keys as exposed (section 20).</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Clear immutability blocking removal. For any chattr +i/+a file found in section 15.10/section 17.5, run chattr -i/-a to unlock it, then remove the protected persistence (webshell, rogue cron/systemd unit, malicious authorized_keys, dropped binary).</Action>
      <Action>Remove the persistence discovered in section 17.2 and any dropped payload identified via section 15.10 (executable/working-directory path).</Action>
      <Action>Rebuild destroyed evidence from off-box copies where log forwarding captured it; note gaps created by the destruction for the incident record.</Action>
      <Action>Run a host anti-malware/IR sweep and hunt for the same tooling and immutable-file trick across peers, especially the other PAM/DB nodes and any host $user/$source_ip touched (section 15.4b, section 17.1).</Action>
      <Action>Remediate the initial-access vector that gave the actor the privileged foothold from which they tampered.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Rotate credentials and keys exposed on $host during the compromised window - the host's service accounts, any SSH keys, and secrets reachable from the workload; if this is a PAM/DB node, treat vaulted secrets as potentially exposed and rotate on priority.</Action>
      <Action>Restore $host from a known-good image if destruction or tampering was extensive; otherwise validate that all eradication actions hold after reboot and that immutability has been cleared where appropriate.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms no recurrence.</Action>
      <Action>Harden (see section 23): forward auditd/syslog off-box in real time so on-host destruction cannot erase the record; restrict chattr/shred to a controlled admin path; and monitor immutability changes on system directories.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- A forensic artifact was overwritten (shred/wipe/srm/truncate of /var/log, the audit trail, or ~/.bash_history) - this alone warrants IR.
- A suspicious file was locked with chattr +i to survive clean-up, indicating protected persistence.
- A coordinated tamper cluster (section 17.4) appears - anti-forensic tool alongside auditctl/journalctl/truncate/rm/unlink.
- The actor is a hands-on root/service context also performing recon, download, or interactive-shell activity, or lateral movement from $host/$source_ip is observed (section 17.1).
- Evidence is incomplete because of NBI's telemetry gaps (target path or authorisation unrecoverable) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a change ticket, disposal/decommission job, or hardening baseline is positively matched to the exact tool + target + user + host + time. Record the reference. Do not create a broad exception; if warranted, scope it to the exact tool + path + user.</Item>
    <Item>false_positive (blocked/authorised exercise): a destructive attempt positively proven to have failed/been blocked, or a sanctioned red/purple-team run - documented as blocked-authorised, never "benign".</Item>
    <Item>misconfiguration: a legitimate automated maintenance job invoked the tool on its own non-forensic paths and was simply not yet baselined; document and add it to the baseline.</Item>
    <Item>true_positive: deliberate evidence destruction or persistence-locking confirmed; containment/eradication/recovery completed, immutability cleared, protected persistence removed, scope of $user/$host/peers established, initial access and lateral movement hunted, no recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Zero baseline = high fidelity. chattr/shred/wipe/srm do not appear in NBI's Linux process telemetry over 30 days or in the live 4-hour window. There is nothing legitimate to tune out, so this rule should be near-silent; when it fires, read every hit.</Item>
    <Item>The target path is the whole investigation. Intent lives in the argument, not the binary. Recover it from auditd.summary.object.primary (auditd_manager stream) or from argline; if both are null, pull the host's auditd PATH/EXECVE records. A forensic path or a suspicious +i target flips the verdict.</Item>
    <Item>user.name is unreliable; user.id/user.effective.id are not. Most process docs on the busy nodes carry a null user.name; the actor is root (effective.id == 0). Never read a null name as "no actor". The classic "unprivileged user escalates" model does not fit these hosts - the risk here is a compromised root/service context.</Item>
    <Item>No parent path → PID lineage. With process.parent.executable/.name unpopulated, reconstruct trees with process.parent.pid/process.pid in a tight window (section 15.3), corroborating with timing since PIDs are reused.</Item>
    <Item>auditd_manager vs auditd.log. The operated-on file field is only on logs-auditd_manager.auditd-*; queries that need the target file should include that stream (all queries here do).</Item>
    <Item>The rule is name-based and evadable. In-shell redirection (&gt; /var/log/x), truncate, rm, or dd if=/dev/zero destroy evidence without any of the four tools and will not trip this rule. Complementary signal: monitor auditd rule deletion (auditctl -D), journal vacuuming, and - most importantly - off-box log-forwarding continuity, so on-host destruction is caught by the gap/absence it creates in the central stream.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) chattr/shred/wipe/srm zero-baseline over 4h fleet-wide and 30d; (2) nim-pam-dbv07/-dbv06 are the busiest Linux nodes, container-based (podman/runc/conmon), actor = root, user.name largely null; (3) auditd.summary.object.primary present on auditd_manager stream only; (4) logs-system.auth-* carries SSH source.ip (e.g. 10.11.101.1 for root on pam07); (5) sudo/su effectively absent in-window on the PAM nodes. All observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>