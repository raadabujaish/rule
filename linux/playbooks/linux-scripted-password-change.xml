<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-linux-scripted-password-change" name="Linux — Scripted Password Change (chpasswd) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="medium">medium</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Persistence</Category>
    <MitreAttack>
      <Technique id="T1098"/>
      <Technique id="T1078.003"/>
      <Technique id="T1078"/>
      <Tactic id="TA0003"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Account Manipulation (T1098)" url="https://attack.mitre.org/techniques/T1098/"/>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts: Local Accounts (T1078.003)" url="https://attack.mitre.org/techniques/T1078/003/"/>
      <Reference title="MITRE ATT&amp;CK - Persistence tactic (TA0003)" url="https://attack.mitre.org/tactics/TA0003/"/>
      <Reference title="Elastic Security - Linux auditd integration (process telemetry fields)" url="https://www.elastic.co/docs/reference/integrations/auditd_manager"/>
      <Reference title="man7.org - chpasswd(8) manual (batch credential update, stdin input)" url="https://man7.org/linux/man-pages/man8/chpasswd.8.html"/>
      <Reference title="man7.org - shadow(5) (the /etc/shadow file chpasswd writes)" url="https://man7.org/linux/man-pages/man5/shadow.5.html"/>
      <Reference title="Red Canary - Linux persistence via account manipulation (detection guidance)" url="https://redcanary.com/threat-detection-report/techniques/"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Linux — Scripted Password Change (chpasswd)</RuleName>
    <RuleId>nbi-lnx-scripted-passwd-change</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-auditd_manager.auditd-*</Index>
    <TriggerLogic>The deployed rule is an Elastic query-type rule (KQL) over Linux auditd process telemetry. Its behavioural core is a single name match on the process-execution event:

One-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline):



Plain English: any process execution on a Linux host where the executed image is chpasswd. The rule matches on the binary name alone because the difference between provisioning and intrusion is not the fact of a batch credential change - it is the launch context (automation runner vs. interactive shell), the affected accounts (a uniform batch of app accounts vs. a privileged/service account or a brand-new account), and the actor's wider session (a config-management toolchain vs. an interactive login with recon). Those distinctions are what this playbook resolves.

Two shapes to hold in mind while reading the telemetry:

- Authorised provisioning: chpasswd launched from an automation working directory (e.g. under /var/lib/cloud or an automation temp path) with a parent PID that resolves to a runner (cloud-init, ansible, puppet, chef-client, salt-minion), touching a patterned batch of application accounts, user.id == 0.
- Hands-on accou</TriggerLogic>
    <KeyAlertFields>event.action, source.ip, event.outcome, process.name, process.executable, process.args, process.pid, process.parent.pid, user.id, user.effective.id, process.working_directory, user.name</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$source_ip"/>
    <Input name="$pid"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Linux - Scripted Password Change (chpasswd) detection on NBI's Elastic Security deployment. The rule fires when an execve recorded by Linux auditd (event.category == "process") has a process.name of chpasswd. chpasswd updates local account credentials in batch, non-interactively, reading account-and-secret pairs from standard input rather than prompting - which is exactly why it appears both in legitimate provisioning scripts and in attacker scripts that reset an account the intruder controls.

The analyst's job is to decide whether a given chpasswd execution on $host is unauthorised account manipulation on a compromised host, an authorised administration/provisioning action, a legitimate automation job not yet baselined, or unprovable with the available telemetry - and to classify the alert as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached. Because the sensitive input arrives on stdin, the credentials themselves are not in the arguments; intent must be read from who ran it, from what parent/context, and which accounts changed (revealed by the companion tools passwd/usermod/useradd that cluster around it).</Overview>
    <BusinessImpact>chpasswd requires root to change another account's credential, and it applies the change directly to /etc/shadow (and /etc/passwd for aging fields) for every user:password line it reads on stdin. An alert therefore means: on $host, one or more local account credentials were set non-interactively by a root-context process. That is a legitimate, everyday provisioning primitive - and simultaneously a classic post-compromise persistence move: an intruder who has already reached root uses chpasswd to reset a service or admin credential to a value they control, locking in durable access and potentially locking out the legitimate owner.

By itself the event is a starting point, not a verdict. The investigative questions are precise and answerable from auditd: *was the launch automated or hands-on* (section 14.2/section 15.1 - working directory + parent PID), *which accounts were affected* (section 15.2b - the companion account tools that DO carry the target name in their arguments), and *does the actor's wider session look like automation or intrusion* (section 15.4/section 17.5). An interactive change to a privileged/service account with recon in the same session and no authorising job i</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Linux — Scripted Password Change (chpasswd).</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND process.name == "chpasswd"
| KEEP @timestamp, host.name, user.name, user.id, process.executable, process.working_directory, process.parent.pid
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm on the alert host (launch context)">
        <Purpose>Confirm on the alert host (launch context)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host" AND process.name == "chpasswd"
| STATS executions = COUNT(*), outcomes = VALUES(event.outcome) BY user.name, user.id, process.working_directory, process.parent.pid
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE event.category=="process" AND host.name=="$host"
    AND process.name=="chpasswd"
    AND @timestamp >= NOW() - 4 hours
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.name, user.id, process.executable, argline, process.working_directory, process.parent.pid
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND process.name == "chpasswd"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY process.executable, user.id
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE event.category=="process" AND host.name=="$host"
    AND process.name IN ("chpasswd","passwd","usermod","useradd","adduser","gpasswd","chage")
    AND @timestamp >= NOW() - 4 hours
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, process.name, argline, user.name, user.id
| SORT @timestamp DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host" AND process.name == "chpasswd"
| KEEP @timestamp, user.id, process.pid, process.parent.pid, process.executable, process.working_directory
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host"
    AND process.parent.pid == $pid
| KEEP @timestamp, user.id, user.effective.id, process.parent.pid, process.pid, process.name, process.executable, process.working_directory
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.id) BY process.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="10" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host" AND user.name == "$user" AND source.ip IS NOT NULL
| STATS logins = COUNT(*) BY source.ip, event.action, event.outcome
| SORT logins DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND source.ip == "$source_ip"
| STATS logins = COUNT(*) BY user.name, host.name, event.action
| SORT logins DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host" AND process.name == "chpasswd"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable, process.working_directory
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.action, event.outcome, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host" AND user.name == "$user"
| KEEP @timestamp, user.id, user.effective.id, process.parent.pid, process.pid, process.name, process.executable, process.working_directory
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.auth-*
| WHERE @timestamp >= NOW() - 4 hours
    AND user.name == "$user" AND event.action == "ssh_login"
| STATS logins = COUNT(*) BY host.name, source.ip, event.outcome
| SORT logins DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host"
    AND process.name IN ("useradd", "adduser", "usermod", "gpasswd", "crontab", "at", "systemctl", "systemd-run")
| STATS executions = COUNT(*), euids = VALUES(user.effective.id) BY process.name, user.id
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host"
    AND user.id != "0" AND user.effective.id == "0"
| STATS transitions = COUNT(*), procs = VALUES(process.name) BY user.name, user.id, user.effective.id
| SORT transitions DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.category == "process" AND host.name == "$host"
    AND process.name IN ("setenforce", "auditctl", "semodule", "chattr", "shred", "truncate", "wget", "curl")
| STATS executions = COUNT(*), euids = VALUES(user.effective.id) BY process.name, user.id
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-auditd.log-*,logs-auditd_manager.auditd-*
| WHERE event.category=="process" AND host.name=="$host" AND user.name=="$user"
    AND @timestamp >= NOW() - 4 hours
| STATS execs=COUNT(*) BY process.name, user.id, process.working_directory
| SORT execs DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Cloud/provisioning automation. cloud-init, image-build pipelines, and golden-image bakes legitimately set account credentials with chpasswd at first boot or during provisioning. These originate from automation working directories and runner parents and touch a patterned batch of accounts.</Item>
        <Item>Configuration management. ansible/puppet/chef/salt runs that set or rotate a managed account's credential as part of a play. Recurring, consistent, tied to a known automation identity.</Item>
        <Item>Authorised administration. An operator resetting a service or user credential per an approved request. This is *authorised*, not an accidental benign - confirm against the request/change record and the operator's identity.</Item>
        <Item>Password-rotation jobs driven by a vault/PAM integration that lands as a chpasswd on the host. Should be attributable to the rotation system.</Item>
        <Item>chpasswd has a zero baseline on NBI. Over a 4-hour window across every Linux host reporting to logs-auditd_manager.auditd-* / logs-auditd.log-*, chpasswd executed 0 times - consistent with the deployed rule's 30-day zero baseline. The Linux estate that reports process telemetry is server-side and automation-driven; batch credential changes are simply not part of its steady-state, so any firing is worth attributing to a specific job or actor.</Item>
        <Item>The $host class here is credential-sensitive by design. The busiest reporting hosts are the PAM/database systems (nim-pam-dbv07, nim-pam-dbv06), whose steady-state includes continuous unix_chkpwd (PAM password-verification helper) and shell/monitoring loops under root. That makes a *credential-write* tool (chpasswd) on this class especially notable: credential verification is normal here; credential setting in batch is not.</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. There is no environment-specific allow-list to apply. Do not create a blanket exception for a host or account off a single alert; scope any exception to an exact automation identity + provisioning path + account set, and only after a documented authorised job or change record.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to stop use of the changed credential and any ongoing hands-on activity.</Action>
      <Action>Reset the affected accounts through the proper channel (from section 15.2b / on-host /etc/shadow review) - do not leave the attacker-set credential in place. Prioritise root, privileged admins, and shared service accounts.</Action>
      <Action>Review authorized_keys and sudoers for the same accounts - an intruder frequently pairs the credential change with an SSH key or a sudo grant, which a password reset alone does not remove.</Action>
      <Action>Suspend the acting session and disable any newly-created account (section 15.2b / section 17.2) pending investigation.</Action>
      <Action>Preserve volatile evidence first where feasible - running process list, /etc/shadow change times, shell history, and the auditd records - before rebuilding. Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove the persistence: delete rogue accounts, revert unauthorised group/sudo grants, remove attacker SSH keys from authorized_keys, and undo expiry/shell changes (section 15.2b, section 17.2).</Action>
      <Action>Reset every credential the intruder could have set or read on $host - the targeted accounts and any shared service credential exposed to the root context.</Action>
      <Action>Hunt for the root-cause access: how the actor reached root (correlate section 17.3 and the host's privilege-escalation signals) and whether other footholds exist across peers, especially the other PAM/database hosts and any host the changed account can reach (section 15.4, section 17.1).</Action>
      <Action>Revert any defence-tampering found in section 17.4 and remove downloaded tooling.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Rotate the affected credentials again through managed tooling / a vault after eradication, so the recovered credential is not one the attacker ever knew, and confirm dependent banking services still authenticate.</Action>
      <Action>Reconcile local accounts on $host against the approved inventory; remove anything unaccounted for.</Action>
      <Action>Restore $host from a known-good image if entrenchment was extensive; otherwise validate that all eradication actions hold after reboot.</Action>
      <Action>Return the host/accounts to service only after section 22 closing criteria are met and monitoring confirms no unexplained further credential changes.</Action>
      <Action>Recommend hardening (section 23): route credential changes through managed tooling and a vault, and alert on interactive (non-automation) chpasswd and on new-account creation on servers.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- section 15.2b shows a privileged, admin, or shared service account changed, or a new account created and immediately credentialed, from an interactive session (section 15.1/section 17.5) with no authorising job.
- The chpasswd session is downstream of a fresh non-root→root escalation (section 17.3) rather than an established automation identity.
- Lateral movement using the changed account or $user is observed (section 17.1), especially toward other PAM/database hosts.
- Surrounding persistence (new accounts, SSH keys, sudo grants) or defence-tampering appears (section 17.2, section 17.4).
- Affected accounts or actor authorisation cannot be established because of NBI's telemetry gaps (stdin/parent-image not collected) and the alert cannot be safely cleared - escalate as needs_escalation t</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a recognised automation run or an approved administrative/change request is positively matched to the exact execution and account set (identity + provisioning path + accounts, or a change reference). Record the reference. Scope any exception narrowly to that automation identity and account set; never blanket-except a host.</Item>
    <Item>false_positive (blocked): a change attempt positively proven to have failed - the execution errored or the actor lacked privilege and no credential changed (event.outcome failure, no companion changes). Documented as a blocked attempt, never "benign"; still investigate the source account/host.</Item>
    <Item>misconfiguration: a legitimate automation job performed the change but was simply not yet baselined - no privileged targeting, no interactive context. Baseline it; recommend vault-sourced secrets.</Item>
    <Item>true_positive: unauthorised account manipulation confirmed; $host isolated, affected accounts reset and their keys/sudo reviewed, root-cause access and other footholds hunted, scope established, incident documented.</Item>
    <Item>needs_escalation: handed to Tier 3/IR and the Linux platform/identity team with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Zero baseline = attributable. chpasswd does not appear in NBI's 4-hour Linux process telemetry (0 executions estate-wide, matching the 30-day baseline). Every firing should be attributable to a specific job or actor; treat an unattributed one as real.</Item>
    <Item>The account is on stdin, not in the event. Never expect the target account in chpasswd's own arguments. Recover it from the companion tools (section 15.2b) or on-host from /etc/shadow change times - the deployed rule's own limitation.</Item>
    <Item>Parent is PID-only; no command line. process.parent.name/process.parent.executable and process.command_line do not exist on NBI auditd (verified). The automation-vs-interactive call leans on process.parent.pid (pivoted via section 15.3) + process.working_directory + the account's wider profile.</Item>
    <Item>user.name is ~32% populated. Lead with user.id (expect 0) and corroborate user.name; do not clear an alert merely because the name field is null.</Item>
    <Item>**Credential *verification* is normal on this host class; credential *setting* is not. On the PAM/database hosts unix_chkpwd runs continuously - that is verification, not change. A chpasswd (a batch write**) on the same class is the anomaly, so read it against that baseline.</Item>
    <Item>This rule misses key-only and direct-shadow persistence. An attacker can entrench via authorized_keys, usermod -p, an expect-driven passwd, or a direct /etc/shadow edit - none trip this name-based rule. Complementary signals to run alongside: monitor /etc/shadow and /etc/passwd writes, new-account/sudoers/group changes, and authorized_keys additions on servers, correlated with the interactive-login and privilege-escalation signals for the same account.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) chpasswd zero-baseline over 4h on the combined auditd streams; (2) auditd parent fields absent → parent-by-PID only, process.command_line absent, stdin not captured; (3) user.name ~32% / user.effective.id ~60% population on process events; (4) PAM/database hosts (nim-pam-dbv07/06) steady-state = continuous unix_chkpwd credential verification under root. All observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>