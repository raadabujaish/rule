<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-mssql-xp-cmdshell-execution" name="MS SQL — xp_cmdshell Execution - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Execution</Category>
    <MitreAttack>
      <Technique id="T1059.003"/>
      <Technique id="T1059"/>
      <Technique id="T1505.001"/>
      <Technique id="T1505"/>
      <Technique id="T1190"/>
      <Tactic id="TA0002"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Command and Scripting Interpreter: Windows Command Shell (T1059.003)" url="https://attack.mitre.org/techniques/T1059/003/"/>
      <Reference title="MITRE ATT&amp;CK - Server Software Component: SQL Stored Procedures (T1505.001)" url="https://attack.mitre.org/techniques/T1505/001/"/>
      <Reference title="MITRE ATT&amp;CK - Exploit Public-Facing Application (T1190)" url="https://attack.mitre.org/techniques/T1190/"/>
      <Reference title="MITRE ATT&amp;CK - Execution tactic (TA0002)" url="https://attack.mitre.org/tactics/TA0002/"/>
      <Reference title="Microsoft Learn - xp_cmdshell (Transact-SQL)" url="https://learn.microsoft.com/en-us/sql/relational-databases/system-stored-procedures/xp-cmdshell-transact-sql"/>
      <Reference title="Microsoft Learn - xp_cmdshell Server Configuration Option" url="https://learn.microsoft.com/en-us/sql/database-engine/configure-windows/xp-cmdshell-server-configuration-option"/>
      <Reference title="Microsoft Learn - SQL Server Audit (Database Engine)" url="https://learn.microsoft.com/en-us/sql/relational-databases/security/auditing/sql-server-audit-database-engine"/>
      <Reference title="NetSPI - Executing OS commands through MSSQL (xp_cmdshell &amp; OLE)" url="https://www.netspi.com/blog/technical-blog/network-penetration-testing/hunting-sql-server-with-powerupsql/"/>
      <Reference title="Elastic Security - SQL Server audit integration (fields reference)" url="https://www.elastic.co/docs/reference/integrations/microsoft_sqlserver"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>MS SQL — xp_cmdshell Execution</RuleName>
    <RuleId>nbi-sql-xp-cmdshell</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-microsoft_sqlserver.audit-*</Index>
    <TriggerLogic>The deployed rule is an Elastic query rule over the SQL Server audit data stream. It fires when sqlserver.audit.statement contains xp_cmdshell. One-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline):



Plain English: any audited SQL statement that names xp_cmdshell. The typical form is EXEC xp_cmdshell '&lt;os command&gt;', optionally preceded in the same session by EXEC sp_configure 'xp_cmdshell', 1; RECONFIGURE; to enable the (default-disabled) feature - the enable-then-run pattern (section 14.2). Because the match is on the procedure name, it catches the call regardless of the command payload; the command payload is then the primary intent signal you read in section 14.1.

Why this is high-severity by design: xp_cmdshell is disabled by default and requires sysadmin (or a specifically granted proxy). Its appearance means either the feature is enabled for a real maintenance use, or an attacker enabled and invoked it. Either way the code path reaches cmd.exe under the service account - often a domain account with local admin on the DB host.</TriggerLogic>
    <KeyAlertFields>sqlserver.audit.session_server_principal_name, sqlserver.audit.server_principal_name, sqlserver.audit.statement, sqlserver.audit.client_ip, sqlserver.audit.database_name, sqlserver.audit.application_name, sqlserver.audit.succeeded, host.name, process.command_line, cmd.exe</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$principal"/>
    <Input name="$client_ip"/>
    <Input name="$host"/>
    <Input name="$database"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the MS SQL - xp_cmdshell Execution detection on NBI's Elastic Security deployment. The rule fires when a SQL Server audit statement contains xp_cmdshell - the extended stored procedure that runs arbitrary operating-system shell commands under the SQL Server service account. Its execution is either a sanctioned DBA/maintenance action or operating-system command execution driven through the database - the latter a common outcome of SQL injection against a banking application, or of hands-on database compromise. xp_cmdshell gives OS-level code execution on a database server - among NBI's most sensitive hosts - enabling data theft, lateral movement, and persistence from inside the database tier.

Unlike the sibling OLE-automation route (which reaches the OS via COM objects and where capability is inferred from the ProgID), xp_cmdshell passes a command line straight to cmd.exe - so the intent is legible directly in the command text. The analyst's job is to decide whether this is authorised DBA use or command-execution abuse - and to classify the alert as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached. The decision is driven by three facts: which principal ran it, from which client, and - decisively - what the command actually did.</Overview>
    <BusinessImpact>An alert means: on $host, principal $principal (from client $client_ip) executed a statement that invoked xp_cmdshell. The procedure was called; the OS command in the statement text is what ran (or was attempted). What remains to be established is (a) what the command does - recon, download/execute, shell/tunnel, or a specific maintenance task; (b) whether it succeeded (sqlserver.audit.succeeded); and (c) whether the principal/client is a DBA/admin context or an application/injection origin.

On NBI, session_server_principal_name is the authoritative acting identity (server_principal_name is null estate-wide - section 8). The single most important field is sqlserver.audit.statement, which carries the command line - read it first. whoami/hostname/net user/net group (recon), powershell/certutil/bitsadmin/curl (download/execute), or a reverse-shell/tunnel are unambiguous abuse; a narrow, targeted maintenance command from a DBA context may be legitimate.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for MS SQL — xp_cmdshell Execution.</Goal>
    <Workflow>
      <Step order="1" name="Read the xp_cmdshell command(s) for this principal (confirm the alert)">
        <Purpose>Read the xp_cmdshell command(s) for this principal (confirm the alert)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND sqlserver.audit.statement LIKE "*xp_cmdshell*"
    AND @timestamp >= NOW() - 2 hours
| STATS execs = COUNT(*), statements = VALUES(sqlserver.audit.statement)
    BY sqlserver.audit.client_ip, sqlserver.audit.database_name, host.name
| SORT execs DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="2" name="Enable-then-run and sensitive-operation breadth (INV-02)">
        <Purpose>Enable-then-run and sensitive-operation breadth (INV-02)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 2 hours
| EVAL sensitive = CASE(
      sqlserver.audit.statement LIKE "*sp_configure*" OR sqlserver.audit.statement LIKE "*RECONFIGURE*"
        OR sqlserver.audit.statement LIKE "*xp_cmdshell*" OR sqlserver.audit.statement LIKE "*sp_oacreate*", 1, 0)
| STATS total = COUNT(*), sensitive_ops = SUM(sensitive), clients = COUNT_DISTINCT(sqlserver.audit.client_ip)
    BY sqlserver.audit.database_name
| SORT sensitive_ops DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="3" name="Confirm on the alert host (all principals running xp_cmdshell)">
        <Purpose>Confirm on the alert host (all principals running xp_cmdshell)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE host.name == "$host" AND event.action == "execute-stored-proc-or-function"
    AND sqlserver.audit.statement LIKE "*xp_cmdshell*"
    AND @timestamp >= NOW() - 4 hours
| STATS execs = COUNT(*), statements = VALUES(sqlserver.audit.statement)
    BY sqlserver.audit.session_server_principal_name, sqlserver.audit.client_ip, sqlserver.audit.database_name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="4" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
| STATS statements = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY sqlserver.audit.client_ip, host.name, sqlserver.audit.database_name, event.action
| SORT statements DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
| STATS statements = COUNT(*), dbs = COUNT_DISTINCT(sqlserver.audit.database_name)
    BY sqlserver.audit.application_name, event.action
| SORT statements DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="6" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
| STATS statements = COUNT(*), hosts = COUNT_DISTINCT(host.name), clients = COUNT_DISTINCT(sqlserver.audit.client_ip)
    BY sqlserver.audit.database_name
| SORT statements DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="7" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE host.name == "$host"
    AND @timestamp >= NOW() - 2 hours
| STATS statements = COUNT(*), principals = COUNT_DISTINCT(sqlserver.audit.session_server_principal_name), clients = COUNT_DISTINCT(sqlserver.audit.client_ip)
    BY event.action
| SORT statements DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="8" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.client_ip == "$client_ip"
    AND @timestamp >= NOW() - 2 hours
| STATS statements = COUNT(*), principals = COUNT_DISTINCT(sqlserver.audit.session_server_principal_name),
        dbs = COUNT_DISTINCT(sqlserver.audit.database_name)
    BY host.name
| SORT statements DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.client_ip == "$client_ip"
    AND event.action IN ("login-succeeded", "login-failed")
    AND @timestamp >= NOW() - 4 hours
| STATS logins = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY event.action, sqlserver.audit.session_server_principal_name, user.name, host.name
| SORT logins DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND host.name == "$host"
    AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, sqlserver.audit.client_ip, sqlserver.audit.database_name, event.action, sqlserver.audit.succeeded, sqlserver.audit.statement
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
    AND (host.name != "$host"
         OR sqlserver.audit.statement LIKE "*addlinkedserver*"
         OR sqlserver.audit.statement LIKE "*openquery*"
         OR sqlserver.audit.statement LIKE "*net use*")
| STATS statements = COUNT(*), actions = VALUES(event.action) BY host.name, sqlserver.audit.database_name
| SORT statements DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
    AND (sqlserver.audit.statement LIKE "*schtasks*" OR sqlserver.audit.statement LIKE "*sc create*"
         OR sqlserver.audit.statement LIKE "*net user*" OR sqlserver.audit.statement LIKE "*reg add*"
         OR sqlserver.audit.statement LIKE "*create login*" OR sqlserver.audit.statement LIKE "*sp_add_job*"
         OR sqlserver.audit.statement LIKE "*create assembly*")
| STATS hits = COUNT(*), statements = VALUES(sqlserver.audit.statement) BY sqlserver.audit.database_name, event.action
| SORT hits DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="13" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
    AND (sqlserver.audit.statement LIKE "*sp_configure*" OR sqlserver.audit.statement LIKE "*reconfigure*"
         OR sqlserver.audit.statement LIKE "*xp_cmdshell*" OR sqlserver.audit.statement LIKE "*sysadmin*"
         OR sqlserver.audit.statement LIKE "*add member*" OR sqlserver.audit.statement LIKE "*localgroup*")
| STATS hits = COUNT(*), statements = VALUES(sqlserver.audit.statement) BY sqlserver.audit.database_name, sqlserver.audit.succeeded
| SORT hits DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="14" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
    AND (sqlserver.audit.statement LIKE "*server audit*" OR sqlserver.audit.statement LIKE "*state = off*"
         OR sqlserver.audit.statement LIKE "*net stop*" OR sqlserver.audit.statement LIKE "*taskkill*"
         OR sqlserver.audit.statement LIKE "*wevtutil*" OR sqlserver.audit.statement LIKE "*vssadmin*")
| STATS hits = COUNT(*), statements = VALUES(sqlserver.audit.statement) BY event.action, sqlserver.audit.succeeded
| SORT hits DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="15" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-microsoft_sqlserver.audit-*
| WHERE sqlserver.audit.session_server_principal_name == "$principal"
    AND @timestamp >= NOW() - 4 hours
| EVAL os_cmd = CASE(sqlserver.audit.statement LIKE "*xp_cmdshell*" OR sqlserver.audit.statement LIKE "*sp_oacreate*", 1, 0)
| STATS statements = COUNT(*), os_cmd_ops = SUM(os_cmd), databases = COUNT_DISTINCT(sqlserver.audit.database_name),
        hosts = COUNT_DISTINCT(host.name), total_rows = SUM(sqlserver.audit.affected_rows)
    BY event.action
| SORT statements DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="16" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Authorised DBA/maintenance use. Some maintenance routines legitimately shell out (e.g. directory listings, file moves, calling a maintenance utility). Authorised, not benign-by-default: confirm the DBA principal, the specific command, an admin-host origin, and a change/owner record.</Item>
        <Item>Legitimate automated jobs/integrations that use xp_cmdshell for a narrow, recognised task on a schedule. If recognised, targeting an expected command, this is a baseline gap rather than an attack - and a candidate to replace with a safer mechanism.</Item>
        <Item>A proven-blocked attempt. If xp_cmdshell is disabled or the call errored (sqlserver.audit.succeeded = "False") with no OS command executed, that is a blocked malicious attempt - documented as such, never "benign".</Item>
        <Item>xp_cmdshell has no place in NBI's routine SQL workload. The dominant principal App_admin (client 10.11.44.1, host nim-kta-dbv01) drives millions of select/insert/sp_executesql statements per hour against the Kofax/Tungsten TotalAgility platform via .Net SqlClient Data Provider - none of which uses xp_cmdshell. So an xp_cmdshell execution by that application principal is off-baseline and highly anomalous.</Item>
        <Item>Principal case-variants are the same identity. App_admin, app_admin, and APP_ADMIN all appear; treat them as one application principal when scoping, but pivot on the exact alerted casing (ES|QL == is case-sensitive) and corroborate with TO_LOWER() where needed.</Item>
        <Item>No historical NBI benign xp_cmdshell use is on record for this rule. There is no environment-specific allow-list to apply. Do not create a blanket exception off one alert; scope any exception to the exact DBA principal + admin client + specific command, and only after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host from egress if a recon/download/shell command succeeded - the command runs under the SQL service account and may have staged tooling or opened an outbound channel. Coordinate with the DBA team so a business-critical instance is contained without avoidable outage, but prioritise stopping post-execution activity.</Action>
      <Action>Treat the SQL Server service account as compromised if xp_cmdshell executed successfully - it typically has local privileges on the DB host and possibly network reach; scope its access ahead of rotation (section 20).</Action>
      <Action>Suspend/disable $principal pending investigation if it is implicated, and block $client_ip at the app/network tier if it is an injection pivot. For an application principal, coordinate with the app owner - disabling it may take the application offline, which for an active RCE is the correct trade-off.</Action>
      <Action>Preserve volatile evidence host-side first: the spawned process tree, dropped files, network connections, and the current sp_configure 'xp_cmdshell' state - none of which is in Elastic. Capture before you remediate.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Disable xp_cmdshell (sp_configure 'xp_cmdshell', 0; RECONFIGURE;) unless a documented, owner-confirmed maintenance use requires it - and if it does, constrain that use to a dedicated least-privilege principal/proxy.</Action>
      <Action>Remove attacker artifacts identified host-side: files written by the command, downloaded tooling, and any persistence surfaced in section 17.2 (services, scheduled tasks, Run keys, new OS/SQL accounts, Agent jobs, CLR).</Action>
      <Action>Remediate the initial-access vector. If $client_ip is an application server and the principal is an app login, parameterise/patch the SQL-injection point and remove excess privilege (an app login should not be sysadmin or able to run xp_cmdshell).</Action>
      <Action>Hunt the same pattern across peers - other DB hosts $principal touched (section 17.1) and any instance sharing the service account - for xp_cmdshell/OLE use, dropped payloads, and persistence.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Rotate the SQL Server service account and any credentials reachable from $host during the execution window; if the account is shared across instances, rotate estate-wide and review for exposure.</Action>
      <Action>Reset $principal's credentials and re-scope its permissions to least privilege (remove sysadmin/xp_cmdshell from application logins).</Action>
      <Action>Restore $host from known-good backup if file-drop/persistence was extensive; otherwise validate that eradication holds after a service restart and that xp_cmdshell is confirmed disabled.</Action>
      <Action>Return to service only after section 22 closing criteria are met and monitoring confirms no recurrence of xp_cmdshell from the principal/client.</Action>
      <Action>Recommend hardening (section 23): keep xp_cmdshell and OLE Automation Procedures disabled, least-privilege the SQL service account's host reach, and protect the SQL Server Audit that produced this alert (see the sibling audit-tampering playbook).</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- The command is reconnaissance, download/execute, or a shell/tunnel and it succeeded (section 14.1) - database-driven OS execution; page IR.
- Enable-then-run is confirmed: sp_configure turned xp_cmdshell on immediately before the command (section 14.2 / section 17.3), especially by an application principal.
- $client_ip is an application server and $principal an application login (section 15.6) - the SQL-injection-to-RCE shape.
- Follow-on persistence, linked-server/lateral reach, or audit/defence tampering is present (section 17.1/section 17.2/section 17.4).
- Evidence is incomplete because the OS-side effect is not collected on NBI and the alert cannot be safely cleared - escalate as needs_escalation with the gap named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised DBA use): a documented, owner-confirmed DBA/maintenance command is positively matched to this exact $principal + $client_ip + $database + time. Record the reference. Scope any exception narrowly (principal + client + command); never a blanket allow.</Item>
    <Item>false_positive (blocked malicious attempt): the audit proves the xp_cmdshell call failed with the feature disabled and no OS command executed; documented as a blocked attempt (never "benign"), with the principal/client still investigated for the source.</Item>
    <Item>misconfiguration: a recognised automated job uses xp_cmdshell and was simply not baselined; baseline it and raise the hardening action to remove it / replace with a safer mechanism.</Item>
    <Item>true_positive: unauthorised OS-command execution confirmed; containment/eradication/recovery completed, scope of principal/client/peers established, and no residual persistence or recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific gaps (truncated command, unverifiable OS-side effect) documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The command text is the whole case. Everything downstream (severity, containment scope, IR trigger) turns on what section 14.1 recovers from sqlserver.audit.statement. Read it first; if it is truncated/null, that alone is grounds to pull the raw record before deciding.</Item>
    <Item>Application principals should never run xp_cmdshell. The dominant App_admin/TotalAgility workload is select/insert/sp_executesql only - an OS command from that identity is off-baseline by construction and points to injection or a compromised app login.</Item>
    <Item>Key every statement LIKE on the principal. On this ~2.5M-doc/hour index, session_server_principal_name == "$principal" narrows to a small set (the uppercase APP_ADMIN variant is ~5k docs/4h) so the leading-wildcard LIKE is cheap; a host-only or client-only LIKE times out / circuit-breaks (verified live) - pre-filter those by event.action == "execute-stored-proc-or-function" as in section 14.3.</Item>
    <Item>Elastic sees the command, not the consequence. SQL Audit records the command line but not the spawned cmd.exe tree, files, or network - those DB hosts have no Sysmon/EDR/file/network telemetry in NBI. Recover the OS-side effect host-side; an empty Elastic result never proves the command was harmless.</Item>
    <Item>session_server_principal_name is authoritative; server_principal_name is null estate-wide. On login-failed, the attempted login is in user.name.</Item>
    <Item>xp_cmdshell is one of several OS routes. OLE automation (sp_OACreate) and CLR are alternatives that reach the OS without xp_cmdshell - correlate with the sibling OLE-automation detection so an attacker who switches routes is still caught.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) xp_cmdshell absent from NBI routine workload - any hit off-baseline; (2) App_admin@10.11.44.1→nim-kta-dbv01 (Kofax/Tungsten TotalAgility) is the dominant benign SQL principal and never runs OS commands; (3) process.*/file.*/hash/URL/DNS all 0-populated on SQL audit - OS-side effect recovered host-side only; (4) host/client-only leading-wildcard LIKE circuit-breaks - key on principal or pre-filter by event.action. Observed live 2026-08-19.</Item>
  </Limitations>
</Playbook>