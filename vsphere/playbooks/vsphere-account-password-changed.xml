<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-vsphere-account-password-changed" name="VMware vSphere — vCenter/ESXi Account Password Changed - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="Medium">Medium</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Persistence</Category>
    <MitreAttack>
      <Technique id="T1098"/>
      <Technique id="T1078"/>
      <Tactic id="TA0003"/>
      <Tactic id="TA0005"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Account Manipulation (T1098)" url="https://attack.mitre.org/techniques/T1098/"/>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts (T1078)" url="https://attack.mitre.org/techniques/T1078/"/>
      <Reference title="MITRE ATT&amp;CK - Persistence tactic (TA0003)" url="https://attack.mitre.org/tactics/TA0003/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title="VMware - vCenter Server and ESXi security &amp; the vpxuser managed account" url="https://techdocs.broadcom.com/us/en/vmware-cis/vsphere/vsphere/8-0/vsphere-security-8-0.html"/>
      <Reference title="VMware - vpxuser account and password rotation" url="https://knowledge.broadcom.com/external/article/318250/"/>
      <Reference title="Elastic - vSphere integration (vpxd/vCenter log fields, event.dataset vsphere.log)" url="https://www.elastic.co/docs/reference/integrations/vsphere"/>
      <Reference title="Mandiant - Hunting for and defending vSphere/ESXi credential abuse and hypervisor ransomware" url="https://cloud.google.com/blog/topics/threat-intelligence/esxi-hypervisors-detection-hardening"/>
      <Reference title="CISA - Protecting VMware ESXi and vCenter (hardening and monitoring guidance)" url="https://www.cisa.gov/news-events/alerts"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>VMware vSphere — vCenter/ESXi Account Password Changed</RuleName>
    <RuleId>nbi-vsphere-password-changed</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-vsphere.log-*</Index>
    <TriggerLogic>The deployed rule is a query rule over the vCenter log stream. Its detection logic reduces to:



One-line Kibana-KQL detection filter (for fast pivoting in Discover / Timeline):



Plain English: any vCenter vpxd event whose message states that an account password was changed. Two message variants occur in NBI:

- [System] [NBI-Datacenter] [&lt;taskid&gt;] [Password was changed for account &lt;account&gt; on host &lt;ESXi&gt;] - the primary line, naming the actor (System for the automatic rotation), the account (vpxuser) and the ESXi host.
- [] [NBI-Datacenter] [&lt;taskid&gt;] [VIM account password was changed on host &lt;ESXi&gt;] - a companion line with a blank actor and no account name, emitted alongside the automatic rotation.

Every actionable field lives inside message. The investigation parses it with ES|QL DISSECT on the [actor] [datacenter] [taskid] [detail] structure that this event class uses. The presence of both the Password was changed for account vpxuser line and its VIM account password was changed companion, by System, across multiple hosts, is the signature of the routine automatic rotation; a lone change, a named human actor, or a change to a host root / named administrative account is the</TriggerLogic>
    <KeyAlertFields>event.dataset, vsphere.log, host.name, log.source.address, user.name, process.name</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$esxi_host"/>
    <Input name="$account"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the vCenter/ESXi Account Password Changed detection on NBI's Elastic Security deployment. The rule fires when a vCenter vpxd log line records that a vSphere account credential was changed - the free-text message contains password was changed. In this environment nearly all such events are the automatic rotation of vpxuser (the vCenter-managed service account used to manage ESXi hosts), performed by the System actor and rolled across many ESXi hosts together - expected and benign.

The risk case is an out-of-band change: an attacker who has reached vCenter or an ESXi host changing (and thereby seizing) a host root or a named administrative credential to lock in control of the hypervisor, or to lock legitimate administrators out of recovery. Because ESXi root and vpxuser control the hypervisor, holding that credential means control of every VM on the host - disk access, cloning, and hypervisor-level ransomware.

The analyst's job is to decide, quickly and defensibly, whether this is an automatic vpxuser rotation or an authorised admin rotation (false_positive), an out-of-band credential takeover (true_positive), a legitimate-but-unbaselined automation (misconfiguration), or unprovable from available data (needs_escalation) - driven by *which account* changed, *by whom* (System vs a named actor), and whether it *rolled fleet-wide* (rotation) or *hit a single host* (targeted).</Overview>
    <BusinessImpact>An alert means: on a vСenter-managed ESXi host ($esxi_host), the credential of a vSphere account ($account) was changed. vSphere credentials are control-plane secrets:

- vpxuser is created and owned by vCenter on every managed ESXi host; vCenter rotates it automatically on a schedule to manage the host. Whoever holds vpxuser can drive the host as vCenter does.
- ESXi root is the hypervisor superuser: full control of the host and every VM on it - console, disk, snapshot, clone, power, and the datastore files.
- A named administrative vSphere/SSO account governs vCenter itself.

Changing one of these credentials is, by itself, a legitimate administrative and automated action. But it is also the exact primitive an attacker uses to seize the control plane (set the secret to something only they know) and to lock out defenders (deny recovery). The alert does not tell you which of these it is - it tells you a control-plane credential moved. The investigation decides *which account*, *by whom*, *from where*, and *whether it matches the automatic/authorised rotation*.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for VMware vSphere — vCenter/ESXi Account Password Changed.</Goal>
    <Workflow>
      <Step order="1" name="Classify the change - which account and by whom (reused from the deployed playbook, verbatim)">
        <Purpose>Classify the change - which account and by whom (reused from the deployed playbook, verbatim)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$esxi_host*"
    AND TO_LOWER(message) LIKE "*password was changed*"
    AND @timestamp >= NOW() - 2 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, taskid, detail
| SORT @timestamp DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="2" name="Rotation pattern or one-off change (reused from the deployed playbook, verbatim)">
        <Purpose>Rotation pattern or one-off change (reused from the deployed playbook, verbatim)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$esxi_host*"
    AND TO_LOWER(message) LIKE "*password was changed*"
    AND @timestamp >= NOW() - 2 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| STATS changes = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp),
        details = VALUES(detail)
    BY actor
| SORT changes DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$esxi_host*"
    AND TO_LOWER(message) LIKE "*password was changed*"
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, taskid, detail
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="4" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$account*"
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| STATS events = COUNT(*), details = VALUES(detail) BY actor
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="5" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$esxi_host*"
    AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, message
| SORT @timestamp DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="6" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*@NBIRQ.COM*"
    AND TO_LOWER(message) LIKE "*successful login*"
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, detail
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="7" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$account*"
    AND (TO_LOWER(message) LIKE "*login*" OR TO_LOWER(message) LIKE "*logged in*"
         OR TO_LOWER(message) LIKE "*logged out*")
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, detail
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="8" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$account*"
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, taskid, detail
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="9" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$account*"
    AND (TO_LOWER(message) LIKE "*ssh session was opened*" OR TO_LOWER(message) LIKE "*.nbirq.com*"
         OR TO_LOWER(message) LIKE "*task:*")
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| STATS events = COUNT(*), details = VALUES(detail) BY actor
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$esxi_host*"
    AND (TO_LOWER(message) LIKE "*ssh session was opened*" OR TO_LOWER(message) LIKE "*creating*"
         OR TO_LOWER(message) LIKE "*added*" OR TO_LOWER(message) LIKE "*permission*")
    AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, message
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="11" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$esxi_host*"
    AND (TO_LOWER(message) LIKE "*password was changed*" OR TO_LOWER(message) LIKE "*permission*"
         OR TO_LOWER(message) LIKE "*role*" OR TO_LOWER(message) LIKE "*added*")
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, detail
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$account*"
    AND (TO_LOWER(message) LIKE "*powered off*" OR TO_LOWER(message) LIKE "*removed*"
         OR TO_LOWER(message) LIKE "*is powered off*")
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [%{detail}]"
| WHERE detail IS NOT NULL
| KEEP @timestamp, actor, detail
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-vsphere.log-*
| WHERE event.dataset == "vsphere.log"
    AND message LIKE "*$account*"
    AND TO_LOWER(message) LIKE "*password was changed*"
    AND @timestamp >= NOW() - 4 hours
| DISSECT message "[%{actor}] [%{datacenter}] [%{taskid}] [Password was changed for account %{acct} on host %{esxi}]"
| WHERE esxi IS NOT NULL
| STATS changes = COUNT(*), hosts = COUNT_DISTINCT(esxi), host_list = VALUES(esxi) BY acct, actor
| SORT hosts DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="14" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Automatic vpxuser rotation. vCenter rotates vpxuser on managed ESXi hosts on a schedule; it appears as a System actor changing vpxuser, paired with the VIM account password was changed companion, rolled across many hosts. This is the dominant benign cause.</Item>
        <Item>Authorised administrative rotation under change control - an admin resetting an ESXi root or a service credential as part of maintenance, credential hygiene, or an offboarding. Legitimate, but must be matched to a ticket, never assumed from the actor name.</Item>
        <Item>IdM / secrets-manager rotation - a privileged-access or secrets tool rotating a vSphere service credential on a schedule. Legitimate automation, but treated as misconfiguration until baselined and tagged (see section 6, section 13).</Item>
        <Item>A change attempt that failed - the task errored and the credential was not actually changed. This is a *blocked action*, documented as such, never labelled "benign".</Item>
        <Item>System/vpxuser automatic rotation is the baseline benign pattern. The XML-era baseline recorded 46 credential changes in 60 days, all automatic vpxuser rotations by System across ESXi hosts. The tell is the pair of lines (Password was changed for account vpxuser … + VIM account password was changed …) and the fleet spread. A change that lacks the companion, names a human actor, or touches a non-vpxuser account stands out against this baseline.</Item>
        <Item>The actor lives in message, in more than one form. The vCenter event stream renders a named user as both NBIRQ\&lt;user&gt; (client login line, e.g. User NBIRQ\Wahab.Admin@127.0.0.1 logged in as VMware vim-java 1.0) and &lt;user&gt;@NBIRQ.COM (the SSO line carrying the source IP). System and a blank actor mark machine-driven events. Key any actor pivot on the bare username so both forms match.</Item>
        <Item>host.name is the vCenter appliance, not the changed host. Every record in this index carries host.name = nx-vcsa01 and log.source.address = 10.11.150.206:40066 (the vCenter's own log-shipping address). The ESXi host whose credential changed is inside message; do not mistake host.name/log.source.address for the target or the actor's origin.</Item>
        <Item>user.name is null on this dataset. There is no structured actor/account field - everything is parsed from message. A vpxuser string does not appear in the general event text in quiet windows (measured 0 references in a recent 4-hour window), so an account pivot on vpxuser legitimately returns nothing between rotations.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Treat the changed credential as attacker-controlled if a true_positive is confirmed: reset/recover $account on $esxi_host under IR control, and rotate it out of the attacker's hands using a break-glass path the attacker does not hold.</Action>
      <Action>Revoke the actor's vCenter/ESXi access - disable the acting SSO/admin account, kill active sessions, and remove any SSH sessions the actor opened (section 17.2).</Action>
      <Action>Preserve volatile evidence first where feasible - the ESXi host's account/credential state, active sessions, and /var/log on the host - because NBI does not audit the credential-store write; host-side capture is the only way to recover the *cause*.</Action>
      <Action>Freeze destructive capability on $esxi_host if hypervisor abuse is in progress (section 17.4/section 17.5): coordinate with the virtualization team to protect running VMs and datastores from power-off/deletion while the account is contained.</Action>
      <Action>Perform changes only via the authorised, human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate every credential the actor could have reached from $esxi_host and vCenter - the host root, vpxuser, other ESXi hosts managed by the same vCenter, and any service/admin account whose secret was accessible during the compromised window.</Action>
      <Action>Remove attacker persistence at the hypervisor tier - rogue local ESXi accounts, unauthorised SSH keys/enabled SSH, unexpected permission/role grants (section 17.2/section 17.3), and any rogue VMs stood up by the actor.</Action>
      <Action>Audit the vCenter permission model for changes the actor may have made (added admins, altered roles), and revert unauthorised grants.</Action>
      <Action>Remediate the access path that let the actor reach vCenter/ESXi in the first place (stolen SSO credentials, exposed management interface, compromised jump host).</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Confirm control-plane integrity: verify vpxuser and host root on $esxi_host (and peers) are back under legitimate control, MFA/least-privilege enforced on vСenter/SSO admin access, and the ESXi management interface restricted.</Action>
      <Action>Restore any VMs that were powered off or removed during the incident (section 17.4/section 17.5) from known-good, immutable backups; validate integrity before returning to service.</Action>
      <Action>Return the account/host to service only after section 22 closing criteria are met and monitoring confirms the automatic-rotation baseline is intact and no further out-of-band changes recur.</Action>
      <Action>Harden (see section 23): baseline/tag the vpxuser rotation and any IdM-driven rotation so human-driven changes stand out, and alert specifically on named-actor changes to host-root/administrative accounts.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- A host root or named administrative credential was changed by a named/unexpected actor (not System/vpxuser), on a single host, with no authorised cause - this alone warrants IR.
- The change correlates with an unexpected login source (section 15.6/section 15.12) or concurrent hypervisor abuse by the same actor - VM create/delete/power-off, datastore access, SSH sessions, or additional credential/permission changes (section 17).
- Fleet-wide changes appear that do not match the documented vpxuser rotation schedule (a mass credential seizure).
- Evidence is incomplete because of NBI's telemetry gaps (the credential-store change is not audited here) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (automatic rotation): the System/vpxuser pattern (companion line + fleet spread) matches the documented rotation schedule. Record the schedule reference.</Item>
    <Item>false_positive (authorised rotation): a change ticket positively matches the exact $account + $esxi_host + actor + time. Record the reference; scope any exception narrowly.</Item>
    <Item>false_positive (proven-failed): the change task errored and the credential was not changed; documented as a blocked action (never "benign").</Item>
    <Item>misconfiguration: a recognised automation/IdM identity rotated a service credential and was simply not baselined; the automation is baselined and tagged.</Item>
    <Item>true_positive: an out-of-band credential seizure is confirmed; the credential is recovered/rotated, the actor contained, concurrent hypervisor abuse and the access path investigated, and no recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The actor and account live only in message. There is no structured user.name/account field on logs-vsphere.log-*; host.name is the vCenter appliance (nx-vcsa01) and log.source.address is the vCenter's own shipping address (10.11.150.206:40066). Parse actor/account/host with DISSECT on [actor] [datacenter] [taskid] [detail], and key actor pivots on the bare username so both NBIRQ\&lt;user&gt; and &lt;user&gt;@NBIRQ.COM forms match.</Item>
    <Item>System/vpxuser + companion + fleet spread = the benign rotation. The single fastest discriminator is whether the Password was changed for account vpxuser line is paired with the VIM account password was changed companion and rolled across hosts (section 14.2/section 17.5). A lone, single-host change by a named actor is the inverse and the concern.</Item>
    <Item>The credential-store change itself is invisible here. NBI does not audit the ESXi/VMdir write that sets the secret - only the event line and the actor's surrounding vCenter activity. Do not wait for evidence that cannot exist in this index; pivot on the actor's session (section 16) and recover the host-side state during response.</Item>
    <Item>Rare event, zero baseline in a short window. Credential changes were ≈46 in 60 days; a 0-row confirm query is normal. Anchor on the alert timestamp and widen via the vCenter task/event UI when the event predates the ES|QL window.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) logs-vsphere.log-* host.name≡nx-vcsa01 and log.source.address≡10.11.150.206:40066 are constants (vCenter appliance), not the target/actor; (2) user.name is null on this dataset - actor/account parsed from message; (3) the automatic rotation signature = System + vpxuser + VIM account password was changed companion + multi-host spread; (4) named actors render as both NBIRQ\&lt;user&gt; and &lt;user&gt;@NBIRQ.COM, with source IP only in the SSO line; (5) vpxuser is not echoed in general event text between rotations (0 references in a recent 4h window). Observed live on 2026-08-17.</Item>
  </Limitations>
</Playbook>