<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-credential-dumping-via-regexe" name="Windows — Credential Dumping via Reg.exe - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1003"/>
      <Technique id="T1003.002"/>
      <Technique id="T1003.004"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - OS Credential Dumping (T1003)" url="https://attack.mitre.org/techniques/T1003/"/>
      <Reference title="MITRE ATT&amp;CK - OS Credential Dumping: Security Account Manager (T1003.002)" url="https://attack.mitre.org/techniques/T1003/002/"/>
      <Reference title="MITRE ATT&amp;CK - OS Credential Dumping: LSA Secrets (T1003.004)" url="https://attack.mitre.org/techniques/T1003/004/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="LOLBAS - Reg.exe" url="https://lolbas-project.github.io/lolbas/Binaries/Reg/"/>
      <Reference title="Microsoft Learn - reg save command" url="https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/reg-save"/>
      <Reference title="Microsoft Learn - reg export command" url="https://learn.microsoft.com/en-us/windows-server/administration/windows-commands/reg-export"/>
      <Reference title="Microsoft Learn - Command line process auditing (Event 4688 command-line GPO)" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/command-line-process-auditing"/>
      <Reference title="Elastic - detection-rules repository (prebuilt credential-access rules and tuning)" url="https://github.com/elastic/detection-rules"/>
      <Reference title="SigmaHQ - registry hive dumping detection rules" url="https://github.com/SigmaHQ/sigma"/>
      <Reference title="Microsoft Learn - SeBackupPrivilege (Back up files and directories)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/security-policy-settings/back-up-files-and-directories"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Windows — Credential Dumping via Reg.exe</RuleName>
    <RuleId>0e6bd47c-60d5-466a-85c7-906ae79f8fc4</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is a query (KQL) analytic. The rule definition captures its logic as: a process named reg.exe runs with process.args containing save or export *together with* a reference to the SAM, SECURITY, or SYSTEM hive. Faithful one-line Kibana-KQL detection filter (behavioural intent, for fast pivoting in Discover / Timeline):



Plain English: reg.exe copied a credential-bearing registry hive to a file. The three hives matter because together they yield offline credential material - SAM holds local account hashes, SECURITY holds LSA secrets and cached domain logons, and SYSTEM holds the boot key needed to decrypt both. A save/export of any of them (most damaging: SAM + SYSTEM, or SECURITY + SYSTEM) is the observable signature.

Two caveats built into this detection on NBI:

- The "SYSTEM" token collides with the system32 path. A naive substring match on system matches the benign C:\Windows\system32\reg.exe image path. The credential hive is HKLM\SYSTEM (a top-level hive), *not* ...\CurrentVersion\... sub-keys and *not* system32. Every hive-matching query in this playbook uses an anchored pattern (\(sam|security|system) followed by a non-digit) so a real HKLM\SYSTEM dump ma</TriggerLogic>
    <KeyAlertFields>process.name, process.executable, reg.exe, process.parent.name, process.parent.pid, cmd.exe, powershell.exe, process.pid, user.name, user.domain, host.name, host.os.type</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$source_ip"/>
    <Input name="$suspicious_pid"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Windows - Credential Dumping via Reg.exe detection on NBI's Elastic Security deployment. The rule targets the classic *offline registry-hive credential-dumping* technique: an operator uses the built-in reg.exe utility to save or export the SAM, SECURITY, or SYSTEM hives to a file, then extracts local account password hashes and LSA secrets/cached credentials on another machine.

The analyst's job is to decide, quickly and defensibly, whether a reg.exe hive export on this host is hands-on credential theft (true_positive), a sanctioned backup/imaging/forensics operation or a positively-proven-blocked attempt (false_positive), a legitimate automated product not yet baselined or the rule's own defect firing/under-firing (misconfiguration), or unproven for want of evidence (needs_escalation) - and to attach the evidence.

Known detection defect (carry it into every triage): the deployed custom rule has no explicit index and its default data view does not reliably include the Windows process-creation stream, so it can under-fire. This playbook queries logs-system.security* (Event 4688) directly - where reg.exe execution is actually recorded on NBI - and the rule is flagged for an index fix.</Overview>
    <BusinessImpact>An alert means: on $host, reg.exe was invoked to save or export one of the credential hives. Unlike an in-memory LSASS dump, a registry-hive export is a privileged file operation - reading HKLM\SAM/SECURITY requires SYSTEM or an administrator holding SeBackupPrivilege. So the alert simultaneously asserts two things: an operator *had* the necessary privilege on $host, and they *materialised* credential secrets to disk where they can be carried off-box and cracked or replayed at leisure.

The investigative question is therefore not "could this expose credentials" (a completed save/export already did) but "was the operation authorised, which hives landed, and did the resulting files leave the host." Because the exported .hiv/.reg file is inert until processed elsewhere, the true impact is measured by *what else the same actor did* - staging, archiving, copying to a share, or moving to another host.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Windows — Credential Dumping via Reg.exe.</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (hive-anchored, collision-safe)">
        <Purpose>Reproduce the rule estate-wide (hive-anchored, collision-safe)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND host.os.type == "windows"
    AND TO_LOWER(process.name) == "reg.exe"
| EVAL cmd = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE (cmd LIKE "*save*" OR cmd LIKE "*export*")
    AND cmd RLIKE ".*\\\\(sam|security|system)([^0-9]|$).*"
| KEEP @timestamp, host.name, user.name, process.parent.name, process.command_line
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm on the alert host (every reg.exe execution, args where audited)">
        <Purpose>Confirm on the alert host (every reg.exe execution, args where audited)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "reg.exe"
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, user.name, user.domain, process.parent.name, process.parent.pid, process.pid, process.executable, process.command_line, arguments
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "reg.exe"
| KEEP @timestamp, host.name, user.name, user.domain, process.name, process.executable, process.parent.name, process.parent.executable, process.pid, process.parent.pid
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.name) == "reg.exe"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.executable, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "reg.exe"
    AND process.args IS NOT NULL
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.name, process.executable, process.parent.name, arguments
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND (TO_LOWER(process.name) == "reg.exe" OR TO_LOWER(process.parent.name) == "reg.exe")
| KEEP @timestamp, user.name, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.parent.pid == $suspicious_pid
| KEEP @timestamp, user.name, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="10" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host" AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY user.name, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "reg.exe"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140", "5145")
    AND user.name == "$user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "wmic.exe", "rundll32.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("vssadmin.exe", "wevtutil.exe", "esentutl.exe", "wmic.exe", "cipher.exe", "fsutil.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
    AND TO_LOWER(process.name) IN ("7z.exe", "7za.exe", "rar.exe", "winrar.exe", "tar.exe", "makecab.exe", "robocopy.exe", "xcopy.exe", "esentutl.exe", "certutil.exe", "curl.exe", "powershell.exe")
| STATS runs = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.name, process.parent.name
| SORT runs DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Backup, imaging, and software-inventory agents that export registry content as part of a job. These overwhelmingly touch SOFTWARE/configuration sub-keys (patch/servicing inventory), *not* the SAM/SECURITY/SYSTEM credential hives - the hive-anchored queries in section 14 separate the two.</Item>
        <Item>Forensic/IR and migration tooling that deliberately captures hives (USMT, DFIR collection scripts). Legitimate, but confirm the operator and change record; a hive capture is authorised evidence collection, not "benign background noise".</Item>
        <Item>Administrator troubleshooting that backs up a hive before editing it. Plausible on a workstation/server; must be matched to a person and a reason.</Item>
        <Item>Red-team / purple-team exercises running the exact technique. These are authorised malicious-technique execution - classify as false_positive (blocked/authorised exercise) only against a ticket or ROE, never dismiss as "benign" on sight.</Item>
        <Item>reg.exe does run on NBI - but the observed usage is benign servicing, not hive dumping. The estate's reg.exe executions in-window come from machine accounts via cmd.exe performing registry export of HKEY_LOCAL_MACHINE\SOFTWARE\... sub-keys - SideBySide, Component Based Servicing, Installer\UserData, Uninstall, Products/Patches - written to C:\ExportFiles\*.reg (seen on nim-st-apv10, which *does* audit command lines). None of these touch SAM/SECURITY/SYSTEM.</Item>
        <Item>The credential-hive baseline is effectively zero. The hive-anchored reproduction (section 14.1) returned 0 events estate-wide - there is no legitimate reg save HKLM\SAM/SECURITY/SYSTEM source to tune out, so any true hive match is a strong anomaly with no standing allow-list.</Item>
        <Item>The command-line signal is bimodal and "wrong-tiered" for this attack. On nim-st-apv10 command lines are ~fully captured; on the jump/VDI hosts (nim-jump-apv03/-apv22) - exactly where an interactive operator would dump hives - reg.exe runs with process.command_line and process.args null. Expect to lose the arguments precisely where you most want them; pivot on parent, image path, privilege, and follow-on behaviour instead.</Item>
        <Item>No historical NBI benign-true-positive for hive dumping is on record. Do not create a blanket host/user exception off a single alert; scope any exception to an exact process.args hive + destination + parent + account, and only after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) on a confirmed true_positive to stop off-host copy and follow-on credential replay. On a shared jump/VDI host, coordinate with IT to avoid dropping unrelated user sessions unnecessarily - but prioritise containment.</Action>
      <Action>Suspend or force-logoff $user's session on $host and disable the account pending investigation if the user context is implicated.</Action>
      <Action>Locate and preserve the exported hive files (paths from section 14.2 command line / section 15.10; common staging: C:\Windows\Temp, profile, ProgramData) before they are deleted; capture the running process list and, if feasible, memory of the launching interpreter. NBI does not audit the file write, so host-side capture is the only way to recover the output artifact.</Action>
      <Action>Terminate the launching interpreter and any archiving/transfer process identified in section 16/section 17.5 if the host cannot yet be isolated.</Action>
      <Action>All containment changes go through the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Recover or securely delete the exported hive files and any archive built from them (section 17.5); confirm no copies remain in staging paths or on reachable shares (5140/5145 targets from section 17.1).</Action>
      <Action>Remove any persistence the operator established after harvesting credentials (section 17.2) - services, scheduled tasks, Run-key writes, rogue accounts - and any renamed/dropped reg.exe copy identified via section 15.10.</Action>
      <Action>Run a full anti-malware / EDR scan on $host and hunt the same operator pattern (interactive reg.exe hive export) and destination across peers, especially other jump/VDI hosts and any host $user touched (section 15.4, section 17.1).</Action>
      <Action>Remediate the access that granted the elevated context on $host (how did the operator obtain SYSTEM/admin / SeBackupPrivilege?).</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Rotate every credential the exported hives exposed: the local administrator and all local accounts (SAM), LSA secrets and any cached domain credentials and service-account secrets used on $host (SECURITY + SYSTEM boot key). If privileged accounts had logged on there (section 17.3), rotate those and review for Kerberos/NTLM secret exposure.</Action>
      <Action>Restore $host from a known-good image if persistence or tampering was extensive; otherwise validate that eradication holds after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms no recurrence of reg.exe hive-export activity.</Action>
      <Action>Harden and fix detection: restrict reg.exe hive export (AppLocker/WDAC), enable Credential Guard, enable the "Include command line in process creation events" GPO on the jump/VDI class (its absence is exactly why this investigation loses the arguments), and fix the deployed rule's missing index so genuine dumps reliably alert.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- A credential-hive set (SAM+SYSTEM or SECURITY+SYSTEM) export is confirmed under a human/admin or unusual parent with no sanctioned cause - this alone warrants IR and treating host credentials as exposed.
- Accompanying credential-access tooling (vssadmin/esentutl/ntdsutil/procdump/comsvcs) or staging/off-host copy is present (section 17.4/section 17.5/section 17.1).
- The acting account is a non-privileged identity that nonetheless produced a hive export (section 17.3), or is a service/privileged identity used off-pattern.
- Lateral movement from $host/$user is observed (section 17.1), especially toward domain controllers or other privileged systems.
- Evidence is incomplete because of NBI's telemetry gaps (null command line, no registry/file/network auditing) and the alert cannot be safel</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a change ticket, recognised backup/imaging/forensics product, or sanctioned red/purple-team ROE is positively matched to the exact hive(s) + $user + $host + time. Record the reference. Scope any exception narrowly (hive + destination + parent + account); do not create a blanket host/user allow.</Item>
    <Item>false_positive (blocked-malicious): the reg.exe operation is positively proven to have failed/been denied with no hive written; document as a blocked attempt (never "benign"), preserve evidence, and hunt the account/host.</Item>
    <Item>misconfiguration: a recognised automated product performed the export without attacker involvement (baseline it), and/or the rule's missing-index defect is confirmed (raise the detection-engineering fix).</Item>
    <Item>true_positive: credential-hive export confirmed; host credentials treated as exposed and rotated, exported files recovered/accounted for, off-host copy hunted, scope of $user/$host/peers established, no residual persistence or recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/endpoint team with the specific evidence gaps (null command line, no owning product/change record) documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Match the hive, not the substring. The single biggest tuning trap here is the SYSTEM token: the benign C:\Windows\system32\reg.exe path and HKLM\SOFTWARE\... servicing exports both contain system. The section 14 queries anchor on \(sam|security|system) followed by a non-digit so HKLM\SYSTEM matches while system32/SOFTWARE do not - validated live to return 0 on the benign nim-st-apv10 servicing job and to match a real HKLM\SYSTEM/HKLM\SAM dump.</Item>
    <Item>reg.exe is present but benign in the baseline. In-window, NBI's reg.exe is machine-account servicing/inventory: export of SideBySide/Component Based Servicing/Installer\UserData/Uninstall to C:\ExportFiles\*.reg (e.g. nim-st-apv10). The credential-hive baseline (section 14.1) is 0 - there is nothing legitimate to tune out, so believe a true hive match.</Item>
    <Item>Command-line capture is bimodal and wrong-tiered. ~50% estate-wide (39,613/79,756 over 4h); full on nim-st-apv10, null on the jump/VDI hosts where an interactive dump is most likely. Expect process.command_line/process.args to be null exactly where you most want them; lean on parent, image path, privilege, and follow-on behaviour, and escalate to pull args host-side. Enabling the command-line GPO on the jump class is the highest-value hardening ask from this rule.</Item>
    <Item>The 4672 test is your best discriminator. Hive export needs SYSTEM/admin (SeBackupPrivilege); whether $user holds special privileges on $host (section 17.3) separates an expected admin/servicing operation from a stolen elevated context faster than anything else on NBI.</Item>
    <Item>source.ip is shared infrastructure. A single RDP-gateway address (validated example 10.111.1.33) fronts many interactive users across several jump hosts. Never treat source.ip as an individual identifier; correlate IP + user + host.</Item>
    <Item>In-memory alternatives are invisible. No Sysmon means no Event 10 (LSASS access); an operator who chooses LSASS memory or a raw hive handle over reg save will not surface in logs-system.security*. Empty != safe.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) reg.exe credential-hive zero-baseline over 4h on logs-system.security*; (2) benign reg.exe servicing pattern = machine accounts exporting SOFTWARE hives to C:\ExportFiles (nim-st-apv10); (3) command-line/process.args host-bimodality (~50%; nim-st-apv10 populated vs jump hosts null); (4) 10.111.1.33 = shared RDP-gateway egress; (5) 4672 holders on nim-jump-apv03 = Wahab.Admin + SYSTEM/DWM-*/machine (interactive users absent); (6) deployed rule 0e6bd47c-60d5-466a-85c7-906ae79f8fc4 has no explicit index → under-fire risk. All observed live 2026-08-19.</Item>
  </Limitations>
</Playbook>