<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-interactive-logon-service-account" name="Interactive logon using service account - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Defense Evasion</Category>
    <MitreAttack>
      <Technique id="T1078"/>
      <Technique id="T1078.003"/>
      <Tactic id="TA0005"/>
      <Tactic id="TA0004"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts (T1078)" url="https://attack.mitre.org/techniques/T1078/"/>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts: Local Accounts (T1078.003)" url="https://attack.mitre.org/techniques/T1078/003/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title="MITRE ATT&amp;CK - Privilege Escalation tactic (TA0004)" url="https://attack.mitre.org/tactics/TA0004/"/>
      <Reference title="Microsoft Learn - 4624: An account was successfully logged on (LogonType reference)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4624"/>
      <Reference title="Microsoft Learn - Logon types and their descriptions" url="https://learn.microsoft.com/en-us/windows-server/identity/securing-privileged-access/reference-tools-logon-types"/>
      <Reference title="Microsoft Learn - Machine (computer) accounts and the $ SAM suffix" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/understand-security-identifiers"/>
      <Reference title="Elastic Security - prebuilt detection rules reference" url="https://www.elastic.co/guide/en/security/current/prebuilt-rule-reference.html"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Interactive logon using service account</RuleName>
    <RuleId>9f44beb5-b163-43f4-bc3f-18d53df0279d</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is a KQL query analytic. Its logic: a successful interactive logon by a machine account. The one-line Kibana KQL detection filter (also the deployed condition):



Plain English: Event 4624 (successful logon), LogonType 2 (interactive, at the physical console or an equivalent local session), where TargetUserName ends in $ (a computer/machine account). Machine accounts should never present a Type-2 console session; the rule captures exactly that inversion.

Note on LogonType matching: on NBI winlog.event_data.LogonType is stored as a string ("2", "3", "10"), so investigation queries compare it as a string. The $ suffix is the SAM-account marker for computer accounts (HOSTNAME$); a trailing-$ account performing interactive logon is the anomaly.</TriggerLogic>
    <KeyAlertFields>source.ip, user.name, host.name, process.command_line, process.args</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$target_user"/>
    <Input name="$host"/>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Interactive logon using service account detection on NBI's Elastic Security deployment. The rule fires on a successful Windows logon (Event 4624) with LogonType 2 (interactive / at-the-console) where the target account name ends with $ (winlog.event_data.TargetUserName matches *$). Accounts ending in $ are machine / computer accounts; they are designed for non-interactive service and computer authentication contexts and authenticate constantly over the network (Type 3), but they are not meant to sit at a console. A genuine interactive session under such an identity is abnormal and can indicate a machine/service credential being driven hands-on by a person or a tool.

The analyst's job is to decide whether this is a real hands-on session under a non-human identity (true_positive), a documented/authorised use or a genuinely benign system session (false_positive, never a bare "benign"), a service/scheduled-task misconfiguration (misconfiguration), or unresolved (needs_escalation) - decided by the logon process behind the event, whether interactive is novel for this identity, and whether the session actually did anything on the host.</Overview>
    <BusinessImpact>An alert means: on $host, a machine/computer account ($target_user, ending in $) completed a successful interactive (Type-2) logon. Machine accounts hold their own credential (the computer account secret) and normally authenticate machine-to-machine over the network. An interactive session under one implies one of three things: a person is driving that identity hands-on (credential abuse), a service or scheduled task is misconfigured to log on with an interactive logon type, or a subsystem has mapped a service start to an interactive-style session.

Because machine accounts frequently carry standing privileges and are excluded from MFA and password-rotation hygiene, a person operating hands-on as a computer/service identity - especially on a domain controller, Tier-0, or application server - can move laterally, access data, and persist while blending into expected service noise. The investigative question is which of the three causes applies, and whether the session did anything on the host.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Interactive logon using service account.</Goal>
    <Workflow>
      <Step order="1" name="Confirm the logon and its logon process (reuse of deployed INV-01)">
        <Purpose>Confirm the logon and its logon process (reuse of deployed INV-01)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
    AND host.name == "$host"
| STATS logons = COUNT(*), logon_types = VALUES(winlog.event_data.LogonType),
        first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY winlog.event_data.LogonProcessName, winlog.event_data.WorkstationName
| SORT logons DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="2" name="Is interactive novel for this identity (reuse of deployed INV-02)">
        <Purpose>Is interactive novel for this identity (reuse of deployed INV-02)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name)
    BY winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 10
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
    AND host.name == "$host"
| KEEP @timestamp, host.name, winlog.event_data.TargetUserName, winlog.event_data.LogonType, winlog.event_data.LogonProcessName, winlog.event_data.WorkstationName, source.ip
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND winlog.event_data.SubjectUserName == "$target_user"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="5" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND winlog.event_data.SubjectUserName == "$target_user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="6" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
| STATS logons = COUNT(*), logon_types = VALUES(winlog.event_data.LogonType) BY host.name
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="7" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="8" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host"
    AND winlog.event_data.TargetUserName == "$target_user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY winlog.event_data.TargetUserName, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="10" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND winlog.event_data.SubjectUserName == "$target_user"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable, process.name
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="11" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host"
    AND winlog.event_data.TargetUserName == "$target_user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY event.code, winlog.event_data.LogonType, winlog.event_data.LogonProcessName, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND winlog.event_data.SubjectUserName == "$target_user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="13" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769")
    AND winlog.event_data.TargetUserName == "$target_user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND winlog.event_data.SubjectUserName == "$target_user" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "net.exe", "net1.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="15" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4672", "4673", "4674")
    AND host.name == "$host"
| STATS priv_events = COUNT(*) BY event.code, winlog.event_data.SubjectUserName
| SORT priv_events DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="16" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe", "sdelete.exe", "sdelete64.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, winlog.event_data.SubjectUserName
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="17" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND winlog.event_data.SubjectUserName == "$target_user"
    AND event.code IN ("4688", "4672", "4673", "4674")
| STATS events = COUNT(*), procs = VALUES(process.name) BY event.code
| SORT events DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="18" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Service or scheduled task configured with an interactive logon type. A recurring Type-2 under the machine/service account, driven by a mis-set service or task, with a service-style logon process and no human activity - a misconfiguration, not an attack (section 6/section 13).</Item>
        <Item>Approved vendor tooling / procedure that legitimately logs on with the account interactively (rare). Must be confirmed against a change/approval record before classifying as authorised.</Item>
        <Item>Benign system-managed sessions. Some subsystems produce an interactive-style logon type for a computer/virtual account with no operator behind it (e.g. window-manager DWM-* and UMFD-* virtual accounts). Confirmed by a service logon process and the absence of any hands-on activity (section 14/section 15.2).</Item>
        <Item>Proven-blocked/limited attempts recorded as such (never "benign"); the identity is still treated as targeted.</Item>
        <Item>Machine accounts authenticate almost exclusively as Type-3 (network) here. In the validation window, $-suffixed accounts such as NIM-DC-DBAP01$, NIM-DC2-DBAP$, NIM-SO-APV1$, NIM-CRS-APPV1$ and NIM-JUMP-APV02$ all appear as Type-3 Kerberos logons; 0 appear as Type-2. A Type-2 by any of them is therefore a sharp, tunable-free anomaly.</Item>
        <Item>The jump/VDI tier is the plausible locus. nim-jump-apv02 runs many interactive sessions (Citrix/RDS). A machine account presenting an interactive session there is where credential abuse or a mis-set task would most plausibly surface. Confirm the LogonProcessName and WorkstationName (section 14.1).</Item>
        <Item>source.ip is effectively never populated on Type-2 console logons. On NBI, interactive (Type-2) 4624 events carry no source.ip (measured 0-of-many in prior windows), so the session origin cannot be geolocated from the alert event - rely on host.name/WorkstationName and correlate. source.ip is present on the account's network (Type-3/10) logons (section 15.6).</Item>
        <Item>No recorded benign-true-positive / allow-list. Do not blanket-except a machine account for interactive logon off one alert; scope any exception to an exact account + host + logon process after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to stop hands-on activity under the machine identity. On a shared jump/VDI host, coordinate with IT to avoid dropping unrelated sessions unnecessarily, but prioritise containment.</Action>
      <Action>Rotate the machine/service account secret. For a computer account, this means resetting the machine password (or rejoining/rotating as appropriate). If the credential was abused, this is the decisive control - the account's standing trust is the attacker's leverage.</Action>
      <Action>Force-logoff the interactive session on $host and terminate any operator tooling spawned under the identity (section 15.2/section 17.5).</Action>
      <Action>Preserve volatile evidence first where feasible (running process list, session artifacts, memory) - NBI does not collect the Type-2 origin IP, so host-side capture is the only way to establish who drove the session.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove any persistence discovered in section 17.2 (services, scheduled tasks, rogue accounts) created under the identity.</Action>
      <Action>Correct mis-set logon rights if the cause is a service/task configured to log on interactively (section 13 misconfiguration branch): change the logon type/account and remove "Allow log on locally" from the service identity via GPO.</Action>
      <Action>Hunt the identity across the estate (section 15.4, section 17.1): where else the machine account authenticated in the window, and whether the same hands-on pattern appears elsewhere.</Action>
      <Action>Run a full anti-malware / EDR scan on $host and any host the session reached; identify how the machine-account secret was obtained (LSASS access, backup, SYSTEM-context foothold) and remediate that vector.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Confirm the machine/service secret rotation held and the account authenticates normally (network-only) after remediation.</Action>
      <Action>Restore $host from a known-good image if persistence or tampering was extensive; otherwise validate that all eradication actions hold after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms no recurrence of interactive logon by the identity.</Action>
      <Action>Harden: enforce "Deny log on locally" for service/machine identities via GPO, move any legitimate interactive need to a dedicated named account, and monitor Type-2/Type-10 by $-suffixed accounts. Enabling command-line auditing on the jump/workstation class would strengthen session-activity attribution (section 8).</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / IR and the Windows-AD team when any of the following hold:

- A User32 console session under the machine identity with hands-on process/privileged activity (section 14.1, section 15.2, section 17.5) that is not authorised - this alone warrants IR.
- $host is a domain controller, Tier-0, or critical application server.
- Persistence installed (section 17.2), privileged operations performed hands-on (section 17.3), new lateral reach coincident with the session (section 17.1), or log clearing/audit tampering (section 17.4).
- The machine-account secret is suspected compromised (credential-access evidence elsewhere) - treat as a broader identity incident.
- Evidence is incomplete because of NBI's telemetry gaps (Type-2 origin IP absent, command-line off) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised/explained): an approval record or a positively benign system-session mechanism (service logon process + no operator activity; recognised virtual account) is matched to the exact account + host + time. Record which; recommend moving any legitimate function to a non-interactive logon right. Never a bare "benign".</Item>
    <Item>misconfiguration: a service/scheduled task set to interactive logon under the machine/service account (recurring Type-2, service process, no human activity); the logon type/account is corrected.</Item>
    <Item>true_positive: a machine/service credential used for a real interactive, hands-on session; host isolated, secret rotated, lateral movement/persistence hunted and remediated, incident documented.</Item>
    <Item>needs_escalation: handed to Tier 3/IR and the Windows-AD team with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The logon process is the fastest discriminator. User32 (with WorkstationName == $host) is a genuine console session - the strongest hands-on indicator under a machine identity. Advapi/Service Control Manager/Kerberos/NtLmSsp more often reflect service/network context mapped to an interactive-style type. Check section 14.1 first.</Item>
    <Item>Network-only baseline makes this high-fidelity. NBI's machine accounts (NIM-DC-DBAP01$, NIM-JUMP-APV02$, NIM-SO-APV1$, …) authenticate as Type-3 Kerberos; 0 Type-2 in the validation window. A lone Type-2 is a sharp, tunable-free anomaly.</Item>
    <Item>source.ip is absent on the Type-2 event. You cannot geolocate the interactive session from the alert; rely on host.name/WorkstationName and host-side capture. source.ip on the account's Type-3/10 legs is *shared* infrastructure (validated 10.11.102.2 fronts CLIUSR, NIM-PAM-APV04$, ANONYMOUS LOGON) - never an individual identifier.</Item>
    <Item>Recurring vs one-off Type-2 splits misconfiguration from intrusion. A persistent Type-2 with a service process and no operator activity is a mis-set service/task; a novel Type-2 with a User32 process and hands-on activity is credential abuse.</Item>
    <Item>Command-line auditing is 0% on the jump tier. Session-activity attribution (section 15.2/section 17.5) under-reports there; empty is not innocence. Lean on lineage, image paths, and privileged-operation events.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) machine accounts authenticate Type-3-only in the 4h baseline, 0 Type-2; (2) source.ip absent on Type-2 4624; (3) winlog.event_data.LogonType stored as string; (4) command-line 0% on nim-jump-apv02; (5) 10.11.102.2 = shared PAM/DC/VDI network-logon source. All observed live on 2026-08-19.</Item>
  </Limitations>
</Playbook>