<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-msbuild-process-injection" name="Process Injection by the Microsoft Build Engine [NBI-4688] - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="Low">Low</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Defense Evasion</Category>
    <MitreAttack>
      <Technique id="T1055"/>
      <Technique id="T1127"/>
      <Technique id="T1127.001"/>
      <Tactic id="TA0005"/>
      <Tactic id="TA0004"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Process Injection (T1055)" url="https://attack.mitre.org/techniques/T1055/"/>
      <Reference title="MITRE ATT&amp;CK - Trusted Developer Utilities Proxy Execution (T1127)" url="https://attack.mitre.org/techniques/T1127/"/>
      <Reference title="MITRE ATT&amp;CK - Trusted Developer Utilities Proxy Execution: MSBuild (T1127.001)" url="https://attack.mitre.org/techniques/T1127/001/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title="MITRE ATT&amp;CK - Privilege Escalation tactic (TA0004)" url="https://attack.mitre.org/tactics/TA0004/"/>
      <Reference title='Elastic - "Process Injection by the Microsoft Build Engine" prebuilt rule reference' url="https://www.elastic.co/docs/reference/security/prebuilt-rules/rules/windows/defense_evasion_msbuild_making_network_connections"/>
      <Reference title="LOLBAS - Msbuild.exe" url="https://lolbas-project.github.io/lolbas/Binaries/Msbuild/"/>
      <Reference title="Microsoft Learn - MSBuild inline tasks" url="https://learn.microsoft.com/en-us/visualstudio/msbuild/msbuild-inline-tasks"/>
      <Reference title="Microsoft Learn - Command line process auditing (Event 4688 command-line GPO)" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/command-line-process-auditing"/>
      <Reference title="Elastic Security - Sysmon (Microsoft-Windows-Sysmon) Event ID 8 CreateRemoteThread reference" url="https://www.elastic.co/guide/en/beats/winlogbeat/current/winlogbeat-modules-sysmon.html"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Process Injection by the Microsoft Build Engine [NBI-4688]</RuleName>
    <RuleId>nbi-b2-process-injection-by-the-microsoft-build-engine</RuleId>
    <RuleType>eql</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is an EQL analytic. As characterised by its trigger logic, it fires on a Sysmon CreateRemoteThread event (event provider Microsoft-Windows-Sysmon, Event ID 8) whose source process image is MSBuild.exe - i.e. MSBuild injecting a thread into a remote process:



Plain English: the build engine, which normally only reads project files and emits binaries, is instead writing a thread into another running process - the injector behaviour.

This signal is telemetry-blocked on NBI. Live checks confirm logs-system.security-* carries only provider Microsoft-Windows-Security-Auditing and has no event.code 8 - there is no Sysmon in NBI (logs-windows.sysmon_operational-* is dead). The deployed rule cannot fire on current data, and the injection itself is unobservable here. The investigation therefore pivots to the one thing NBI *does* record: the Windows Security 4688 process-creation event for MSBuild. The NBI-runnable one-line Kibana KQL pivot filter is:



Everything downstream reasons from MSBuild's presence, parent, invocation, and host role, not from the (uncollectable) thread injection. Empty 4688 results are expected on a bank estate where MSBuild should appear only on</TriggerLogic>
    <KeyAlertFields>process.name, process.executable, process.parent.name, process.parent.executable, process.pid, process.parent.pid, process.entity_id, user.name, host.name, host.os.type, process.command_line, process.args</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$process"/>
    <Input name="$suspicious_pid"/>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Process Injection by the Microsoft Build Engine detection on NBI's Elastic Security deployment. The deployed rule targets the moment MSBuild.exe creates a thread inside another process - the classic pattern of the .NET build engine being abused as an in-memory injector. MSBuild is a Microsoft-signed, allow-listed developer utility that can compile and execute inline C#/VB tasks straight from a project file, so an attacker who can invoke it can run and inject shellcode under a trusted binary, defeating application allow-listing and many AV signatures.

The critical NBI reality - stated up front because it shapes everything below - is that the deployed detection depends on Sysmon CreateRemoteThread telemetry that NBI does not collect (see section 2 and section 8). The exact injection event therefore cannot fire or be confirmed on current telemetry, and the honest investigative posture is to reason from the closest live-observable evidence: the 4688 process-creation record for MSBuild.exe, its parent, its invocation, and the host's developer-tooling footprint. The analyst's job is to classify the activity as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached - while explicitly documenting that the injection signal itself is telemetry-blocked.</Overview>
    <BusinessImpact>An alert (were the Sysmon signal present) means: on $host, MSBuild.exe created a remote thread in another process - MSBuild acting as an injector. Because NBI cannot observe that event, an *investigation on NBI* is instead triggered by, or pivots to, MSBuild having executed at all on a host that has no development purpose. The distinction matters: on a genuine developer/build machine, MSBuild running is normal and the injection claim would need the (missing) Sysmon evidence; on a server, jump host, or ordinary user workstation, MSBuild executing is itself anomalous and is the strongest signal available in NBI's telemetry.

The investigative question is therefore twofold: (1) is $host a place MSBuild has any legitimate reason to run, and (2) what launched MSBuild and with what invocation - a real .sln/.csproj build under a developer IDE/agent, or a lone inline-task project (or no project) launched by an office/script/browser parent. The second shape is trusted-developer-utility abuse consistent with the injection the rule was written to catch, even though the injection event cannot be seen here.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Process Injection by the Microsoft Build Engine [NBI-4688].</Goal>
    <Workflow>
      <Step order="1" name="Confirm MSBuild ran on the alert host and read its invocation">
        <Purpose>Confirm MSBuild ran on the alert host and read its invocation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.name) == "msbuild.exe"
    AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, host.name, user.name, process.parent.name, process.command_line, process.args
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Estate-wide MSBuild presence (context for the zero baseline)">
        <Purpose>Estate-wide MSBuild presence (context for the zero baseline)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.name) == "msbuild.exe"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.executable, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "$process"
| KEEP @timestamp, host.name, user.name, process.executable, process.parent.name, process.parent.executable, process.pid, process.parent.pid, process.command_line
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.name) == "$process"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.executable, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.name) == "$process"
    AND process.args IS NOT NULL
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.name, process.executable, process.parent.name, arguments
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host" AND user.name == "$user"
    AND TO_LOWER(process.name) == "msbuild.exe"
    AND @timestamp >= NOW() - 4 hours
| EVAL parent_class = CASE(
    TO_LOWER(process.parent.name) IN ("devenv.exe", "msbuild.exe", "dotnet.exe", "cmd.exe", "powershell.exe", "code.exe"), "dev-or-shell",
    TO_LOWER(process.parent.name) IN ("winword.exe", "excel.exe", "outlook.exe", "mshta.exe", "wscript.exe", "cscript.exe", "explorer.exe", "rundll32.exe"), "anomalous-office-script",
    "other")
| STATS execs = COUNT(*) BY parent_class, process.parent.name, user.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.parent.pid == $suspicious_pid
| KEEP @timestamp, user.name, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host"
    AND @timestamp >= NOW() - 4 hours
    AND TO_LOWER(process.name) IN ("devenv.exe", "msbuild.exe", "dotnet.exe", "csc.exe", "vbc.exe", "git.exe", "code.exe", "nuget.exe")
| STATS execs = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host" AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY user.name, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "$process"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND user.name == "$user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "wscript.exe", "cscript.exe", "rundll32.exe", "regsvr32.exe", "installutil.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe", "sdelete.exe", "sdelete64.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND event.code == "4688"
    AND process.parent.pid == $suspicious_pid
| STATS children = COUNT(*), distinct_children = COUNT_DISTINCT(process.name) BY process.name
| SORT children DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Genuine software development / CI builds. On a developer workstation or a build/CI agent, MSBuild.exe compiling a real .sln/.csproj under devenv.exe, dotnet.exe, or a build-agent shell is entirely expected. This is the dominant benign case and is confirmed by the host's developer-tooling footprint (section 15.5) and a real project path (section 14/section 15.2).</Item>
        <Item>IDE and restore operations - Visual Studio, dotnet build/restore, NuGet, and design-time builds spawn MSBuild frequently on dev machines.</Item>
        <Item>Packaging / installer frameworks that shell out to MSBuild during setup on engineering hosts.</Item>
        <Item>These are not benign by assumption: they are confirmed by evidencing a developer/build context (rich dev tooling, dev-IDE/agent parent, real project). An MSBuild execution positively proven blocked by allow-listing/AV is recorded as blocked-malicious, never "benign".</Item>
        <Item>MSBuild is absent from NBI's live 4-hour 4688 window. Across the estate, MSBuild.exe did not appear as a process at all in the validated window. On a bank estate this is expected - MSBuild belongs on developer workstations, of which NBI's Windows telemetry shows very few. There is therefore no noisy legitimate source to tune out, and any MSBuild execution is a strong anomaly worth confirming.</Item>
        <Item>The plausible-legitimate locus is a developer/build host, not the busy servers. NBI's busiest Windows hosts are application/backup/build-adjacent servers (nim-est-apv07, nim-est-apv04, nim-fti-apv01) dominated by service-account and machine-account process creation; interactive developer tooling (devenv.exe, git.exe, csc.exe) is not part of their normal footprint. If MSBuild surfaces there without surrounding dev tooling, treat it as anomalous.</Item>
        <Item>Command-line auditing is bimodal. Some servers populate process.command_line/process.args (e.g. nim-est-apv07 ≈ full), while jump/VDI and many workstations do not. Expect the MSBuild invocation to be recoverable on some hosts and null on others (section 8) - a null command line is not exoneration.</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. There is no environment-specific allow-list to apply; scope any future exception to an exact image + path + parent + host + user after a documented developer/build owner is confirmed.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to stop post-injection activity. On a shared server, coordinate with IT to avoid dropping unrelated services, but prioritise containment when abuse is corroborated.</Action>
      <Action>Suspend or force-logoff $user's session on $host and disable the account pending investigation if the user context is implicated; reset its credentials (section 20).</Action>
      <Action>Terminate the MSBuild process and its descendants ($suspicious_pid tree from section 15.3b / section 17.5) and any process it may have injected - because the injection target is not visible on NBI, capture a full running-process list and memory host-side before termination so the injected process can be identified.</Action>
      <Action>Preserve volatile evidence first where feasible: running process list, loaded modules, memory of MSBuild and suspected targets, and the on-disk MSBuild project/inline-task file (the malicious artifact) located via section 15.2b/section 15.10. NBI does not collect the injection or the project write, so host-side capture is the only way to recover them.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove the persistence discovered in section 17.2 (services, scheduled tasks, Run keys, rogue accounts) and any dropped payload identified via section 15.10.</Action>
      <Action>Delete the malicious MSBuild project / inline-task file recovered host-side, and any second-stage binaries it fetched or wrote.</Action>
      <Action>Run a full anti-malware / EDR scan on $host and hunt for the same project/payload and parentage across peers - especially any host $user touched (section 15.4, section 17.1) and any other host where MSBuild appears without a developer footprint (section 14.3).</Action>
      <Action>Remediate the initial-access vector (phishing document/macro, dropped loader) that produced the office/script parent for MSBuild.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Reset $user's password and any credentials accessible from $host during the abuse window; if privileged accounts logged on there (section 17.3), rotate those too.</Action>
      <Action>Restore $host from a known-good image if injection/persistence was extensive; otherwise validate that all eradication actions hold after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms MSBuild does not recur on a non-dev host.</Action>
      <Action>Raise the telemetry gap. Deploying Sysmon (or Elastic Defend) CreateRemoteThread telemetry so this rule can actually fire, allow-listing MSBuild to developer hosts, and enabling full command-line auditing on the host class are the highest-value hardening asks from this rule.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- MSBuild executed on a non-developer host (section 15.5) with an office/script/browser parent (section 15.3) or an inline/Temp/Downloads/no-project invocation (section 14.2/section 15.2b).
- The MSBuild PID spawned recon, credential-access, download, or persistence tooling (section 17.5), or persistence was installed (section 17.2).
- Lateral movement from $host/$user is observed (section 17.1), especially toward domain controllers or other privileged systems.
- The acting account is privileged or a service identity, or log clearing / audit-policy tampering appears (section 17.4).
- Evidence is incomplete because of NBI's telemetry gaps (the CreateRemoteThread injection signal is not collected) and the alert cannot be safely cleared - escalate as needs_escalation with the gap named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a developer/build owner, dev-IDE/agent parent, and a real .sln/.csproj are positively matched to the exact $process + $user + $host + time on a host with a genuine developer footprint. Record the reference; scope any exception to the exact image + path + parent + host + user.</Item>
    <Item>false_positive (blocked-malicious): MSBuild execution positively proven blocked by allow-listing/AV; documented as blocked (never "benign").</Item>
    <Item>misconfiguration: a legitimate build/CI agent was simply not yet baselined as a developer system; baseline it and restrict MSBuild on non-dev hosts.</Item>
    <Item>true_positive: MSBuild abuse on a non-dev host confirmed from the 4688 evidence; containment/eradication/recovery completed, scope of $user/$host/peers established, no residual persistence or recurrence, and the injection telemetry gap raised for remediation.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps (Sysmon injection signal, null command line) documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The detection is telemetry-blocked; the investigation is not. The deployed rule needs Sysmon CreateRemoteThread (Event ID 8), which NBI does not collect. Do not wait for an injection event that cannot exist here - reason from MSBuild presence, parent, invocation, and host role on 4688, and record VALIDATION_BLOCKED for the injection signal.</Item>
    <Item>MSBuild's near-zero baseline is your fidelity. MSBuild is absent from NBI's live 4h window; there is nothing legitimate to tune out, so any execution on a non-developer host is high-signal despite the low deployed severity.</Item>
    <Item>The host-role test (section 15.5) is the fastest discriminator. Rich devenv/dotnet/git/csc footprint = developer machine (MSBuild expected); MSBuild alone on a server/jump/workstation = anomalous.</Item>
    <Item>Command-line capture is bimodal. The project path / inline task is your best on-telemetry lead where the host audits it, and simply null where it does not - corroborate with parent, host role, and PID lineage instead.</Item>
    <Item>The signed binary is not the artifact. MSBuild is Microsoft-signed; the malicious content is the *project/inline-task file*. Hash the project, not (only) the executable, and recover it host-side (section 15.10).</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) logs-system.security-* carries only Microsoft-Windows-Security-Auditing, no event.code 8 - MSBuild injection rule is telemetry-blocked; (2) MSBuild zero-baseline over 4h; (3) command-line/process.args host-bimodality; (4) process.hash.* absent on 4688. All observed live on 2026-08-19.</Item>
  </Limitations>
</Playbook>