<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-mshta-script-execution" name="Script Execution via Microsoft HTML Application [NBI-4688] - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Defense Evasion</Category>
    <MitreAttack>
      <Technique id="T1218"/>
      <Technique id="T1218.005"/>
      <Technique id="T1218.011"/>
      <Tactic id="TA0005"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - System Binary Proxy Execution (T1218)" url="https://attack.mitre.org/techniques/T1218/"/>
      <Reference title="MITRE ATT&amp;CK - System Binary Proxy Execution: Mshta (T1218.005)" url="https://attack.mitre.org/techniques/T1218/005/"/>
      <Reference title="MITRE ATT&amp;CK - System Binary Proxy Execution: Rundll32 (T1218.011)" url="https://attack.mitre.org/techniques/T1218/011/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title='Elastic - prebuilt rule "Mshta Making Network Connections / HTML Application" references' url="https://www.elastic.co/docs/reference/security/prebuilt-rules/rules/windows/defense_evasion_suspicious_mshta_child_process"/>
      <Reference title="LOLBAS - Mshta.exe" url="https://lolbas-project.github.io/lolbas/Binaries/Mshta/"/>
      <Reference title="LOLBAS - Rundll32.exe (mshtml,RunHTMLApplication)" url="https://lolbas-project.github.io/lolbas/Binaries/Rundll32/"/>
      <Reference title="Red Canary - Mshta threat detection" url="https://redcanary.com/threat-detection-report/techniques/mshta/"/>
      <Reference title="Microsoft Learn - Command line process auditing (Event 4688 command-line GPO)" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/command-line-process-auditing"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Script Execution via Microsoft HTML Application [NBI-4688]</RuleName>
    <RuleId>nbi-b2-script-execution-via-microsoft-html-application</RuleId>
    <RuleType>eql</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is an EQL analytic. As characterised by its trigger logic, it fires on a process-creation event where the image is mshta.exe or rundll32.exe, the command line carries an HTML-application/script marker, and the parent is not one of the excluded application launchers:



Plain English: the HTML-application engine executed script - an inline VBScript/JScript command, a remote HTA fetched over HTTP, or an HTA opened from disk - under a signed Windows binary, and not from a recognised application launcher.

The rule keys on Windows Security 4688 (event.code : "4688"), which NBI collects, so it is live-capable - but it depends entirely on the command line, which is only ~50% populated on NBI (section 8). The one-line Kibana KQL detection/pivot filter is:



In NBI's live window mshta.exe is absent and no rundll32.exe script markers are present, so the rule should be near-silent; a match is a high-value lead. Where the command line is null the script is invisible in this index and must be recovered from the host/EDR - an empty result is the expected zero baseline and is not evidence of safety.</TriggerLogic>
    <KeyAlertFields>process.name, process.executable, mshta.exe, rundll32.exe, process.parent.name, process.parent.executable, process.pid, process.parent.pid, process.entity_id, user.name, host.name, host.os.type</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$process"/>
    <Input name="$suspicious_pid"/>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Script Execution via Microsoft HTML Application detection on NBI's Elastic Security deployment. The rule fires when mshta.exe or rundll32.exe runs script through the Microsoft HTML-application engine - inline VBScript/JScript (GetObject, WScript.Shell, .run(, .Exec(), RegRead/RegWrite, StrReverse, eval(), the mshtml RunHTMLApplication entry point (the classic rundll32 … mshtml,RunHTMLApplication), a remote HTA over http(s), or an HTA opened from a download/archive path. Legitimate wfshell.exe/MSACCESS.EXE/GTInstaller.exe parents are excluded by the deployed rule.

This is a classic signed-binary script-execution and defence-evasion technique used in phishing and malware loaders: attacker code runs under a Microsoft-signed binary, bypassing application controls and many AV signatures. The analyst's job is to decide whether this is a rare-but-sanctioned application/installer behaviour (false_positive, authorised), malicious HTA/script execution driven by phishing or a loader (true_positive), an unbaselined line-of-business HTA app (misconfiguration), or unproven (needs_escalation). The script payload, whether the HTA is remote/download/archive-sourced, and the launching parent (office/browser/mail = phishing) are the discriminators. This analytic is the upstream companion to the mshta child-process detection: here the concern is the script mshta *ran*; there, what it *launched*.</Overview>
    <BusinessImpact>An alert means: on $host, mshta.exe or rundll32.exe executed script through the HTML-application engine. Both are Microsoft-signed, allow-listed binaries; mshta.exe hosts HTA content, and rundll32.exe mshtml.dll,RunHTMLApplication is the documented way to run HTA/script without an .hta file at all. Adversaries use either to run VBScript/JScript that downloads and executes the next stage - from a phishing document macro, a mshta http://… link, or an HTA delivered in an archive.

The logon/render step is not itself malicious; the script content and its source are the signal. The investigative questions are: what does the script do (fetch over HTTP? execute a command via WScript.Shell/.Exec()? obfuscate with StrReverse/eval?), where did the HTA come from (remote, download, archive, or a local application directory), and what launched the engine (an office/browser/mail/explorer parent is the phishing chain; a recognised installer parent is the benign case the rule already tries to exclude).</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Script Execution via Microsoft HTML Application [NBI-4688].</Goal>
    <Workflow>
      <Step order="1" name="Confirm on the alert host (faithful reproduction of the rule's markers)">
        <Purpose>Confirm on the alert host (faithful reproduction of the rule's markers)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.name) IN ("mshta.exe", "rundll32.exe")
    AND @timestamp >= NOW() - 4 hours
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*runhtmlapplication*" OR cl LIKE "*getobject*" OR cl LIKE "*wscript.shell*" OR cl LIKE "*.regread*" OR cl LIKE "*.regwrite*" OR cl LIKE "*eval(*" OR cl LIKE "*strreverse*" OR cl LIKE "*mshta*http*" OR cl LIKE "*.hta*" OR cl LIKE "*.run(*" OR cl LIKE "*).exec()*"
| KEEP @timestamp, host.name, user.name, process.name, process.parent.name, process.command_line, process.args
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Estate-wide engine prevalence (context for the zero baseline)">
        <Purpose>Estate-wide engine prevalence (context for the zero baseline)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.name) IN ("mshta.exe", "rundll32.exe")
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) IN ("mshta.exe", "rundll32.exe")
| KEEP @timestamp, host.name, user.name, process.name, process.executable, process.parent.name, process.pid, process.parent.pid, process.command_line
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.name) IN ("mshta.exe", "rundll32.exe")
    AND @timestamp >= NOW() - 4 hours
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*.hta*" OR cl LIKE "*runhtmlapplication*" OR cl LIKE "*getobject*" OR cl LIKE "*wscript.shell*" OR cl LIKE "*eval(*" OR cl LIKE "*http*"
| EVAL payload_source = CASE(
    cl LIKE "*mshta*http*" OR cl LIKE "*runhtmlapplication*", "remote-hta-or-inline-rundll",
    cl LIKE "*downloads*", "hta-from-download",
    cl LIKE "*7z*" OR cl LIKE "*rar$*" OR cl LIKE "*temp?_*" OR cl LIKE "*bnz.*", "hta-from-archive",
    cl LIKE "*getobject*" OR cl LIKE "*wscript.shell*" OR cl LIKE "*.run(*" OR cl LIKE "*eval(*" OR cl LIKE "*strreverse*", "inline-script",
    "other")
| STATS execs = COUNT(*) BY payload_source, process.name, process.parent.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) IN ("mshta.exe", "rundll32.exe")
    AND process.args IS NOT NULL
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, user.name, process.name, process.parent.name, process.executable, arguments
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host" AND user.name == "$user"
    AND TO_LOWER(process.name) IN ("mshta.exe", "rundll32.exe")
    AND @timestamp >= NOW() - 4 hours
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*.hta*" OR cl LIKE "*runhtmlapplication*" OR cl LIKE "*getobject*" OR cl LIKE "*wscript.shell*" OR cl LIKE "*eval(*" OR cl LIKE "*http*"
| EVAL parent_class = CASE(
    TO_LOWER(process.parent.name) IN ("winword.exe", "excel.exe", "powerpnt.exe", "outlook.exe", "onenote.exe"), "office",
    TO_LOWER(process.parent.name) IN ("chrome.exe", "msedge.exe", "firefox.exe", "iexplore.exe", "brave.exe"), "browser",
    TO_LOWER(process.parent.name) == "explorer.exe", "explorer-user-clicked",
    TO_LOWER(process.parent.name) IN ("wscript.exe", "cscript.exe", "powershell.exe", "cmd.exe", "mshta.exe"), "script-chain",
    "other")
| STATS execs = COUNT(*) BY parent_class, process.parent.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.parent.pid == $suspicious_pid
| KEEP @timestamp, user.name, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="10" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host" AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY user.name, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "$process"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND user.name == "$user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "certutil.exe", "bitsadmin.exe", "curl.exe", "powershell.exe", "wscript.exe", "cscript.exe", "rundll32.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe", "sdelete.exe", "sdelete64.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND event.code == "4688"
    AND process.parent.pid == $suspicious_pid
| STATS children = COUNT(*), distinct_children = COUNT_DISTINCT(process.name) BY process.name
| SORT children DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Sanctioned local application / installer HTAs. Some enterprise apps and installers legitimately run a local .hta via mshta.exe; the deployed rule already excludes the common wfshell.exe/MSACCESS.EXE/GTInstaller.exe launchers. A recognised installer/application parent, a local path, and no remote/obfuscated payload is the benign case - confirmed, not assumed.</Item>
        <Item>rundll32.exe legitimate uses unrelated to mshtml - most will not carry the script markers and so will not fire; a marker match on rundll32 is unusual.</Item>
        <Item>Administrator/vendor configuration scripts wrapped in an HTA. Confirm against a change record.</Item>
        <Item>A script/HTA positively proven blocked or failed (errored, denied by policy, no download or child activity) is recorded as blocked-malicious, never "benign".</Item>
        <Item>mshta.exe is absent and rundll32.exe carries no script markers in NBI's live 4-hour window. The inline-script/HTA variants this rule isolates are the exception; any match is a high-value lead with no noisy legitimate source to tune out.</Item>
        <Item>The plausible-legitimate locus is an interactive user or a jump/VDI host running a genuine local application HTA; the busy servers are dominated by service/machine-account process creation and would not normally run mshta at all.</Item>
        <Item>The detection is command-line-bound, and command-line auditing is bimodal (~50%). Where the GPO is off, the script markers cannot be seen and the rule cannot match - so a quiet host is not a safe host. Recover the script from EDR/host where the command line is null (section 8).</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. There is no environment-specific allow-list to apply; scope any future exception to an exact application HTA + parent + path + host + user (by identity, matching the deployed rule's excluded-parent approach), after a documented owner is confirmed.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to stop the script fetching further stages and moving on. Coordinate with IT on shared hosts but prioritise containment.</Action>
      <Action>Suspend or force-logoff $user's session on $host and disable the account pending investigation; reset its credentials (section 20).</Action>
      <Action>Terminate the engine process and its descendants ($suspicious_pid tree from section 15.3b / section 17.5).</Action>
      <Action>Preserve volatile evidence first where feasible: running process list, the HTA/script file, any payload it fetched, and memory of the engine. NBI does not collect the script content (unless in the command line) or the network fetch, so host-side capture is the only way to recover them.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove the persistence discovered in section 17.2 (scheduled tasks, Run keys, services) and any dropped payload the script fetched.</Action>
      <Action>Delete the HTA/script and any second-stage binaries, recovered host-side.</Action>
      <Action>Run a full anti-malware / EDR scan on $host and hunt for the same script/HTA and the mshta/rundll32-with-markers pattern across peers - especially any host $user touched (section 15.4, section 17.1).</Action>
      <Action>Remediate the initial-access vector - the phishing message/attachment (section 15.11) and the office/browser/mail/explorer parent that launched the engine (section 15.3).</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Reset $user's password and any credentials accessible from $host during the intrusion window; if privileged accounts logged on there (section 17.3), rotate those too.</Action>
      <Action>Restore $host from a known-good image if the loader established persistence or dropped multiple stages; otherwise validate that all eradication actions hold after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms the engine does not recur with script markers.</Action>
      <Action>Harden. Block or restrict mshta.exe by policy (WDAC/AppLocker), alert on office/browser parents spawning mshta/rundll32, strip HTA attachments at the mail gateway, and - critically for this rule - enable command-line auditing on the host class so the script is actually visible to the detection.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- The script is remote/download/archive-sourced or an inline command execution (section 15.2), especially with obfuscation (StrReverse/eval).
- The engine's parent is office/browser/mail/explorer (section 15.3) - an active phishing chain.
- Download/persistence follow-on is present (section 17.2), or the engine spawned further tooling (section 17.5).
- Lateral movement from $host/$user is observed (section 17.1), especially toward domain controllers or other privileged systems.
- The acting account is privileged/service, or log clearing / audit-policy tampering appears (section 17.4).
- Evidence is incomplete because the command line is null on this host (script invisible in-index) and cannot be recovered - escalate as needs_escalation with the gap named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a recognised local application/installer HTA - matching or resembling the deployed rule's excluded parents, a local path, and no remote/obfuscated payload - is positively matched to the exact engine + $user + $host + time. Record the owner; scope any exception by identity/parent, not a broad rule change.</Item>
    <Item>false_positive (blocked-malicious): the script/HTA was positively proven blocked/failed (no download, no child activity); documented as blocked (never "benign").</Item>
    <Item>misconfiguration: a legitimate application relies on HTA/mshta and was simply not yet baselined/excluded; baseline it and recommend migrating off mshta.</Item>
    <Item>true_positive: malicious HTML-application script execution confirmed (remote/delivered HTA or inline command from a phishing parent); containment/eradication/recovery completed, scope of $user/$host/peers and the phishing source established, no residual persistence or recurrence.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps (null command line, unrecoverable script/parent) documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>This detection lives or dies by the command line. The markers are in process.command_line/process.args, which are only ~50% populated on NBI. Where null, the rule cannot match and the script is invisible in-index - a quiet host is not a safe host; recover the script from EDR/host. Enabling command-line auditing on the interactive/workstation class is the single highest-value hardening ask for this rule.</Item>
    <Item>The reused queries concatenate both fields. TO_LOWER(CONCAT(COALESCE(process.command_line,""), " ", COALESCE(MV_CONCAT(process.args," "),""))) surfaces a marker wherever either field is populated - the robust way to read the script on NBI.</Item>
    <Item>rundll32 … mshtml,RunHTMLApplication is fileless HTA. No .hta file is needed; the RunHTMLApplication marker on rundll32 is as significant as an .hta on mshta.</Item>
    <Item>Parent = initial vector. Office/browser/mail/explorer parents are the phishing fingerprint; a script-chain parent indicates a multi-stage loader; verify a claimed installer parent against the deployed rule's excluded set (wfshell.exe/MSACCESS.EXE/GTInstaller.exe).</Item>
    <Item>Pair with the companion analytic. This rule catches the script mshta ran; the mshta *child-process* rule catches what it launched - investigate them together when both fire on the same host/user.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) mshta zero-baseline and no rundll32 script markers over 4h on logs-system.security-*; (2) command-line/process.args host-bimodality (~50%) directly caps this rule's coverage; (3) process.hash.* absent on 4688. All observed live on 2026-08-19.</Item>
  </Limitations>
</Playbook>