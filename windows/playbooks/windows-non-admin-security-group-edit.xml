<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-non-admin-security-group-edit" name="Non-Admin Edited a Security-enabled Group - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Persistence</Category>
    <MitreAttack>
      <Technique id="T1098"/>
      <Technique id="T1078"/>
      <Tactic id="TA0003"/>
      <Tactic id="TA0004"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Account Manipulation (T1098)" url="https://attack.mitre.org/techniques/T1098/"/>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts (T1078)" url="https://attack.mitre.org/techniques/T1078/"/>
      <Reference title="MITRE ATT&amp;CK - Persistence tactic (TA0003)" url="https://attack.mitre.org/tactics/TA0003/"/>
      <Reference title="MITRE ATT&amp;CK - Privilege Escalation tactic (TA0004)" url="https://attack.mitre.org/tactics/TA0004/"/>
      <Reference title="Microsoft Learn - Event 4728 (member added to a security-enabled global group)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4728"/>
      <Reference title="Microsoft Learn - Event 4729 (member removed from a security-enabled global group)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4729"/>
      <Reference title="Microsoft Learn - Event 4730 (security-enabled global group deleted)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4730"/>
      <Reference title="Microsoft Learn - Event 5136 (a directory service object was modified)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-5136"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Non-Admin Edited a Security-enabled Group</RuleName>
    <RuleId>61ece333-1a2c-48c0-b06b-50a8c3dbf30b</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is a query-type analytic (kuery) over logs-system.security-*. It selects security-enabled global group changes by an editor not named like an administrator:



- 4728 - a member was added to a security-enabled global group (the escalation-relevant action).
- 4729 - a member was removed from a security-enabled global group.
- 4730 - a security-enabled global group was deleted (destructive).
- The not user.name : (*admin* or *ADMIN*) clause excludes any editor whose account name contains "admin"; the actor is carried in winlog.event_data.SubjectUserName, the group in winlog.event_data.TargetUserName, and the affected member (a DN) in winlog.event_data.MemberName.

Plain English: an account whose name does not contain "admin" changed the membership of, or deleted, a security-enabled global group. The rule treats any non-"admin"-named editor as unexpected, on the premise that group membership should be managed only by privileged identities - a premise that is both over-inclusive (delegated managers/service accounts trip it) and evadable (a compromised *.admin account is silently excluded). Those are genuine coverage caveats carried through section 8 and section 23.</TriggerLogic>
    <KeyAlertFields>host.name</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$actor"/>
    <Input name="$group"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Non-Admin Edited a Security-enabled Group detection on NBI's Elastic Security deployment. The rule fires when a security-enabled global group is changed - a member is added (Event 4728), removed (4729), or the group is deleted (4730) - by an account whose name does not contain "admin". Security-group membership frequently governs access (VPN, application, file, or privileged access), so an unexpected editor is a classic privilege-escalation and persistence move: an attacker who has footholded a normal account grants itself or an accomplice into an access-bearing group.

The catch - central to this investigation - is that the rule's "admin" test is a naive name-substring check, so many legitimate editors (delegated group managers, IAM/provisioning service accounts, helpdesk roles) trip it simply because their names lack "admin". The analyst's job is to decide whether a genuinely unauthorised principal altered a sensitive group (true_positive), an authorised-but-not-admin-named editor did routine work (false_positive), a provisioning/sync service is mis-flagged (misconfiguration), or authorisation is unclear (needs_escalation) - using the actual *privilege and role* of the actor, not the name the rule keyed on.</Overview>
    <BusinessImpact>An alert means: on a domain controller, $actor (an account not named like an admin) changed security-enabled global group $group - added or removed a member, or deleted the group. The change has already been written to the directory. What the alert does *not* establish is authorisation: the identical event is produced by a delegated helpdesk operator doing sanctioned work and by a compromised normal account escalating its own access.

The investigative questions are: exactly what changed (a member added - especially the actor adding *itself* - vs a benign removal; one group vs several), does $actor have any legitimate directory-management role (a broad, steady pattern of directory admin events vs a single isolated edit from an account that never touches the directory), and how sensitive is $group (VPN/remote-access, privileged-access, application-admin, or file-access groups are high value) and is $actor an outlier among the accounts that normally edit it. A self-add into a sensitive access group by an account with no admin baseline is the strongest true-positive shape.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Non-Admin Edited a Security-enabled Group.</Goal>
    <Workflow>
      <Step order="1" name="Recover exactly what the actor changed">
        <Purpose>Recover exactly what the actor changed</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.SubjectUserName == "$actor"
    AND event.code IN ("4728","4729","4730") AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, event.code, winlog.event_data.TargetUserName, winlog.event_data.MemberName, host.name
| SORT @timestamp DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm the change on the target group">
        <Purpose>Confirm the change on the target group</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.TargetUserName == "$group"
    AND event.code IN ("4728","4729","4730") AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, event.code, winlog.event_data.SubjectUserName, winlog.event_data.MemberName, host.name
| SORT @timestamp DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$actor"
    AND event.code IN ("4728","4729","4730","4732","4733","4756","4757")
| KEEP @timestamp, event.code, winlog.event_data.TargetUserName, winlog.event_data.MemberName, host.name
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND winlog.event_data.SubjectUserName == "$actor"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY process.name
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="5" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND winlog.event_data.SubjectUserName == "$actor"
    AND TO_LOWER(process.name) IN ("net.exe", "net1.exe", "dsadd.exe", "dsmod.exe", "powershell.exe", "cmd.exe", "mmc.exe", "wmic.exe")
| STATS executions = COUNT(*) BY process.parent.name, process.name, host.name
| SORT executions DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="6" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.SubjectUserName == "$actor"
    AND event.code IN ("4728","4729","4732","4733","4756","4757","4720","4722","4724","4726","4672")
    AND @timestamp >= NOW() - 4 hours
| STATS events = COUNT(*), hosts = COUNT_DISTINCT(host.name)
    BY event.code
| SORT events DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="7" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$actor"
    AND event.code IN ("4728","4729","4730","4732","4756","4720","4672","4688")
| STATS events = COUNT(*), distinct_codes = COUNT_DISTINCT(event.code) BY host.name
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$actor"
    AND source.ip IS NOT NULL AND source.ip != "::1"
| STATS logons = COUNT(*) BY source.ip, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND winlog.event_data.TargetUserName == "$actor"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$actor"
    AND event.code IN ("4728","4729","4730","4732","4733","4756","4757","4720","4722","4724","4726","4672","4688")
| KEEP @timestamp, event.code, winlog.event_data.TargetUserName, winlog.event_data.MemberName, process.name, host.name
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND user.name == "$actor"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$actor"
    AND event.code IN ("4728","4732","4756","4720","4722","4724","7045","4698")
| STATS events = COUNT(*), distinct_targets = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY event.code
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="13" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND winlog.event_data.SubjectUserName == "$actor"
| STATS special_priv_events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY host.name
| SORT special_priv_events DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="14" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND winlog.event_data.SubjectUserName == "$actor"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, host.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="15" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security-*
| WHERE winlog.event_data.TargetUserName == "$group"
    AND event.code IN ("4728","4729","4730") AND @timestamp >= NOW() - 4 hours
| STATS changes = COUNT(*), editors = COUNT_DISTINCT(winlog.event_data.SubjectUserName),
        editor_list = VALUES(winlog.event_data.SubjectUserName)
    BY event.code
| SORT changes DESC
| LIMIT 15
]]>
        </Query>
      </Step>
      <Step order="16" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Delegated group managers and helpdesk roles whose account names simply lack "admin". These are the dominant benign case - confirmed by a rich, steady directory-management baseline (section 15.4) and the actor being an established editor of the group (section 17.5), not assumed.</Item>
        <Item>IAM / HR-provisioning or directory-sync service accounts that manage memberships programmatically (a misconfiguration/naming-gap case - the account should be excluded by identity, not by the *admin substring).</Item>
        <Item>Routine membership removals (4729) as part of joiner-mover-leaver processes.</Item>
        <Item>A change positively proven to have failed or been rolled back (denied, or immediately reversed with no effective access granted) is recorded as blocked-malicious/ineffective, never a bare "benign".</Item>
        <Item>Group changes are low-volume and largely made by delegated, non-"admin"-named accounts on the DCs. In the live window, security-enabled group edits were made by accounts such as Abdulrahman.Diaa, Mohammed.Adnan, Yasser.Amar, and Lana.alaa (none containing "admin") against groups like Audi.security, Disk Encryption, and BPMUSERS, alongside genuinely *admin-named accounts (jamal.admin, karrar.admin) that the rule excludes. The non-admin-named editors are exactly what this rule surfaces - most are delegated managers, so the actor's role/baseline (section 15.4) is the decisive check.</Item>
        <Item>The DCs that log these events are nim-dc-dbap01 and nim-dc2-dbap. Group-management activity concentrates there; the actor's origin and session (section 15.6/section 15.12) help separate a sanctioned admin workstation from an unexpected source.</Item>
        <Item>Sensitive vs low-value groups differ sharply. Audi.security, Network Admins, LAPS Workstation, and Solarwinds-mw.network-team are access/privilege-bearing; a self-service or distribution-style group is lower. Judge $group by function (section 17.5).</Item>
        <Item>No blanket exclusion should be encoded by name. Recurring authorised editors should be allowed by identity, never by widening the *admin substring (which only deepens the evasion gap).</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Reverse the unauthorised membership change on $group (remove an added member / restore a removed one / recover a deleted group) once a true_positive is confirmed - coordinate the directory write with the AD/IAM team via the authorised change path.</Action>
      <Action>Disable $actor pending investigation and reset its credentials (section 20); if the actor added an accomplice account, disable that too.</Action>
      <Action>Freeze the access the group grants if it is high-value (e.g. temporarily gate the VPN/application access the group confers) until scope is understood.</Action>
      <Action>Preserve evidence first: the group's current and prior membership, the actor's session details, and the DC security logs - before reversing, so the change and its effect are documented.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove any accomplice accounts or additional memberships the actor created (section 17.2), and audit all recent edits to $group and to any other group the actor touched (section 15.1).</Action>
      <Action>Revoke exercised access. Determine whether the new membership was used (VPN session, application login, admin action) and contain those sessions; assume the actor's credential is compromised and hunt reuse (section 17.1).</Action>
      <Action>Reset $actor's credentials and any privileged credentials exposed during the actor's session; review directory ACLs that allowed the edit and remove the mis-delegated permission.</Action>
      <Action>Remediate the initial-access vector that compromised the actor account.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Restore correct group membership and ownership, and confirm the group's access grants match policy after remediation.</Action>
      <Action>Reset $actor's password and rotate any credentials exposed during the incident; re-enable the account only after remediation and monitoring.</Action>
      <Action>Return access to service only after section 22 closing criteria are met and monitoring confirms no unauthorised re-edit of $group or sibling groups.</Action>
      <Action>Harden. Tighten group-management delegation (least privilege), **replace the rule's name-based *admin test with an identity/privilege-and-sensitivity-aware model, and deploy a 5136/DACL-and-attribute analytic** on sensitive groups to close the direct-directory-write and domain-local/universal gaps (section 8) - the highest-value hardening from this rule.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / IR and the AD/IAM team (and notify the customer) when any of the following hold:

- A confirmed self-add or an addition to a privileged/access-bearing group (section 17.5) by an actor with no admin/delegated baseline (section 15.4/section 17.3).
- Deletion of a security-enabled group (4730), or multiple additions across groups (section 17.2).
- The actor shows onward movement (section 17.1) or defence evasion / log clearing (section 17.4).
- The new access has already been exercised, or $group governs VPN / banking-application / privileged access.
- The actor's authorisation to manage $group cannot be established (owner/delegation unknown) - escalate as needs_escalation to the AD/IAM team and the group owner.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): $actor is confirmed an authorised delegated group manager / helpdesk role whose name merely lacks "admin" (rich management baseline in section 15.4, established editor in section 17.5, change fits the role) - documented. Add a recurring authorised editor to an identity-based allowance; never widen the *admin substring.</Item>
    <Item>false_positive (blocked/ineffective): the change was positively proven to fail or be rolled back with no effective access granted; documented as such, never a bare "benign".</Item>
    <Item>misconfiguration: $actor is a recognised IAM/HR-provisioning or directory-sync service account making routine, patterned changes; exclude it by identity and recommend a privilege/sensitivity-aware rule.</Item>
    <Item>true_positive: an unauthorised principal expanded (or destroyed) access via a security-enabled group; membership reversed, $actor remediated, exercised access reviewed, initial foothold hunted, no residual or recurrence.</Item>
    <Item>needs_escalation: handed to AD/IAM + Tier 3/IR with the specific authorisation gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Privilege, not the name, decides it. The rule keys on a *admin name-substring; the verdict comes from the actor's actual directory-management role (section 15.4/section 17.3) and the group's sensitivity (section 17.5). A delegated manager without "admin" in their name is the dominant benign case.</Item>
    <Item>Self-add is the sharpest signal. Compare winlog.event_data.MemberName (a DN) to $actor - an actor adding itself to an access group is the textbook privesc/persistence move.</Item>
    <Item>Two deployed-rule coverage gaps - surface for tuning (do not auto-fix): (1) the *admin exclusion is evadable - a compromised *.admin account is silently excluded and never fires; (2) the rule sees only security-enabled global groups (4728/4729/4730) - domain-local/universal edits (4732/4733/4756/4757) and direct 5136 directory writes bypass it. Recommend a complementary 5136/DACL-and-attribute analytic on sensitive groups and an identity/privilege-aware editor model.</Item>
    <Item>Allow by identity, never by name. Recurring authorised non-admin-named editors (delegated managers, provisioning accounts) should be excluded by account, which also avoids deepening the evasion gap.</Item>
    <Item>Empty != safe (sparse events). Directory changes are ~2 per 4h; the alert may predate the window. Re-run anchored to the alert time before concluding absence.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) non-"admin"-named delegated editors are normal on NBI (Abdulrahman.Diaa, Mohammed.Adnan, Yasser.Amar, Lana.alaa) - role/baseline is the discriminator; (2) group changes concentrate on nim-dc-dbap01/nim-dc2-dbap; (3) rule gaps: *admin evasion + no 4732/4733/4756/4757/5136 coverage. All observed live on 2026-08-19.</Item>
  </Limitations>
</Playbook>