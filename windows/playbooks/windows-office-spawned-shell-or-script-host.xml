<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-office-spawned-shell-or-script-host" name="Windows — Office Application Spawned a Command Shell or Script Host - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Execution</Category>
    <MitreAttack>
      <Technique id="T1566.001"/>
      <Technique id="T1566"/>
      <Technique id="T1204.002"/>
      <Technique id="T1204"/>
      <Technique id="T1059"/>
      <Technique id="T1059.001"/>
      <Tactic id="TA0002"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - User Execution: Malicious File (T1204.002)" url="https://attack.mitre.org/techniques/T1204/002/"/>
      <Reference title="MITRE ATT&amp;CK - Phishing: Spearphishing Attachment (T1566.001)" url="https://attack.mitre.org/techniques/T1566/001/"/>
      <Reference title="MITRE ATT&amp;CK - Command and Scripting Interpreter (T1059)" url="https://attack.mitre.org/techniques/T1059/"/>
      <Reference title="MITRE ATT&amp;CK - PowerShell (T1059.001)" url="https://attack.mitre.org/techniques/T1059/001/"/>
      <Reference title="MITRE ATT&amp;CK - Execution tactic (TA0002)" url="https://attack.mitre.org/tactics/TA0002/"/>
      <Reference title="Elastic Security - detection rules repository (Execution / Office child-process analytics)" url="https://github.com/elastic/detection-rules"/>
      <Reference title="Red Canary - Threat Detection: Office applications spawning command shells" url="https://redcanary.com/threat-detection-report/techniques/"/>
      <Reference title="LOLBAS - Mshta / Regsvr / Rundll32 / Certutil (interpreter LOLBins)" url="https://lolbas-project.github.io/"/>
      <Reference title="Microsoft Learn - Command line process auditing (Event 4688 command-line GPO)" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/command-line-process-auditing"/>
      <Reference title="Microsoft Learn - Attack Surface Reduction rules (block Office child processes)" url="https://learn.microsoft.com/en-us/defender-endpoint/attack-surface-reduction-rules-reference"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Windows — Office Application Spawned a Command Shell or Script Host</RuleName>
    <RuleId>nbi-win-office-spawns-shell</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is a KQL (query) rule on logs-system.security-*. Its logic in plain English: a 4688 process creation where process.parent.name is one of the six core Office apps AND process.name is one of the ten shell/script-host/download interpreters.

One-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline):



The rule matches on the relationship, not on any command-line content - so it fires whether or not the host captures the interpreter's arguments. That is deliberate: on NBI process.command_line is only ~50% populated (see section 8), so the payload text can be missing even on a true firing. The parent-child shape survives that gap; the command line, when present, decides intent.</TriggerLogic>
    <KeyAlertFields>powershell.exe, pwsh.exe, powershell.file.script_block_text, process.name, process.executable, process.parent.name, process.pid, process.parent.pid, process.entity_id, user.name, process.command_line, process.args</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Windows - Office Application Spawned a Command Shell or Script Host detection on NBI's Elastic Security deployment. The rule fires on a process-creation event (Windows Security 4688) where the parent process is a Microsoft Office application (winword.exe, excel.exe, powerpnt.exe, outlook.exe, onenote.exe, msaccess.exe) and the child process is a command shell, script host, or download LOLBin (cmd.exe, powershell.exe, pwsh.exe, wscript.exe, cscript.exe, mshta.exe, rundll32.exe, regsvr32.exe, bitsadmin.exe, certutil.exe).

An Office document has essentially no legitimate reason to launch a shell or scripting engine; this parent-child relationship is the observable signature of document-borne code execution - a malicious macro, an embedded object, or a DDE/OLE payload running when a user opens a phishing attachment. The analyst's job is to decide, quickly and defensibly, whether malicious code executed (true_positive), whether this is a rare authorised Office automation or a positively-blocked attempt (false_positive), a benign add-in/template artifact (misconfiguration), or unproven (needs_escalation) - with the interpreter's command line and any follow-on chain as evidence.</Overview>
    <BusinessImpact>An alert means: on $host, a Microsoft Office process directly spawned a command interpreter or scripting engine under $user. Office applications are document editors - Word, Excel, PowerPoint, Outlook, OneNote, Access. In normal use they do not shell out to cmd/PowerShell/wscript/mshta/rundll32/certutil. When they do, it is almost always because content *inside a document* executed code: a VBA macro calling Shell/WScript.Shell, an XLM/Excel-4.0 macro, an embedded OLE package, a DDE field, or a Follina-style template/protocol handler.

The child process already ran - this is an execution event, not an attempt to run. The investigative question is therefore not *did code execute* (it did) but *what did it do and was it authorised*: the interpreter's command line (a download cradle, an encoded command, a remote scriptlet) and whatever the interpreter spawned next tell you whether this is the first on-host step of an intrusion or a rare sanctioned automation.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Windows — Office Application Spawned a Command Shell or Script Host.</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND event.type == "start"
    AND TO_LOWER(process.parent.name) IN ("winword.exe","excel.exe","powerpnt.exe","outlook.exe","onenote.exe","msaccess.exe")
    AND TO_LOWER(process.name) IN ("cmd.exe","powershell.exe","pwsh.exe","wscript.exe","cscript.exe","mshta.exe","rundll32.exe","regsvr32.exe","bitsadmin.exe","certutil.exe")
| KEEP @timestamp, host.name, user.name, process.parent.name, process.name, process.executable, process.command_line
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm on the alert host and recover the payload">
        <Purpose>Confirm on the alert host and recover the payload</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host"
    AND process.parent.name IN ("winword.exe","excel.exe","powerpnt.exe","outlook.exe","onenote.exe","msaccess.exe")
    AND process.name IN ("cmd.exe","powershell.exe","pwsh.exe","wscript.exe","cscript.exe","mshta.exe","rundll32.exe","regsvr32.exe","bitsadmin.exe","certutil.exe")
    AND @timestamp >= NOW() - 4 hours
| EVAL argline = MV_CONCAT(process.args, " ")
| KEEP @timestamp, process.parent.name, process.name, process.command_line, argline, winlog.event_data.SubjectUserName
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.parent.name) IN ("winword.exe","excel.exe","powerpnt.exe","outlook.exe","onenote.exe","msaccess.exe")
| KEEP @timestamp, user.name, process.parent.name, process.name, process.executable, process.pid, process.parent.pid
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.parent.name) IN ("winword.exe","excel.exe","powerpnt.exe","outlook.exe","onenote.exe","msaccess.exe")
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-windows.powershell*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4104" AND host.name == "$host"
    AND (TO_LOWER(powershell.file.script_block_text) LIKE "*downloadstring*"
         OR TO_LOWER(powershell.file.script_block_text) LIKE "*downloadfile*"
         OR TO_LOWER(powershell.file.script_block_text) LIKE "*invoke-webrequest*"
         OR TO_LOWER(powershell.file.script_block_text) LIKE "*frombase64string*"
         OR TO_LOWER(powershell.file.script_block_text) LIKE "*iex*")
| KEEP @timestamp, host.name, powershell.file.script_block_text
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.parent.name) IN ("cmd.exe","powershell.exe","pwsh.exe","wscript.exe","cscript.exe","mshta.exe","rundll32.exe","regsvr32.exe")
    AND @timestamp >= NOW() - 4 hours
| STATS execs = COUNT(*), last_seen = MAX(@timestamp) BY process.parent.name, process.name
| SORT execs DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="7" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="9" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host" AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.parent.name) IN ("winword.exe","excel.exe","powerpnt.exe","outlook.exe","onenote.exe","msaccess.exe","cmd.exe","powershell.exe","pwsh.exe","wscript.exe","cscript.exe","mshta.exe","rundll32.exe","regsvr32.exe")
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable, process.name
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="11" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="12" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688" AND host.name == "$host" AND user.name == "$user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="13" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND user.name == "$user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "wscript.exe", "cscript.exe", "rundll32.exe", "mshta.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="15" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672" AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="16" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe", "sdelete.exe", "sdelete64.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="17" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host" AND event.code == "4688"
    AND TO_LOWER(process.parent.name) IN ("cmd.exe","powershell.exe","pwsh.exe","wscript.exe","cscript.exe","mshta.exe","rundll32.exe","regsvr32.exe")
| STATS children = COUNT(*), distinct_children = COUNT_DISTINCT(process.name) BY process.parent.name, process.name
| SORT children DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="18" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Sanctioned Office add-ins or integrated line-of-business tools that legitimately shell out - an add-in that calls cmd/PowerShell to run a known helper during a document workflow. Rare, and it must be confirmed with the application owner (authorisation proven, not assumed).</Item>
        <Item>Deployment/templating tooling that drives Office headlessly (e.g. report generation, mail-merge automation) and shells to a helper as a side effect.</Item>
        <Item>Administrator or red/purple-team testing deliberately running an Office-to-shell technique. This is *not* benign - it is authorised malicious-technique execution and is classified false_positive (blocked/authorised) only against a change ticket or exercise ROE, never dismissed on sight.</Item>
        <Item>The Office-to-interpreter pattern has a zero baseline in NBI. Over a 4-hour window across the estate, no Office application (winword/excel/powerpnt/outlook/onenote/msaccess) appeared as the parent of any process in 4688. There is no noisy legitimate source to tune out - so any firing is a strong anomaly with no standing exception to apply.</Item>
        <Item>Command-line capture is bimodal and host-dependent. Some hosts populate process.command_line/process.args fully (e.g. the automation servers nim-st-apv10/-apv11 at ~100%), while the interactive workstation/jump tier where a user would open a document often captures neither. Expect the payload text to be null exactly on the hosts where this attack is most plausible; corroborate with process.args and, failing that, lineage and follow-on behaviour.</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. Do not create a blanket exception off a single alert. If one is ever warranted, scope it to the exact Office parent + interpreter image + command + user, and only after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to stop post-execution activity and C2. On a shared workstation/jump host, coordinate with IT but prioritise containment.</Action>
      <Action>Suspend or force-logoff $user's session on $host and disable the account pending investigation; the account's credentials are exposed and must be reset (section 20).</Action>
      <Action>Terminate the interpreter and its descendants (the child PID tree from section 15.3/section 17.5) if the host cannot yet be isolated.</Action>
      <Action>Preserve volatile evidence first where feasible: the running process list, the source document and any temp copy, the interpreter's memory, and the user's Outlook/mailbox item - NBI does not capture the maldoc, so host/mailbox-side capture is the only way to recover it.</Action>
      <Action>Block the delivery indicators (sender, URL, attachment hash) at the mail gateway and endpoints once identified out of band. Deploy changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Remove the persistence discovered in section 17.2 (services, scheduled tasks, Run keys, rogue accounts) and any dropped payload identified via section 15.10 (process.executable path).</Action>
      <Action>Delete the source document from the host and quarantine the delivering email across all recipient mailboxes; pull the same attachment/sender from the mail gateway estate-wide.</Action>
      <Action>Run a full anti-malware / EDR scan on $host and hunt for the same payload hash and delivery indicators across peers, especially other hosts $user touched (section 15.4, section 17.1).</Action>
      <Action>Remediate the initial-access vector: confirm macro policy, and remove the offending add-in/template if a benign-but-dangerous automation was the cause.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Reset $user's password and any credentials accessible from $host during the compromise window; if privileged accounts logged on there (section 17.3), rotate those too.</Action>
      <Action>Restore $host from a known-good image if persistence or tampering was extensive; otherwise validate that all eradication actions hold after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms the Office-to-interpreter condition does not recur.</Action>
      <Action>Recommend host hardening (section 23): Office macro restrictions from the internet, Attack Surface Reduction rules blocking Office child processes, and command-line auditing on the workstation class would have made this exact investigation far stronger.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- The interpreter command line is a download cradle / encoded command / remote scriptlet (section 14.2), or a decoded PowerShell payload is hostile (section 15.2b).
- A recon/download/execution follow-on chain is visible (section 15.3, section 17.5), or persistence was installed (section 17.2).
- Lateral movement from $host/$user is observed (section 17.1), especially toward domain controllers or privileged systems.
- Local privilege escalation followed the execution (section 17.3) or log clearing/audit tampering appears (section 17.4).
- Evidence is incomplete because of NBI's telemetry gaps (command line blind, maldoc/network not collected) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a change ticket, a documented add-in/automation, or a sanctioned red/purple-team ROE is positively matched to the exact Office parent + interpreter + command + $user + $host + time. Record the reference; scope any exception to that exact tuple, never a blanket rule.</Item>
    <Item>false_positive (blocked-malicious): the interpreter launched but its command was positively proven prevented/errored with no second stage; documented as a blocked malicious attempt, never "benign".</Item>
    <Item>misconfiguration: a benign add-in/template/deployment script reproducibly produced the spawn without attacker involvement; a hardening/tuning action is raised.</Item>
    <Item>true_positive: document-borne execution confirmed; containment/eradication/recovery completed, scope of $user/$host/peers established, delivery indicators blocked, and no residual persistence or recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Zero baseline = high fidelity. No Office application appeared as a process parent in NBI's 4-hour Windows telemetry. There is nothing legitimate to tune out, so this rule should be near-silent; when it fires, believe it.</Item>
    <Item>The payload is in the command line, and the command line is bimodal. Recover it with process.command_line and MV_CONCAT(process.args, " ") (section 14.2). It is populated on the automation tier (e.g. nim-st-apv10/-apv11 ~100%) but frequently null on the interactive workstation/jump tier where a maldoc is most plausible. A null command line is a telemetry gap, not evidence of benign use.</Item>
    <Item>Use the PowerShell operational log for decoded content. When the child is powershell.exe/pwsh.exe, logs-windows.powershell-* 4104 recovers the decoded script that 4688 cannot (section 15.2b). Key it on host.name - the operational log's user.name is SYSTEM and powershell.connected_user.name is null on NBI.</Item>
    <Item>winlog.event_data.TargetUserName is null on 4688. The acting user on process creation is user.name / winlog.event_data.SubjectUserName. Key all process/user pivots on user.name; do not reuse a logon-style TargetUserName predicate on 4688.</Item>
    <Item>No Sysmon → PID-based lineage. Reconstruct trees with process.pid/process.parent.pid in a tight window and corroborate with process.parent.name (PIDs are reused).</Item>
    <Item>The maldoc and the network are invisible here. Recover the document and the delivering email from the host/mailbox out of band, and pivot egress on FortiGate by host IP. Empty in-index results never clear the host.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) Office-parent → interpreter-child zero-baseline over 4h on logs-system.security-*; (2) process.command_line/process.args host-bimodality (automation tier populated vs interactive tier null); (3) logs-windows.powershell-* 4104 present (~2.3k/4h) with connected_user.name null → key on host; (4) winlog.event_data.TargetUserName null on 4688. All observed live on 2026-08-19.</Item>
  </Limitations>
</Playbook>