<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-password-spray-single-source" name="Windows — Password Spray From a Single Source - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="Medium">Medium</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1110"/>
      <Technique id="T1110.003"/>
      <Tactic id="TA0006"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Brute Force (T1110)" url="https://attack.mitre.org/techniques/T1110/"/>
      <Reference title="MITRE ATT&amp;CK - Password Spraying (T1110.003)" url="https://attack.mitre.org/techniques/T1110/003/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="Elastic Security - Password spraying / multiple-account-failure detections" url="https://github.com/elastic/detection-rules"/>
      <Reference title="Microsoft Learn - Event 4625 (An account failed to log on) and status/sub-status codes" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4625"/>
      <Reference title="Microsoft Learn - Event 4624 (An account was successfully logged on) and logon types" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4624"/>
      <Reference title="Microsoft - Password spray investigation guidance" url="https://learn.microsoft.com/en-us/security/operations/incident-response-playbook-password-spray"/>
      <Reference title="CISA - Detecting and mitigating password spraying" url="https://www.cisa.gov/news-events/alerts/2019/09/05/acsc-releases-advisory-password-spraying-attacks"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Windows — Password Spray From a Single Source</RuleName>
    <RuleId>nbi-auth-spray-single-source</RuleId>
    <RuleType>threshold</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is a threshold rule on logs-system.security-*. Its pre-threshold selection and grouping:

- Selection (Kibana KQL):



- Threshold: group by source.ip; fire when cardinality of winlog.event_data.TargetUserName ≥ 10 within the 1-hour window (evaluated from now-70m).
- Exclusions baked into the rule: ANONYMOUS LOGON, and the internal auth-aggregator subnets 10.11.15.0/24, 10.11.101.0/24, 10.11.102.0/24 (NAT/proxy egress points that legitimately front many users and would otherwise trip the cardinality threshold).

Plain English: one source IP failed network sign-in against ≥10 distinct accounts in an hour. This is a volume/cardinality signal - it characterises the *shape* of the traffic; it does not by itself prove any account was compromised. Success (section 15.6b/section 17.5) decides the verdict.</TriggerLogic>
    <KeyAlertFields>source.ip, event.outcome, host.name</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Windows - Password Spray From a Single Source detection on NBI's Elastic Security deployment. The rule is a threshold analytic over Windows Security 4625 network-logon (LogonType 3) failures, grouped by source.ip, that fires when a single source IP fails against 10 or more distinct target accounts within a 1-hour window - the fan-out shape of password spraying (one source, many accounts, few attempts each, to stay under per-account lockout).

The single most important decision is did the attack succeed - did any targeted account produce a successful sign-in (Event 4624) from this source, or follow-on malicious access. The analyst classifies the alert as true_positive (a targeted credential was used / post-access occurred), false_positive (a genuine malicious spray positively proven blocked/unsuccessful - documented as blocked-malicious, never "benign"), misconfiguration (a stale-credential or service retry loop, not a real spray), or needs_escalation (success/failure cannot be established).</Overview>
    <BusinessImpact>An alert means: from $source_ip, Windows recorded failed network logons (4625, type 3) against ten or more different accounts inside an hour. LogonType 3 is a network logon - SMB, RPC, HTTP/WinRM, LDAP bind, or any service that authenticates over the network - so the source is reaching the estate's authentication surface (typically domain controllers and servers) and trying account after account.

The spray *shape* is established by the rule; what remains is intent and outcome:

- Outcome: did any of those accounts *succeed* from this source (a 4624)? Windows authoritatively logs both failure and success, so 4625s with no corresponding 4624 for a given (account, source) positively establishes that those attempts failed.
- Intent/nature: is the source a pure attacker (only failures) or a legitimate host/service whose *stale credential* is looping (failures alongside its own successes, Kerberos, processes, and share access)? The former is a genuine spray; the latter is a misconfiguration wearing a spray's clothes.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Windows — Password Spray From a Single Source.</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND event.code == "4625" AND winlog.event_data.LogonType == "3"
    AND source.ip IS NOT NULL
    AND TO_LOWER(winlog.event_data.TargetUserName) != "anonymous logon"
| STATS distinct_targets = COUNT_DISTINCT(winlog.event_data.TargetUserName), failures = COUNT(*) BY source.ip
| WHERE distinct_targets >= 10
| SORT distinct_targets DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm on the alert source and profile the burst">
        <Purpose>Confirm on the alert source and profile the burst</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip" AND event.code == "4625"
    AND winlog.event_data.LogonType == "3"
    AND @timestamp >= NOW() - 2 hours
| STATS failures = COUNT(*), reasons = VALUES(winlog.event_data.SubStatus)
    BY winlog.event_data.TargetUserName
| SORT failures DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4625")
| STATS events = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="4" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4625") AND winlog.event_data.LogonType == "3"
| STATS failed = COUNT(CASE(event.code == "4625", 1, null)),
        succeeded = COUNT(CASE(event.code == "4624", 1, null))
    BY winlog.event_data.TargetUserName
| SORT failed DESC
| LIMIT 60
]]>
        </Query>
      </Step>
      <Step order="5" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4625")
| STATS attempts = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName) BY host.name, event.code
| SORT attempts DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="6" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip"
    AND @timestamp >= NOW() - 2 hours
| STATS events = COUNT(*) BY event.code, event.outcome
| SORT events DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="7" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip" AND event.code IN ("4624","4625")
    AND winlog.event_data.LogonType == "3"
    AND @timestamp >= NOW() - 2 hours
| STATS failed = COUNT(CASE(event.code == "4625", 1, null)),
        succeeded = COUNT(CASE(event.code == "4624", 1, null))
    BY winlog.event_data.TargetUserName
| WHERE succeeded > 0
| SORT succeeded DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="8" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE source.ip == "$source_ip" AND event.code IN ("5140","5145")
    AND @timestamp >= NOW() - 2 hours
| STATS accesses = COUNT(*)
    BY winlog.event_data.ShareName, host.name
| SORT accesses DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="9" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4625", "4634", "4647", "4648", "4768", "4769")
| STATS events = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp)
    BY event.code, event.outcome
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="10" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4625", "5140", "5145")
| KEEP @timestamp, event.code, event.outcome, winlog.event_data.TargetUserName, winlog.event_data.LogonType, host.name, winlog.event_data.ShareName, winlog.event_data.SubStatus
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="11" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4624", "4648", "5140", "5145")
| STATS events = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName)
    BY host.name, event.code, winlog.event_data.ShareName
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="12" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip"
    AND event.code IN ("4769", "5140", "5145")
| STATS events = COUNT(*), accounts = COUNT_DISTINCT(winlog.event_data.TargetUserName)
    BY event.code, host.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 2 hours
    AND source.ip == "$source_ip" AND event.code == "4624"
    AND winlog.event_data.LogonType == "3"
| STATS successes = COUNT(*), first_success = MIN(@timestamp), last_success = MAX(@timestamp), hosts = COUNT_DISTINCT(host.name)
    BY winlog.event_data.TargetUserName
| SORT successes DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="14" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Stale/incorrect stored credentials on a service, scheduled task, mapped drive, or application that retries continuously after a password change - producing repeated failures. This is the most common benign cause, but it usually concentrates on a fixed small set of service accounts, not ten-plus distinct users.</Item>
        <Item>Misconfigured application or middleware cycling a credential set (e.g. a connector trying several service identities) from a working host.</Item>
        <Item>Authorised security scanners / credential-validation tooling (including ScanWave) that test authentication. This is investigation context only - the identical success/blocked decision logic applies, and authorisation must be positively proven, never assumed. Scanners are never auto-trusted or whitelisted.</Item>
        <Item>The rule already excludes NBI's auth-aggregator subnets (10.11.15.0/24, 10.11.101.0/24, 10.11.102.0/24). These are real NAT/proxy egress points where one IP fronts many users; live data confirms the highest-cardinality "spray-shaped" sources sit in 10.11.15.0/24 (24 and 22 distinct accounts), which is exactly why they are excluded. Before closing a borderline case, re-validate that these exclusion subnets still match current infrastructure - if the estate re-IPs, the exclusions can blind the rule or, conversely, a real attacker inside those subnets is invisible.</Item>
        <Item>source.ip is present on ~71% of 4625/4624 (network/type-3 logons) but null on local/type-2. This is a source-keyed rule, so local-interactive failures are out of its scope by design.</Item>
        <Item>Enumeration vs guessing is visible in SubStatus. In the validated sample, 10.11.9.1 fired 169 failures against a single non-existent account (SRLCL, 0xc0000064) plus one bad-password failure each against several real users - a mixed enumeration/guessing pattern worth characterising, not a clean textbook spray. Read the reasons before deciding.</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. Do not create a blanket source exception; if warranted, scope it to the exact source + accounts + service after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>If a victim account succeeded (section 15.6b/section 17.5): force-reset that account immediately and revoke its Kerberos tickets and active sessions, before the attacker consolidates the foothold.</Action>
      <Action>Isolate hosts showing post-access (section 17.1) where the source reached ADMIN$/C$ or authenticated onward.</Action>
      <Action>Take defensive action on $source_ip if it is external/unauthorised: deny/block at the firewall/VPN and monitor for it re-appearing from a new IP (sprayers rotate sources).</Action>
      <Action>Preserve evidence: capture the section 14.2/section 15.6 outputs (targeted accounts, reasons, successes) and the section 16 timeline to the case before anything is reset.</Action>
      <Action>Deploy changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate credentials for every account that succeeded from the source, and review whether the sprayed password matches a weak/expired policy that other accounts share.</Action>
      <Action>Hunt the succeeding account's subsequent activity across the estate (its logons, share access, process creation on hosts it reached) to find and remove any foothold, staged tooling, or persistence it established after the success.</Action>
      <Action>For a misconfiguration: engage the account/service owner to correct the stale stored credential / service configuration that produced the failure loop.</Action>
      <Action>Block the source infrastructure and any related IPs at the perimeter; feed the source to threat intel if external.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Return reset accounts to service with a strong, unique password (and MFA where available) once their subsequent activity is confirmed clean.</Action>
      <Action>Restore any isolated host after validating no persistence/tooling remains from post-access.</Action>
      <Action>Verify account-lockout / smart-lockout and password policy are effective against spraying, and revalidate the rule's excluded auth-aggregator subnets (10.11.15/101/102.0/24) against current infrastructure so the exclusions neither blind the rule nor mis-scope legitimate egress.</Action>
      <Action>Close the alert only after section 22 criteria are met and monitoring confirms the source/behaviour does not recur.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response and the AD/identity team when any of the following hold:

- A victim account succeeded from $source_ip (section 15.6b/section 17.5) - this alone warrants IR and an immediate reset.
- Post-access is observed (section 17.1) - ADMIN$/C$ or onward authentication to hosts the source does not administer, especially domain controllers or privileged systems.
- The succeeding account is privileged/service or the source is external/unattributed.
- Success/failure cannot be reliably established (telemetry gap), or a success exists whose owning identity/authorisation is undeterminable - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>true_positive: a targeted victim credential succeeded from the source (and/or post-access occurred); the account(s) are reset, sessions/tickets revoked, post-access hunted and contained, and the incident documented.</Item>
    <Item>false_positive (blocked-malicious): a genuine spray (many distinct accounts, bad-password/no-such-user reasons) with the success check empty - 4625 failures, no 4624 success. Record verdict as *"malicious password-spray attempt observed, blocked/unsuccessful, no credential compromised"* - never "benign" - and take defensive action on the source.</Item>
    <Item>misconfiguration: a mixed legitimate host/service with failures concentrated on a fixed service-account set (a stale-credential/retry loop); the owner corrects the configuration and a scoped tuning exception is proposed once the fix is scheduled.</Item>
    <Item>needs_escalation: handed to Tier 3/IR + AD/identity with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Shape is the alert; success is the verdict. The threshold proves the fan-out shape only. Always run the success check (section 15.6b) before deciding - a spray with no 4624 success is a blocked-malicious false_positive; a single success flips it to true_positive.</Item>
    <Item>Absence of success is proof, absence of context is not. 4625 failures with no 4624 for a (account, source) authoritatively prove those attempts failed. But absence of post-access, source attribution, or host-local corroboration never proves the source benign.</Item>
    <Item>The source profile is the misconfiguration discriminator. A pure-failure source (section 15.6a) is an attacker; a mixed source (its own successes/Kerberos/processes/shares) is a working host whose stale credential loops - engage the owner, do not treat as a spray.</Item>
    <Item>LogonType is a string and TargetUserName is populated on logon events. Compare winlog.event_data.LogonType == "3"; key targeted-account queries on winlog.event_data.TargetUserName (unlike 4688, where that field is null).</Item>
    <Item>source.ip is a network-logon-only, sometimes-shared identifier. Present on ~71% (type 3/10) and null on local/type-2. The excluded aggregator subnets exist because one IP fronts many users - correlate IP + account + host, and re-verify the exclusion subnets against current infrastructure.</Item>
    <Item>The source has no on-host context here. Process/persistence/evasion validation requires resolving $source_ip to a host and pivoting that host's 4688 out of band - those events carry no source.ip.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) highest-cardinality spray-shaped sources sit in the excluded 10.11.15.0/24 (24/22 distinct accounts) - exclusions are load-bearing; (2) source.ip ~71% on network logons, null on type-2; (3) winlog.event_data.LogonType is a string, SubStatus/TargetUserName populated on 4625/4624; (4) 10.11.9.1 observed spraying/enumerating (169× SRLCL 0xc0000064 + single bad-password hits) on 2026-08-19.</Item>
  </Limitations>
</Playbook>