<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-rdp-file-suspicious-path" name="Remote Desktop File Opened from Suspicious Path [NBI-4688] - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="medium">medium</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Initial Access</Category>
    <MitreAttack>
      <Technique id="T1566"/>
      <Technique id="T1566.002"/>
      <Technique id="T1021"/>
      <Technique id="T1021.001"/>
      <Tactic id="TA0001"/>
      <Tactic id="TA0011"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Phishing: Spearphishing Link (T1566.002)" url="https://attack.mitre.org/techniques/T1566/002/"/>
      <Reference title="MITRE ATT&amp;CK - Remote Services: Remote Desktop Protocol (T1021.001)" url="https://attack.mitre.org/techniques/T1021/001/"/>
      <Reference title="MITRE ATT&amp;CK - Initial Access tactic (TA0001)" url="https://attack.mitre.org/tactics/TA0001/"/>
      <Reference title="MITRE ATT&amp;CK - Command and Control tactic (TA0011)" url="https://attack.mitre.org/tactics/TA0011/"/>
      <Reference title="Microsoft Learn - Supported RDP file (.rdp) settings (full address, drivestoredirect, redirectclipboard)" url="https://learn.microsoft.com/en-us/windows-server/remote/remote-desktop-services/clients/rdp-files"/>
      <Reference title='CISA - AA24-317A / APT29 "rogue RDP" spearphishing campaign advisory' url="https://www.cisa.gov/news-events/alerts"/>
      <Reference title="Microsoft Threat Intelligence - Midnight Blizzard rogue RDP (.rdp attachment) campaign" url="https://www.microsoft.com/en-us/security/blog/2024/10/29/midnight-blizzard-conducts-large-scale-spear-phishing-campaign-using-rdp-files/"/>
      <Reference title="Microsoft Learn - Command line process auditing (Event 4688 command-line GPO)" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/command-line-process-auditing"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Remote Desktop File Opened from Suspicious Path [NBI-4688]</RuleName>
    <RuleId>nbi-b2-remote-desktop-file-opened-from-suspicious-path</RuleId>
    <RuleType>eql</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is an EQL rule. Reconstructed from the deployed trigger logic, the detection is:



Plain English: a process start where the image is mstsc.exe and a process argument references a .rdp file under a download or archive-extraction or mail-attachment path. A .rdp file is a plain-text configuration that can point the client at an attacker-controlled RDP server and silently redirect local drives, clipboard, and authentication to it. Delivering that file through phishing and getting the victim to double-click it is the rogue-RDP initial-access pattern.

One-line Kibana KQL detection filter (for fast pivoting in Discover / Timeline):



The .rdp *contents* (the target server = "full address", and the drive/clipboard redirection flags) are not in the 4688 event - the event proves the client opened a file from a suspicious path; the target and redirection settings must be recovered from the file itself or from network/RDP logs.</TriggerLogic>
    <KeyAlertFields>process.name, process.executable, mstsc.exe, process.parent.name, process.parent.executable, process.pid, process.parent.pid, process.entity_id, user.name, user.domain, process.command_line, process.args</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$source_ip"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Remote Desktop File Opened from Suspicious Path detection on NBI's Elastic Security deployment. The rule fires when mstsc.exe (the Windows RDP client) is started with an argument pointing at a .rdp file located under a delivery path - a user's Downloads folder, an AppData\Local\Temp archive-extraction folder (7z*, Rar$*, Temp?_*, BNZ.*), or the Outlook attachment cache (Content.Outlook). Opening a .rdp file that arrived by mail, web, or archive - rather than a curated internal shortcut - is the observable signature of the "rogue RDP" delivery technique.

The analyst's job is to decide whether this is a user legitimately opening a saved or shared internal connection file (false_positive - authorised), a malicious .rdp delivered by phishing that connects the victim outward to an attacker-controlled server (true_positive), a legitimate workflow that stages .rdp files in a flagged path (misconfiguration), or an event whose file and target cannot be resolved (needs_escalation). The .rdp origin, the launching parent, and the connection target are the discriminators.</Overview>
    <BusinessImpact>An alert means: on $host, $user launched mstsc.exe against a .rdp file sitting in a download, archive-extraction, or Outlook-attachment path. It does not, by itself, prove an outbound session to an attacker was established - it proves a .rdp of untrusted provenance was opened by the RDP client. The consequential questions:

1. How did the .rdp arrive? A Content.Outlook path means it came as a mail attachment; a Downloads path means web download; a 7z*/Rar$*/Temp?_* extraction folder means it was carried inside an archive (a common evasion to slip the attachment past filters).
2. What launched it? outlook.exe, a browser, or an archive tool (winrar, 7zFM) as the parent is the phishing-delivery shape; explorer.exe opening a file the user saved themselves is weaker but still warrants checking the target.
3. Where does the .rdp point? An external or unrecognised "full address", especially with drivestoredirect:s:* or redirectclipboard:i:1, is the rogue-RDP payoff - the victim's drives/clipboard/credentials are exposed to the attacker's server.

Because process.command_line is ~50% populated on NBI (section 8) and the .rdp path may live only in the multivalued process.args, both are fold</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Remote Desktop File Opened from Suspicious Path [NBI-4688].</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.os.type == "windows"
    AND TO_LOWER(process.name) == "mstsc.exe"
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*.rdp*" AND (cl LIKE "*downloads*" OR cl LIKE "*temp*" OR cl LIKE "*7z*" OR cl LIKE "*rar$*" OR cl LIKE "*bnz.*" OR cl LIKE "*content.outlook*")
| KEEP @timestamp, host.name, user.name, process.parent.name, process.command_line
| SORT @timestamp DESC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="2" name="Confirm on the alert host and read the file path - reused from the deployed playbook">
        <Purpose>Confirm on the alert host and read the file path - reused from the deployed playbook</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE event.code == "4688" AND host.name == "$host"
    AND TO_LOWER(process.name) == "mstsc.exe"
    AND @timestamp >= NOW() - 4 hours
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*.rdp*" AND (cl LIKE "*downloads*" OR cl LIKE "*temp*" OR cl LIKE "*7z*" OR cl LIKE "*rar$*" OR cl LIKE "*bnz.*" OR cl LIKE "*content.outlook*")
| KEEP @timestamp, host.name, user.name, process.parent.name, process.command_line, process.args
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
    AND TO_LOWER(process.name) == "mstsc.exe"
| KEEP @timestamp, host.name, user.name, user.domain, process.parent.name, process.command_line, process.args, process.pid, process.parent.pid
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="4" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND TO_LOWER(process.name) == "mstsc.exe"
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*.rdp*"
| STATS execs = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.parent.name
| SORT execs DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE event.code == "4688" AND host.name == "$host" AND user.name == "$user"
    AND TO_LOWER(process.name) == "mstsc.exe"
    AND @timestamp >= NOW() - 4 hours
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| EVAL launch_type = CASE(cl LIKE "*.rdp*", "rdp-file", cl LIKE "*/v:*", "explicit-target", "interactive-or-unknown")
| STATS execs = COUNT(*) BY launch_type, process.parent.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="6" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE event.code == "4688" AND host.name == "$host" AND user.name == "$user"
    AND TO_LOWER(process.name) == "mstsc.exe"
    AND @timestamp >= NOW() - 4 hours
| EVAL cl = TO_LOWER(CONCAT(COALESCE(process.command_line, ""), " ", COALESCE(MV_CONCAT(process.args, " "), "")))
| WHERE cl LIKE "*.rdp*"
| EVAL origin = CASE(
    cl LIKE "*content.outlook*", "outlook-attachment",
    cl LIKE "*downloads*", "web-download",
    cl LIKE "*7z*" OR cl LIKE "*rar$*" OR cl LIKE "*bnz.*" OR cl LIKE "*temp?_*", "archive-extract",
    cl LIKE "*temp*", "temp",
    "other")
| STATS execs = COUNT(*) BY origin, process.parent.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.parent.name) IN ("outlook.exe", "msedge.exe", "chrome.exe", "firefox.exe", "winrar.exe", "7zfm.exe", "explorer.exe")
| STATS execs = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.parent.name, process.name
| SORT execs DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="9" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="10" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host" AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY user.name, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND TO_LOWER(process.name) == "mstsc.exe"
    AND process.args IS NOT NULL
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, user.name, process.parent.name, arguments
| SORT @timestamp DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.command_line
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND user.name == "$user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "wscript.exe", "cscript.exe", "rundll32.exe", "mshta.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe", "sdelete.exe", "sdelete64.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND user.name == "$user"
    AND (
        event.code IN ("5140", "5145")
        OR (event.code == "4688" AND NOT TO_LOWER(process.executable) LIKE "*windows*" AND NOT TO_LOWER(process.executable) LIKE "*program files*")
    )
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, host.name, process.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>User-saved internal connection files. A user who saved a .rdp for an internal server into Downloads and re-opens it will match the path filter without any phishing involved.</Item>
        <Item>Legitimately mailed internal .rdp files. IT or a colleague emailing a connection file that lands in Content.Outlook and is opened directly.</Item>
        <Item>Support/automation tools that stage .rdp in Temp. A management or remote-support workflow that writes a .rdp into a Temp path before launching it.</Item>
        <Item>Archive-delivered internal .rdp. A .rdp legitimately shipped inside a .zip/.7z/.rar and extracted to a Temp folder before opening.</Item>
        <Item>**mstsc.exe is normal on the jump/VDI tier, but almost always the *interactive* variant. On nim-jump-apv02/-apv03/-apv22, named users launch mstsc.exe via explorer.exe or RuntimeBroker.exe, typically with /v:&lt;internal-host-or-IP&gt; to reach internal servers - routine admin work. The .rdp-file-from-suspicious-path variant is the exception this rule isolates, and it did not** appear in the validated 4-hour window.</Item>
        <Item>The jump host is exactly where a mailed .rdp would be opened. Because these hosts carry interactive sessions and mail/browser clients, a Content.Outlook/Downloads .rdp opened here is plausible and high-priority; contrast it against the user's normal /v: interactive usage (section 15.3).</Item>
        <Item>Command-line capture is bimodal (section 8). Where the jump host lacks the command-line GPO, the .rdp path may be present only in process.args (folded in below) - or absent entirely; lean on the parent and a preceding mail/browser/archive event.</Item>
        <Item>No standing allow-list. There is no recorded NBI benign-true-positive baseline for this rule. Do not create a blanket exception off one alert; scope any exception to an exact internal target + path + account after the target is documented.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate $host (network-contain / quarantine) if a true_positive is confirmed, to sever the outbound RDP session and stop redirected-drive access. On a shared jump/VDI host, coordinate with IT but prioritise containment.</Action>
      <Action>Block the .rdp target (the full address from the file) at the firewall/proxy - outbound RDP (TCP 3389) to that destination - to stop re-connection.</Action>
      <Action>Suspend or force-logoff $user's session on $host and disable the account if the user context is implicated; reset its credentials (section 20), since the rogue session can harvest them.</Action>
      <Action>Preserve the .rdp file and volatile evidence first (running process list, the file from the delivery path) before remediation.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Quarantine and remove the .rdp file from the delivery path and from any other hosts/mailboxes it reached (section 15.11); pull the delivering email at the gateway.</Action>
      <Action>Remove any persistence or payload delivered over the RDP session (section 17.2) and any RemoteApp component the .rdp auto-launched.</Action>
      <Action>Run a full anti-malware / EDR scan on $host and hunt for the same .rdp (by target address / hash) across the estate, especially other jump hosts and any recipient of the same lure.</Action>
      <Action>Remediate the delivery vector - strip/quarantine .rdp attachments at the mail gateway and block the download source.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Reset $user's password and any credentials that were reachable during the rogue session; if privileged accounts were exposed (section 17.3), rotate those too.</Action>
      <Action>Restore $host from a known-good image if a payload executed or persistence was installed; otherwise validate eradication holds after reboot.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms no outbound RDP to the target recurs.</Action>
      <Action>Recommend hardening (section 23): block outbound RDP to untrusted destinations, disable .rdp drive/clipboard redirection by policy, strip .rdp attachments at the mail gateway, and enable command-line auditing on the jump/workstation tier.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- The .rdp full address is an external/unrecognised server, especially with drive/clipboard redirection enabled.
- The .rdp arrived by mail (Content.Outlook) or web/archive and the delivering email/download is found (section 15.11, section 16).
- A privileged jump-host account is the actor (section 17.3), or lateral movement / share access follows (section 17.1, section 17.5).
- A payload or persistence was delivered over the session (section 17.2), or defence-tampering appears (section 17.4).
- The .rdp file/target cannot be recovered and the alert cannot be safely cleared - escalate as needs_escalation with the gap named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): the .rdp target is a confirmed internal server and the file is a user-saved/shared legitimate connection consistent with the account's role - target and owner documented; scope any exception to the exact internal target + path + account.</Item>
    <Item>false_positive (proven-blocked): the outbound connection to an external target was positively proven to fail/be blocked (no session established) - recorded as a blocked rogue attempt, never "benign"; preserve the .rdp and hunt the delivery.</Item>
    <Item>misconfiguration: a legitimate tool stages .rdp files in a flagged path with a confirmed internal target; relocate the distribution and baseline the workflow.</Item>
    <Item>true_positive: a phishing-delivered .rdp connected (or attempted to connect) $user to an attacker-controlled server; containment/eradication/recovery completed, delivery scoped, no residual sessions.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the .rdp/target and delivery gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The .rdp file decides it, and it is not in telemetry. 4688 proves mstsc opened a file from a suspicious path; the target (full address) and redirection flags live in the .rdp file - retrieve it host-side. An external target with drivestoredirect:s:*/redirectclipboard:i:1 is the rogue-RDP payoff.</Item>
    <Item>Outbound RDP is invisible on NBI. There is no process-to-network telemetry (no Sysmon), so the session to the attacker cannot be seen in logs-system.security*. Corroborate egress in logs-fortinet_fortigate.log-* (dst 3389) by the host IP; on-cluster, source.ip shows only the operator's *inbound* origin, not the .rdp's outbound destination.</Item>
    <Item>Command-line capture is bimodal. The .rdp path may be in process.args only, or absent, on the jump/VDI tier - fold command_line + MV_CONCAT(process.args," ") and lean on the parent (outlook/browser/archive-tool) when the path is null.</Item>
    <Item>Origin buckets the case fast. Content.Outlook = mail; Downloads = web; 7z*/Rar$*/Temp?_*/BNZ.* = archive-delivered (an evasion). Correlate the timing with a preceding mail/browser/archive event for $user (section 16).</Item>
    <Item>source.ip is shared infrastructure. A single VDI/jump egress IP (validated example: 10.11.102.15) fronts many users; correlate IP + user + host, never treat it as an individual identifier.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) mstsc.exe on the jump tier is normally interactive /v: to internal servers via explorer/RuntimeBroker; no suspicious-path .rdp in-window over 4h; (2) command-line/process.args host-bimodality on the jump tier; (3) no process-to-network telemetry (rogue-RDP outbound target not observable on-cluster); (4) 10.11.102.15 = shared VDI/admin egress fronting nim-jump-apv02. Observed live 2026-08-19.</Item>
  </Limitations>
</Playbook>