<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-service-account-interactive-logon" name="Defense Evasion / Lateral Movement — Service Account Interactive Logon (Type 2/10) - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="High">High</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Defense Evasion</Category>
    <MitreAttack>
      <Technique id="T1078"/>
      <Technique id="T1078.002"/>
      <Technique id="T1021"/>
      <Technique id="T1021.001"/>
      <Tactic id="TA0005"/>
      <Tactic id="TA0008"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - Valid Accounts: Domain Accounts (T1078.002)" url="https://attack.mitre.org/techniques/T1078/002/"/>
      <Reference title="MITRE ATT&amp;CK - Remote Services: Remote Desktop Protocol (T1021.001)" url="https://attack.mitre.org/techniques/T1021/001/"/>
      <Reference title="MITRE ATT&amp;CK - Defense Evasion tactic (TA0005)" url="https://attack.mitre.org/tactics/TA0005/"/>
      <Reference title="MITRE ATT&amp;CK - Lateral Movement tactic (TA0008)" url="https://attack.mitre.org/tactics/TA0008/"/>
      <Reference title="Microsoft Learn - Audit logon events / Logon types (Event 4624)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/auditing/event-4624"/>
      <Reference title="Microsoft Learn - Group Managed Service Accounts (gMSA) overview" url="https://learn.microsoft.com/en-us/windows-server/security/group-managed-service-accounts/group-managed-service-accounts-overview"/>
      <Reference title="Microsoft Learn - Deny log on locally / through Remote Desktop Services (user rights)" url="https://learn.microsoft.com/en-us/windows/security/threat-protection/security-policy-settings/deny-log-on-locally"/>
      <Reference title="MITRE ATT&amp;CK - Remote Services (T1021)" url="https://attack.mitre.org/techniques/T1021/"/>
      <Reference title="CISA - Identity and Access Management best practices (service-account hardening)" url="https://www.cisa.gov/resources-tools/resources/identity-and-access-management"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Defense Evasion / Lateral Movement — Service Account Interactive Logon (Type 2/10)</RuleName>
    <RuleId>nbi-svc-account-interactive-logon</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security-*</Index>
    <TriggerLogic>The deployed rule is a query (KQL) rule over Windows Security 4624. Its logic is:



Plain English: a successful interactive or RDP logon whose target account name matches the service-account naming convention. A true service account should appear almost exclusively as type 3 (network) and occasionally type 5 (service) / 4 (batch) - never as type 2 or 10. So any hit is, by construction, an identity being used against its design.

Two logon types, two risk profiles:
- Type 10 (RemoteInteractive / RDP) carries source.ip - the origin host is the key pivot, and RDP with a service credential from an unexpected origin is the higher-risk case.
- Type 2 (interactive / console) has no source.ip - someone was at, or session-hosted on, the machine; origin attribution then depends on host-side session/process activity.

The target host's role matters as much as the logon type: an interactive service-account logon onto a domain controller, jump host, or database server is far more serious than onto the account's own application host.</TriggerLogic>
    <KeyAlertFields>user.name, host.name, source.ip</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$target_user"/>
    <Input name="$source_ip"/>
    <Input name="$host"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Service Account Interactive Logon detection on NBI's Elastic Security deployment. The rule fires on a successful logon (Event 4624) with LogonType 2 (interactive/console) or 10 (RemoteInteractive/RDP) where the TargetUserName matches NBI's service-account naming (e.g. *.prod, *.servacc, ICBS*, CITRIX*, SIGCAP*, Veeam*, Solarwinds*, and similar). Service accounts are designed for non-interactive use - network (type 3), service (type 5), batch (type 4). An interactive or RDP logon carrying a service account's credential means a human is signing in as that machine identity.

The analyst's job is to decide whether this is a sanctioned break-glass/administrative use of the account (false_positive - authorised, though a hygiene problem), interactive misuse or attacker lateral movement (true_positive), or a stale configuration where a human-used account was miscategorised as a service account (misconfiguration) - and to classify as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached. The discriminators are the account's normal logon-type profile (does it *ever* log on interactively?), the origin of the logon, and what the account did after signing in.</Overview>
    <BusinessImpact>Service accounts exist to run software unattended. They are typically over-privileged, exempt from MFA, and excluded from interactive-logon and password-rotation policy because "nobody logs in as them." That is exactly why an interactive logon with one is dangerous: it puts a human (operator or attacker) behind a trusted automation identity that identity controls do not scrutinise.

An alert therefore means: on $host, the service account $target_user completed an interactive or RDP logon. Two innocent-to-malicious readings compete:
- An operator reusing a shared service credential for hands-on work (poor hygiene - real risk, but not an intrusion), or
- An attacker who harvested the service account's password (from a config file, a memory dump, a Kerberoast, or a prior foothold) and is now using it hands-on to move through the estate while blending into an "expected" identity.

The investigation resolves which, using the account's baseline logon profile, the logon origin, and post-logon behaviour.</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Defense Evasion / Lateral Movement — Service Account Interactive Logon (Type 2/10).</Goal>
    <Workflow>
      <Step order="1" name="Confirm the interactive logon and where it landed">
        <Purpose>Confirm the interactive logon and where it landed</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4624" AND winlog.event_data.TargetUserName == "$target_user"
    AND (winlog.event_data.LogonType == "2" OR winlog.event_data.LogonType == "10")
    AND @timestamp >= NOW() - 4 hours
| STATS logons = COUNT(*), srcs = VALUES(source.ip), last_seen = MAX(@timestamp)
    BY host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Baseline the account's normal logon-type profile">
        <Purpose>Baseline the account's normal logon-type profile</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4624" AND winlog.event_data.TargetUserName == "$target_user"
    AND @timestamp >= NOW() - 4 hours
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Characterise the RDP origin">
        <Purpose>Characterise the RDP origin</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security-*
| WHERE event.code == "4624" AND source.ip == "$source_ip"
    AND (winlog.event_data.LogonType == "2" OR winlog.event_data.LogonType == "10")
    AND @timestamp >= NOW() - 4 hours
| STATS logons = COUNT(*), accounts = VALUES(winlog.event_data.TargetUserName) BY host.name
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="4" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
| STATS logons = COUNT(*), sources = COUNT_DISTINCT(source.ip), last_seen = MAX(@timestamp) BY host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND user.name == "$target_user"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY process.name, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host" AND user.name == "$target_user"
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, process.name, process.parent.name, process.executable, arguments
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host" AND user.name == "$target_user"
| STATS executions = COUNT(*) BY process.parent.name, process.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="8" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
| STATS logons = COUNT(*), types = VALUES(winlog.event_data.LogonType) BY host.name
| SORT logons DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="9" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="10" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND winlog.event_data.TargetUserName == "$target_user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*), hosts = COUNT_DISTINCT(host.name) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY winlog.event_data.TargetUserName, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="12" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host" AND user.name == "$target_user"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="13" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647", "4648", "4768", "4769")
    AND host.name == "$host" AND winlog.event_data.TargetUserName == "$target_user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647", "4648", "4768", "4769")
    AND host.name == "$host" AND winlog.event_data.TargetUserName == "$target_user"
| KEEP @timestamp, event.code, winlog.event_data.LogonType, source.ip, host.name
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="15" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND winlog.event_data.TargetUserName == "$target_user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="16" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND user.name == "$target_user" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "net.exe", "net1.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="17" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="18" name="Defense evasion validation">
        <Purpose>Defense evasion validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("wevtutil.exe", "fsutil.exe", "vssadmin.exe", "wmic.exe", "cipher.exe", "sdelete.exe", "sdelete64.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="19" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-system.security-*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND event.code == "4688"
    AND user.name == "$target_user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name), last_seen = MAX(@timestamp) BY process.name
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="20" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Authorised break-glass / maintenance - an entitled operator deliberately using the service account interactively (e.g. to debug the service on its host, or via a sanctioned jump host). This is *authorised*, not "benign", and it is a hygiene finding: confirm the operator and change record, then raise the shared-credential gap.</Item>
        <Item>Mislabelled human/shared accounts - an account that *matches* the service-account naming pattern but is really used by people and routinely logs on interactively. That is a classification error (misconfiguration), revealed by the baseline (section 14.2 / section 15.12).</Item>
        <Item>Failed hostile attempts - an interactive logon *attempt* that was positively proven to have failed (a paired 4625 with no successful 4624). Record as a blocked attempt, never "benign".</Item>
        <Item>Automated tooling that shells to a console - rare setups where management software instantiates a console session under the service identity; verify against the tool's expected behaviour, do not assume.</Item>
        <Item>NBI's real service accounts are overwhelmingly type-3. CITRIX.NBI logged on 64,905 times in 4 hours across 24 hosts, 100% type 3 (network), 0 interactive; Solarwinds.Srv (12,837), Veeam.Backup, sigcap.prod, and FilenetTds.prod show the same pure-network profile. There is a clean, high-contrast baseline to test any interactive hit against - the anomaly is unambiguous when it appears.</Item>
        <Item>The likely "authorised" origin is the shared jump/VDI tier. source.ip = 10.11.102.15 is a shared Citrix/VDI egress that fronts ~17 distinct named admin identities across many hosts (Wahab.Admin, karrar.admin, jamal.admin, and others). If an interactive service-account logon is a sanctioned admin action, this is the kind of origin it will come from - but a shared egress IP is weak attribution: correlate IP + account + target host + operator, never treat the IP as an automatic pass.</Item>
        <Item>Service accounts here are not locally privileged on the DC/DB host by default. On nim-dc-dbap01, Event 4672 (special privileges) is held by the machine account, SYSTEM, and named admins (Wahab.Admin, karrar.admin, jamal.admin) - CITRIX.NBI is absent. So a service account that suddenly *does* appear in 4672 after an interactive logon is a strong escalation signal (section 17.3).</Item>
        <Item>No historical NBI benign-true-positive is on record for this rule. There is no environment-specific allow-list. Scope any exception to the exact account + origin + target host + entitled operator, and only after a documented authorised cause.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>Isolate the target $host and the origin (for type 10, the $source_ip host) if a true_positive is confirmed, to stop onward movement under the credential. Prioritise DC/DB/PAM/SSO targets.</Action>
      <Action>Reset the service-account credential and rotate any secret it protects - but coordinate with the application owner first: a blind reset of a live service account (e.g. CITRIX.NBI, which brokers 24 hosts) will cause an outage. Plan the rotation with the owner so the service is re-secured without dropping production.</Action>
      <Action>Kill the interactive session on $host and, where the account should never be interactive, apply a Deny log on locally / Deny log on through Remote Desktop Services restriction to it (GPO) to prevent recurrence during the investigation.</Action>
      <Action>Preserve evidence first: the 4624/4625 records, the origin, and any post-logon 4688/5140 activity under the account.</Action>
      <Action>Deploy/confirm changes only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Rotate the service credential (and the KDS/gMSA secret if applicable) with the app owner, and rotate any downstream secrets the account could read (config files, connection strings, vault entries it had access to).</Action>
      <Action>Remove any persistence the operator installed on $host or its onward hosts (section 17.2): services, scheduled tasks, Run keys, rogue accounts.</Action>
      <Action>Hunt the account's reach: every host it authenticated to in the window (section 15.4, section 17.1), for post-logon actions and further credential use - especially DCs, DB, PAM/SSO, and backup servers.</Action>
      <Action>Close the acquisition path: if the credential was Kerberoasted, harvested from a config/dump, or exposed on the jump tier, remediate that source so a rotation is not immediately re-compromised.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Return the account to non-interactive-only operation: enforce Deny-interactive/RDP logon rights via GPO, and migrate to a group Managed Service Account (gMSA) so the secret is machine-managed and cannot be used for a human logon.</Action>
      <Action>Eliminate shared human use of service credentials - provide named admin accounts with proper entitlements and PAM-brokered access instead.</Action>
      <Action>Restore any affected host from known-good if persistence/tampering was extensive; otherwise validate eradication holds after reboot.</Action>
      <Action>Return the account/host to service only after section 22 closing criteria are met and monitoring confirms no further interactive logons for the identity.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response and the AD/identity team (and notify the customer) when any of the following hold:

- The interactive service-account logon landed on a Tier-0 / domain controller / DB / PAM / SSO / backup host, or came RDP from an unexpected origin (not the sanctioned jump tier).
- The account newly appears in 4672 on the host (section 17.3), or post-logon privileged activity, credential access, or share enumeration is visible (section 17.2, section 17.5).
- Onward lateral movement from the account to new hosts is observed (section 17.1), especially toward domain controllers.
- Log clearing or audit-policy tampering appears (section 17.4).
- Evidence is incomplete (type-2 logon, null source, no host-side activity) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.

Involve the application owner before rotating the credential,</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): an entitled operator + a change/break-glass record + a sanctioned origin are positively matched to the interactive logon. Close as authorised and raise the shared-service-credential hygiene finding. Do not create a broad exception; if one is warranted, scope it to the exact account + origin + target host + operator.</Item>
    <Item>false_positive (blocked attempt): the interactive logon *attempt* is positively proven to have failed (paired 4625, no successful 4624) - documented as blocked, never "benign"; the origin is still investigated.</Item>
    <Item>misconfiguration: the baseline (section 14.2 / section 15.12) shows the account routinely logs on interactively - it is a mislabelled human/shared identity; correct its classification/naming and convert it to a proper user or a genuinely non-interactive service account.</Item>
    <Item>true_positive: interactive credential misuse confirmed; target and origin contained, credential reset/rotated with the app owner, post-logon actions and account reach hunted, and no recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>The baseline is the whole game. NBI's real service accounts are clean, high-volume type-3 identities (CITRIX.NBI = 64,905 network logons / 24 hosts / 0 interactive in 4h; Solarwinds.Srv, Veeam.Backup, sigcap.prod the same). Against that wall of network traffic, a single type-2/10 event is unmistakable - run section 14.2 first and let the profile decide service-misuse vs mislabelled-human.</Item>
    <Item>winlog.event_data.LogonType is a string on NBI. Compare with == "2" / == "10", not integers. TargetUserName (vendor-native) is the identity anchor, not user.name - the rule and every auth pivot key on it.</Item>
    <Item>Type-2 has no origin. Interactive (console) logons carry no source.ip; for those, origin attribution comes from host-side process activity (section 15.2, section 17.5), not this event. Type-10 (RDP) is where $source_ip is your pivot.</Item>
    <Item>source.ip is shared infrastructure. 10.11.102.15 fronts ~17 named admin identities across hosts; 10.11.18.21 is the Citrix broker egress for CITRIX.NBI. Never treat a shared egress IP as an individual identifier or an automatic authorisation - correlate IP + account + host + operator.</Item>
    <Item>The account may not be locally privileged. CITRIX.NBI is absent from 4672 on nim-dc-dbap01; a service account that *gains* special privileges after an interactive logon is escalating (section 17.3).</Item>
    <Item>Evasion to expect: a careful attacker holding the credential stays on non-interactive types (type 3 network / type 5 service) to move laterally and never trips this rule, or RDPs through the sanctioned jump host to blend into an expected origin. Complement with a service-account behaviour analytic on type-3 fan-out to novel hosts and on new source IPs for the same identity (section 24).</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) service-account roster with pure-type-3 baselines - CITRIX.NBI, Solarwinds.Srv, Veeam.Backup, sigcap.prod, FilenetTds.prod; (2) 10.11.102.15 = shared VDI/jump egress fronting ~17 admins; (3) 10.11.18.21 = Citrix broker egress for CITRIX.NBI; (4) CITRIX.NBI not in 4672 on nim-dc-dbap01; (5) winlog.event_data.LogonType is a string. Observed live 2026-08-19.</Item>
  </Limitations>
</Playbook>