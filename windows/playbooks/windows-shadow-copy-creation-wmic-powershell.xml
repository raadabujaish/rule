<?xml version="1.0" encoding="UTF-8"?>
<Playbook xmlns="https://soc.example/playbooks" id="PB-windows-shadow-copy-creation-wmic-powershell" name="Creation of Shadow Copy with wmic and powershell - SOC Playbook" version="3.0" status="Validated" classification="TLP:AMBER">
  <Metadata>
    <Owner>NBI SOC / Detection Engineering</Owner>
    <LastUpdated>2026-08-19</LastUpdated>
    <Severity default="high">high</Severity>
    <Confidence default="Medium">Medium</Confidence>
    <Category>Credential Access</Category>
    <MitreAttack>
      <Technique id="T1003.003"/>
      <Technique id="T1003"/>
      <Technique id="T1047"/>
      <Technique id="T1490"/>
      <Tactic id="TA0006"/>
      <Tactic id="TA0040"/>
    </MitreAttack>
    <References>
      <Reference title="MITRE ATT&amp;CK - OS Credential Dumping: NTDS (T1003.003)" url="https://attack.mitre.org/techniques/T1003/003/"/>
      <Reference title="MITRE ATT&amp;CK - Windows Management Instrumentation (T1047)" url="https://attack.mitre.org/techniques/T1047/"/>
      <Reference title="MITRE ATT&amp;CK - Inhibit System Recovery (T1490)" url="https://attack.mitre.org/techniques/T1490/"/>
      <Reference title="MITRE ATT&amp;CK - Credential Access tactic (TA0006)" url="https://attack.mitre.org/tactics/TA0006/"/>
      <Reference title="MITRE ATT&amp;CK - Impact tactic (TA0040)" url="https://attack.mitre.org/tactics/TA0040/"/>
      <Reference title="Elastic - Security detection rules (prebuilt rules reference)" url="https://www.elastic.co/docs/reference/security/prebuilt-rules"/>
      <Reference title="Elastic - detection-rules repository (source for shadow-copy analytics)" url="https://github.com/elastic/detection-rules"/>
      <Reference title="LOLBAS - Wmic.exe" url="https://lolbas-project.github.io/lolbas/Binaries/Wmic/"/>
      <Reference title="ADSecurity (Sean Metcalf) - How Attackers Dump Active Directory Database Credentials (ntds.dit via shadow copy)" url="https://adsecurity.org/?p=2398"/>
      <Reference title="Microsoft Learn - Volume Shadow Copy Service overview" url="https://learn.microsoft.com/en-us/windows-server/storage/file-server/volume-shadow-copy-service"/>
      <Reference title="Microsoft Learn - Command line process auditing (Event 4688 command-line GPO)" url="https://learn.microsoft.com/en-us/windows-server/identity/ad-ds/manage/component-updates/command-line-process-auditing"/>
    </References>
  </Metadata>
  <AlertContext>
    <RuleName>Creation of Shadow Copy with wmic and powershell</RuleName>
    <RuleId>837af20c-0e76-410e-a024-bed14819f2af</RuleId>
    <RuleType>query</RuleType>
    <Index>logs-system.security*</Index>
    <TriggerLogic>The deployed rule is an Elastic query rule (KQL). Its behavioural core, reconstructed from the rule's trigger logic, is a process-creation match on the shadow-copy verb:



Plain English: any process creation (Event 4688, event.type == "start") on a Windows host where the image is wmic.exe or powershell.exe and the command line contains the string shadowcopy. That string is what distinguishes the *creation* API surface - wmic shadowcopy call create, Get-WmiObject/Get-CimInstance Win32_ShadowCopy, ([WMICLASS]"Win32_ShadowCopy").Create(...) - from ordinary interpreter use. The rule does not attempt to read the verb (create vs delete) itself; that judgement is left to this playbook (section 14).

One-line Kibana-KQL detection filter (for fast pivoting in Discover / Timeline):



Two caveats shape everything downstream:

- process.command_line is only ~50% populated on NBI's 4688 stream (host-dependent GPO - see section 8). The rule can therefore only fire where the command line is actually captured; on command-line-less hosts an identical action is invisible to *this* rule. Where the line is present, read the verb carefully.
- The rule watches only two images. An attacker who creates</TriggerLogic>
    <KeyAlertFields>process.name, process.executable, process.parent.name, process.parent.pid, process.entity_id, process.pid, user.name, user.domain, host.name, host.os.type, process.command_line, process.args</KeyAlertFields>
  </AlertContext>
  <Inputs>
    <Input name="$host"/>
    <Input name="$user"/>
    <Input name="$process"/>
    <Input name="$source_ip"/>
    <Input name="$pid"/>
  </Inputs>
  <ExecutiveSummary>
    <Overview>This playbook drives triage and investigation of the Creation of Shadow Copy with wmic and powershell detection on NBI's Elastic Security deployment. The rule fires when wmic.exe or powershell.exe runs with a command line containing shadowcopy - i.e. a Volume Shadow Copy is being created *programmatically and on demand* (for example wmic shadowcopy call create, or instantiating the Win32_ShadowCopy WMI class from PowerShell) rather than through a scheduled, sanctioned backup product.

On-demand shadow-copy creation is a deliberately dual-use action, and the whole investigation turns on separating the two faces:

- On a Domain Controller it is the classic route to steal NTDS.dit: a shadow copy exposes the otherwise-locked Active Directory database (and the SYSTEM registry hive that holds the boot key) so both can be copied out and every domain credential cracked or replayed offline - a single action that can compromise the entire domain.
- On a member server or workstation the same command can be benign backup/maintenance, or it can be a pre-ransomware manipulation of the shadow-copy store (creation, then deletion/resize to inhibit recovery).

The analyst's goal is to determine - quickly and defensibly - whether an on-demand shadow copy on $host is authorised backup/maintenance, hands-on credential theft, recovery-inhibition staging, or a management-tooling misconfiguration, and to classify the alert as true_positive, false_positive, misconfiguration, or needs_escalation with evidence attached. The dominant discriminators, in order, are the role of the host (a DC makes this</Overview>
    <BusinessImpact>A Volume Shadow Copy is a point-in-time, read-only snapshot of a volume. It exists precisely so that locked, in-use files can be read - which is exactly why it is abused: on a running Domain Controller the AD database C:\Windows\NTDS\ntds.dit is held open by the lsass/ntds service and cannot be copied directly, but it *can* be copied out of a fresh shadow. Creating a shadow on demand from wmic/PowerShell, rather than letting the backup product manage it, is the on-keyboard operator's way to get that readable copy.

An alert therefore means: on $host, an interactive or scripted wmic.exe/powershell.exe invocation asked Windows to create a shadow copy. That is the enabling step of NTDS theft on a DC, and a recovery-tampering precursor on servers. It is not by itself proof of exfiltration - the shadow may have been created and never read - but it is the action that makes offline credential theft *possible*, so it is treated as high-consequence. The investigative questions are: what is $host's role, was this create or delete, who ran it, and did an ntds.dit/hive copy or a shadow deletion follow?</BusinessImpact>
  </ExecutiveSummary>
  <Investigation>
    <Goal>Decide true_positive / false_positive / misconfiguration / needs_escalation for Creation of Shadow Copy with wmic and powershell.</Goal>
    <Workflow>
      <Step order="1" name="Reproduce the rule estate-wide (confirm the detection logic)">
        <Purpose>Reproduce the rule estate-wide (confirm the detection logic)</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-01">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND process.name IN ("wmic.exe","powershell.exe","pwsh.exe")
    AND process.command_line LIKE "*shadowcopy*"
| KEEP @timestamp, host.name, user.name, process.name, process.command_line
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="2" name="Recover the shadow-copy command and account on the alert host">
        <Purpose>Recover the shadow-copy command and account on the alert host</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-02">
          <![CDATA[
FROM logs-system.security*
| WHERE event.code == "4688" AND host.name == "$host"
    AND process.name IN ("wmic.exe","powershell.exe","pwsh.exe")
    AND process.command_line LIKE "*shadowcopy*"
    AND @timestamp >= NOW() - 4 hours
| KEEP @timestamp, user.name, process.name, process.command_line
| SORT @timestamp DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="3" name="Shadow-copy and recovery-tampering context on the host">
        <Purpose>Shadow-copy and recovery-tampering context on the host</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-03">
          <![CDATA[
FROM logs-system.security*
| WHERE event.code == "4688" AND host.name == "$host"
    AND process.command_line LIKE "*shadow*"
    AND process.name IN ("vssadmin.exe","wmic.exe","powershell.exe","pwsh.exe","diskshadow.exe","wbadmin.exe","bcdedit.exe")
    AND @timestamp >= NOW() - 4 hours
| STATS execs = COUNT(*), cmds = VALUES(process.command_line) BY process.name, user.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="4" name="Entity pivoting">
        <Purpose>Entity pivoting</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-04">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.name IN ("wmic.exe","powershell.exe","pwsh.exe","vssadmin.exe","diskshadow.exe","wbadmin.exe")
| KEEP @timestamp, host.name, user.name, user.domain, process.name, process.executable, process.parent.name, process.parent.pid, process.pid, process.command_line
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="5" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-05">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND process.name == "$process"
| STATS executions = COUNT(*), hosts = COUNT_DISTINCT(host.name), users = COUNT_DISTINCT(user.name) BY process.executable, process.parent.name
| SORT executions DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="6" name="Process investigation">
        <Purpose>Process investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-06">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.name == "$process"
    AND process.args IS NOT NULL
| EVAL arguments = MV_CONCAT(process.args, " ")
| KEEP @timestamp, host.name, user.name, process.executable, process.parent.name, arguments
| SORT @timestamp DESC
| LIMIT 50
]]>
        </Query>
      </Step>
      <Step order="7" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-07">
          <![CDATA[
FROM logs-system.security*
| WHERE event.code == "4688" AND host.name == "$host" AND user.name == "$user"
    AND @timestamp >= NOW() - 4 hours
| STATS execs = COUNT(*), procs = VALUES(process.name) BY process.parent.name
| SORT execs DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="8" name="Parent-Child process analysis">
        <Purpose>Parent-Child process analysis</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-08">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.parent.pid == $pid
| KEEP @timestamp, user.name, process.parent.name, process.parent.pid, process.name, process.pid, process.executable
| SORT @timestamp ASC
| LIMIT 100
]]>
        </Query>
      </Step>
      <Step order="9" name="User investigation">
        <Purpose>User investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-09">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND user.name == "$user"
| STATS executions = COUNT(*), distinct_procs = COUNT_DISTINCT(process.name) BY host.name
| SORT executions DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="10" name="Host investigation">
        <Purpose>Host investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-10">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
| STATS executions = COUNT(*), users = COUNT_DISTINCT(user.name) BY process.name, process.parent.name
| SORT executions ASC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="11" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-11">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND host.name == "$host" AND user.name == "$user"
    AND source.ip IS NOT NULL
| STATS logons = COUNT(*) BY source.ip, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 20
]]>
        </Query>
      </Step>
      <Step order="12" name="IP investigation">
        <Purpose>IP investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-12">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4624"
    AND source.ip == "$source_ip"
| STATS logons = COUNT(*) BY user.name, host.name, winlog.event_data.LogonType
| SORT logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="13" name="File investigation">
        <Purpose>File investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-13">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND process.name == "$process"
| STATS executions = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY process.executable
| SORT executions DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="14" name="Authentication investigation">
        <Purpose>Authentication investigation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-14">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4625", "4634", "4647")
    AND host.name == "$host" AND user.name == "$user"
| STATS events = COUNT(*), first_seen = MIN(@timestamp), last_seen = MAX(@timestamp) BY event.code, winlog.event_data.LogonType, source.ip
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="15" name="Timeline Reconstruction">
        <Purpose>Timeline Reconstruction</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-15">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4688"
    AND host.name == "$host"
    AND user.name == "$user"
| KEEP @timestamp, process.parent.name, process.parent.pid, process.name, process.pid, process.executable, process.command_line
| SORT @timestamp ASC
| LIMIT 200
]]>
        </Query>
      </Step>
      <Step order="16" name="Lateral movement validation">
        <Purpose>Lateral movement validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-16">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code IN ("4624", "4648", "4768", "4769", "5140")
    AND user.name == "$user"
    AND host.name != "$host"
| STATS events = COUNT(*) BY host.name, event.code, winlog.event_data.LogonType
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="17" name="Persistence validation">
        <Purpose>Persistence validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-17">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("schtasks.exe", "reg.exe", "sc.exe", "at.exe", "powershell.exe", "wscript.exe", "cscript.exe", "rundll32.exe"))
        OR event.code == "7045"
        OR event.code == "4720"
        OR event.code == "4698"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 40
]]>
        </Query>
      </Step>
      <Step order="18" name="Privilege escalation validation">
        <Purpose>Privilege escalation validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-18">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND event.code == "4672"
    AND host.name == "$host"
| STATS special_priv_logons = COUNT(*) BY user.name
| SORT special_priv_logons DESC
| LIMIT 25
]]>
        </Query>
      </Step>
      <Step order="19" name="Defense evasion / recovery-inhibition validation">
        <Purpose>Defense evasion / recovery-inhibition validation</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-19">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND (
        (event.code == "4688" AND TO_LOWER(process.name) IN ("vssadmin.exe", "wmic.exe", "wbadmin.exe", "bcdedit.exe", "diskshadow.exe", "wevtutil.exe", "fsutil.exe"))
        OR event.code == "1102"
        OR event.code == "4719"
    )
| STATS events = COUNT(*) BY event.code, process.name, user.name
| SORT events DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="20" name="Impact assessment">
        <Purpose>Impact assessment</Purpose>
        <Query platform="Elastic" language="ES|QL" id="Q-20">
          <![CDATA[
FROM logs-system.security*
| WHERE @timestamp >= NOW() - 4 hours
    AND host.name == "$host"
    AND event.code == "4688"
    AND process.parent.pid == $pid
| STATS children = COUNT(*), distinct_children = COUNT_DISTINCT(process.name) BY process.name
| SORT children DESC
| LIMIT 30
]]>
        </Query>
      </Step>
      <Step order="21" name="Finalize verdict">
        <DecisionTree>
        </DecisionTree>
      </Step>
    </Workflow>
  </Investigation>
  <ClassificationGuidance>
    <TruePositive>
      <Definition></Definition>
    </TruePositive>
    <FalsePositive>
      <Definition></Definition>
      <Indicators>
        <Item>Sanctioned backup products create shadow copies constantly - but they do it through the VSS writer framework under a service identity, not by an interactive wmic shadowcopy call create or an ad-hoc PowerShell one-liner. A backup that legitimately shells to wmic/PowerShell is unusual and should be baselined, not assumed.</Item>
        <Item>Administrator maintenance / imaging / migration scripts that snapshot a volume before a change. Real, but should map to a change ticket and a recognised operator.</Item>
        <Item>Monitoring / systems-management agents that query Win32_ShadowCopy for *reporting* (enumeration) rather than creation - read the verb: an enumeration (Get/gwmi with no Create) is not the same as a Create call, though both can carry the shadowcopy string.</Item>
        <Item>Red-team / purple-team exercises deliberately performing NTDS-via-shadow. These are not benign - they are authorised malicious-technique execution and must be confirmed against a change ticket or exercise ROE before being classified as false_positive (blocked/authorised), never dismissed on sight.</Item>
        <Item>The rule's own trigger has a zero baseline. wmic.exe appeared 0 times across the entire estate in 4h, and no 4688 command line contained shadowcopy (0 hits). There is no noisy legitimate source to tune out - any firing is a strong anomaly.</Item>
        <Item>powershell.exe is live but its bulk is benign automation, not shadow copies. PowerShell ran ~570 times across 10 hosts in the window, overwhelmingly on nim-st-apv10 / nim-st-apv11 where a python.exe parent launches short -EncodedCommand -NoProfile snippets that read registry TimeZone values (a monitoring agent). None create shadow copies. Do not let routine encoded-PowerShell volume distract from a genuine shadowcopy command line.</Item>
        <Item>NBI has real Domain Controllers in scope - nim-dc-dbap01 and nim-dc2-dbap. On these hosts process.command_line *is* captured (validated 1/1 on the DC), so a real shadow-copy create on a DC would surface with a readable command. A shadow copy on either DC is credential theft until disproven and is paged, not triaged slowly.</Item>
        <Item>Command-line capture is bimodal (see section 8): ~100% on some servers (nim-st-apv10/11, and the DCs), 0% on jump/VDI and many app hosts. On a null-command-line host the rule cannot even fire, so absence of alerts there is a blind spot, not safety.</Item>
      </Indicators>
    </FalsePositive>
    <Misconfiguration>
      <Definition></Definition>
    </Misconfiguration>
    <NeedsEscalation>
      <Definition></Definition>
    </NeedsEscalation>
  </ClassificationGuidance>
  <ResponseActions>
    <Phase name="Containment">
      <Action>If $host is a Domain Controller and a shadow create is confirmed, treat the whole domain as credential-exposed and page IR immediately - do not wait for proof of the copy-out, because the copy is frequently un-audited on NBI (section 8). Coordinate DC isolation carefully with AD/IT (isolating a DC is disruptive), but prioritise stopping active exfiltration.</Action>
      <Action>On a server/workstation, network-contain / quarantine $host if a true_positive (theft or recovery inhibition) is confirmed.</Action>
      <Action>Suspend or force-logoff $user's session on $host and disable the account pending investigation if the context is implicated; reset its credentials (section 20).</Action>
      <Action>Terminate the flagged process and its descendants ($pid tree from section 15.3b / section 17.5) and any in-progress copy (esentutl/ntdsutil/archive) if the host cannot yet be isolated.</Action>
      <Action>Preserve volatile evidence first where feasible: the shadow-copy list (vssadmin list shadows), any staged ntds.dit/hive/archive files, the running process list, and memory of the flagged process - NBI does not collect the copy-out, so host-side capture is the only way to recover it.</Action>
      <Action>Perform any change only via the authorised human-approved DEPLOY path; investigation itself is read-only.</Action>
    </Phase>
    <Phase name="Eradication">
      <Action>Delete the illicit shadow copy created by the flagged action if it still exists, and remove any staged copies of ntds.dit, the SYSTEM/SECURITY hives, and archives identified via section 15.3b / section 17.5.</Action>
      <Action>Remove any persistence discovered in section 17.2 (services, scheduled tasks, Run keys, rogue accounts) and any dropped tooling identified via section 15.10 (process.executable path).</Action>
      <Action>Run a full anti-malware / EDR scan on $host and hunt for the same activity across peers - other DCs, backup servers, and any host $user touched (section 15.4, section 17.1).</Action>
      <Action>Remediate the initial-access / privilege-acquisition vector that gave the operator the administrative foothold from which they created the shadow.</Action>
      <Action>If the DC's ntds.dit was (or may have been) copied, proceed to the domain-compromise recovery in section 20 - eradication on the single host is not sufficient once the domain database is exposed.</Action>
    </Phase>
    <Phase name="Recovery">
      <Action>Reset $user's password and any credentials accessible from $host during the window; if privileged accounts were active there (section 17.3), rotate those too.</Action>
      <Action>If a DC shadow create with plausible NTDS exposure is confirmed, invoke the domain-compromise procedure: enterprise-wide privileged-credential resets, a krbtgt double-reset, review of Kerberos/NTLM secret exposure and Golden-Ticket risk, and heightened monitoring for forged-ticket use. Treat every domain hash as compromised until proven otherwise.</Action>
      <Action>Restore $host from a known-good image if persistence or tampering was extensive; otherwise validate that all eradication actions hold after reboot, and confirm the shadow store is clean.</Action>
      <Action>Return the host/account to service only after section 22 closing criteria are met and monitoring confirms no recurrence.</Action>
      <Action>Harden (recommend via section 23): restrict on-demand VSS creation on DCs to the sanctioned backup product, set a SACL on C:\Windows\NTDS so copy-out is audited (4663), enable the command-line process-auditing GPO on server/DC classes so the create-vs-delete verb is always captured, and alert on shadow-copy deletion as an independent ransomware early-warning.</Action>
    </Phase>
  </ResponseActions>
  <Escalation>
    <Trigger>Escalate to Tier 3 / Incident Response (and notify the customer) when any of the following hold:

- $host is a Domain Controller and a shadow copy was created on demand - this alone warrants IR as potential domain compromise.
- The create is followed by an ntds.dit / registry-hive copy (section 14.3, section 15.3b) or a shadow-copy deletion / recovery tampering (section 17.4).
- The actor context is hands-on (interactive session, recon, archive/exfil tooling) under a human/compromised account rather than a sanctioned backup identity (section 15.3a, section 17.3).
- Lateral movement from $host/$user is observed (section 17.1), especially toward or from other DCs or backup infrastructure.
- Evidence is incomplete because of NBI's telemetry gaps (null command line, un-audited copy-out) and the alert cannot be safely cleared - escalate as needs_escalation with the gaps named.</Trigger>
    <To>SOC L2 / IR</To>
  </Escalation>
  <ClosureCriteria>
    <Item>false_positive (authorised): a change ticket, recognised backup identity + schedule, or sanctioned red/purple-team ROE is positively matched to this exact $process + $user + $host + time on a non-DC, with no ntds.dit/hive copy and no shadow deletion. Record the reference. Scope any exception narrowly (exact host + account + command + schedule); never a blanket rule, and never on a DC.</Item>
    <Item>false_positive (blocked/authorised attempt): a hostile create positively proven to have failed or been denied with no shadow produced and no follow-on copy - documented as a blocked attempt, never "benign"; investigate the account.</Item>
    <Item>misconfiguration: a legitimate but unbaselined maintenance script used ad-hoc wmic/PowerShell VSS instead of the sanctioned product on a non-DC, with benign follow-on and no credential-copy or deletion; a baseline/hardening action is raised (and the DC exclusion enforced).</Item>
    <Item>true_positive: unauthorised shadow create for credential theft (NTDS/hive extraction, especially on a DC) or recovery inhibition; containment/eradication/recovery completed, scope of $user/$host/peers established, domain-wide reset performed if a DC was involved, and no residual persistence or recurrence on monitoring.</Item>
    <Item>needs_escalation: handed to Tier 3/IR with the specific evidence gaps documented.</Item>
  </ClosureCriteria>
  <Limitations>
    <Item>Host role is the verdict, not the tool. wmic/PowerShell is only the trigger; DC-or-not sets the ceiling. Establish role before anything else, and treat nim-dc-dbap01 / nim-dc2-dbap shadow creates as NTDS theft until disproven.</Item>
    <Item>Zero baseline = high fidelity. At validation wmic.exe had 0 executions estate-wide over 4h and no 4688 command line contained shadowcopy (0 hits). There is nothing legitimate to tune out; when this rule fires, believe it.</Item>
    <Item>PowerShell volume is benign automation, not shadows. The ~570 PowerShell/4h are dominated by a python.exe-parented, -EncodedCommand -NoProfile agent reading registry TimeZone values on nim-st-apv10/nim-st-apv11 (machine accounts). Don't let that volume mask a real Win32_ShadowCopy create.</Item>
    <Item>Command-line capture is bimodal - but present where it matters most. ~100% on nim-st-apv10/11 and on the DCs (validated 1/1 on nim-dc-dbap01), 0% on jump/VDI/many app hosts. On a DC you will usually get the verb; on a null-command-line host the rule cannot even fire (a blind spot, not safety).</Item>
    <Item>No Sysmon → PID-based lineage. Reconstruct the copy-out chain (create → esentutl/ntdsutil/copy/7z) with process.pid/process.parent.pid in a tight window; corroborate with process.parent.name (PIDs are reused).</Item>
    <Item>4672 is normal on a DC. Unlike a jump host, named admins legitimately receive special-privilege logons on nim-dc-dbap01 (validated: multiple *.admin accounts plus SYSTEM and the machine account). So on a DC the 4672 test proves *capability*, not *malice*; pair it with verb + hands-on context + copy/deletion follow-on.</Item>
    <Item>The copy-out is usually invisible on NBI. 4657 registry auditing is off and 4663 is File-object/SACL-scoped with no assumed SACL on NTDS; the shadow create is the enabling event you can see - recover the copied database host-side. Empty != safe.</Item>
    <Item>KB-worthy (persist to NBI customer scope): (1) wmic.exe zero-baseline and shadowcopy-command zero-baseline over 4h on logs-system.security*; (2) PowerShell 4h volume dominated by python-parented encoded-PS TimeZone reads on nim-st-apv10/11; (3) DCs nim-dc-dbap01 + nim-dc2-dbap present with command-line populated; (4) command-line/process.args host-bimodality; (5) DC 4672 includes named *.admin accounts (admin-equiv logons normal on DC); (6) 10.11.18.4 = wahab.admin RDP (type 10) origin to the DC. All observed live 2026-08-19.</Item>
  </Limitations>
</Playbook>